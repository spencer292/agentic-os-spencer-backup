# --- Brand Build System (00-brand-build) service keys ---
# All optional — every stage has a documented fallback. Recommended minimum: GEMINI_API_KEY.
GEMINI_API_KEY=            # Primary image gen (logos, brand-in-action mockups) — https://aistudio.google.com/apikey
OPENAI_API_KEY=            # Fallback image gen (gpt-image-1) — https://platform.openai.com/api-keys
FIRECRAWL_API_KEY=         # Optional: auto-scrape existing site for brand voice — https://firecrawl.dev
