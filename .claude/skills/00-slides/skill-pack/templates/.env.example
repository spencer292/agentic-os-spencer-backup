# 00-slides needs no API keys for its core pipeline.
#
# Brand visual identity is built by mkt-visual-identity, which extracts tokens
# from a URL, PDF, screenshots, or brand docs using keyless WebFetch + the
# Google Fonts resolver — no API key required.
