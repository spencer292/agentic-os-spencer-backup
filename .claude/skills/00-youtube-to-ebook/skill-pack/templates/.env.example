# YouTube Data API v3 — channel listing (only needed for channel mode)
# Direct video URLs work without this key.
# Get a free key at https://console.cloud.google.com/ (10,000 units/day free tier)
# Used by: tool-youtube
YOUTUBE_API_KEY=
