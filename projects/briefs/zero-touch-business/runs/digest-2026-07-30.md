# Route Ready — Weekly Digest — 2026-07-30

## Readiness / Health
`ztb-readiness.mjs`: `launch_ready: true`, no `pending` items. Gumroad token, product URL, lead-magnet endpoint, site deploy hook, ads account, and GSC/GA config all report present.

## Sales (Gumroad)
- **Last 7 days: 0 sales, $0.00 gross, 0 refunds.**
- **Lifetime: 0 sales, $0.00 gross.**

### Purchasability check (mandatory)
All 4 live products report `published: true` — no store-closed incident this week:
| Product | Permalink | Published |
|---|---|---|
| Cleaning Business Starter Kit | `xolvu` | true |
| Service Business Pricing Cheat Sheet (Free) | `vvgis` | true |
| Lawn Care Business Starter Kit | `hjesto` | true |
| Pressure Washing Business Starter Kit | `hntns` | true |

Both products named in the job spec (kit `xolvu`, freebie `vvgis`) are confirmed live and purchasable.

## Content
8 articles shipped since launch (`content-log.md`), all deployed to routereadykits.com:
- 07-20 residential-cleaning-contract-template-free-vs-paid
- 07-21 commercial-cleaning-contract-template
- 07-22 house-cleaning-pricing-calculator
- 07-23 cleaning-quote-template
- 07-27 cleaning-business-invoice-template
- 07-28 move-out-cleaning-checklist-template
- 07-29 deep-cleaning-checklist-template
- 07-30 cleaning-business-welcome-packet (humanizer 7→9/10, em-dash density fixed 13→0)

No failures logged this run. (Prior gap 07-24–07-26 from a stopped cron runtime was already flagged in last week's digest — no new gap this week.)

### Book clicks (The Route on Amazon)
`/book`: **38 clicks last 7 days, 56 lifetime.** Amazon-side conversions aren't visible without an Associates account — clicks are the only signal.

### Organic search (GSC)
`rr-gsc-report.mjs --json` **errored**, verbatim:
> `PERMISSION_DENIED — Google Search Console API has not been used in project 377890328473 before or it is disabled. Enable it by visiting https://console.developers.google.com/apis/api/searchconsole.googleapis.com/overview?project=377890328473 then retry.`

Same failure as the prior two digests (07-28, 07-29) — this is not the "not connected" case, the GSC property/site URL is configured, but the Search Console **API** itself has never been enabled in the GCP project. No organic clicks/impressions/position data available. Not substituting Cloudflare Worker request counts — those count crawlers/assets, not visitors.

## Ads
Live pull via `rr-weekly-ads-report.mjs` **errored** this run, verbatim:
> `AUTH_FAILED: invalid_grant — Token has been expired or revoked.`

This is a **new failure** — yesterday's ads-manager run (2026-07-29, per `ads-log.md`) completed successfully ($25.67 spend / 24 clicks trailing 7-14d, all 24 keywords enabled, 0 disapprovals, MTD $25.67 of $75 cap). The `ROUTE_READY_ADS_REFRESH_TOKEN` has broken between then and now. Not reporting yesterday's numbers as current — they are stale as of this run. Fix: Spencer re-runs `node projects/briefs/zero-touch-business/scripts/rr-mint-refresh-token.mjs` to re-authorize and write a fresh token.

### Paid vs organic split
Cannot be produced this week — both sides are broken: GSC API disabled (no organic figure) and the ads refresh token just expired (no live paid figure either). Third consecutive week this split has been blocked, and the ads side is now also down.

## Spencer time
`spencer-minutes.md`: **70 minutes logged**, all from setup on 2026-07-19. No new touches logged since.
**$/Spencer-hour = $0.00 ÷ (70/60 hr) = $0.00/hr** — lifetime gross is still $0.

## Three-bucket status

**Needs Spencer:**
- **New:** Ads refresh token expired/revoked (`invalid_grant`) — run `node projects/briefs/zero-touch-business/scripts/rr-mint-refresh-token.mjs` to re-authorize. This also silently breaks `ztb-ads-manager` and `ztb-ads-budget-guard` crons until fixed — the budget guard can't pause an overspend it can't see.
- Enable the Search Console API in GCP project `route-ready-ads` (377890328473) — one click, unblocks organic reporting and the paid/organic split. Link above. Third week open.
- Gumroad payout/KYC flagged "verify cleared" in the post-launch queue, never tested — 0 sales to date to trigger a payout. Confirm before the first sale, not after.
- Phase 0 optional items still open (not blocking): Pinterest business account, business-entity/tax note.

**Paused (not cron-backed):** None currently — content publishing, ads management, budget guard, and this digest are all cron jobs (`ztb-content-publisher`, `ztb-ads-manager`, `ztb-ads-budget-guard`, `ztb-weekly-digest`). Pinterest (channel #3) hasn't been started yet — backlog, not a stalled manual step.

**Stalled-silently watchlist:**
- Google indexing status still unknown until the GSC API is enabled — can't distinguish "not indexed" from "indexed but no clicks."
- Ads reporting/management is now silently blind: with the refresh token dead, `ztb-ads-manager` (weekly) and `ztb-ads-budget-guard` (daily) cannot see spend or pause an overspend until re-authorized. Campaign keeps serving against real budget with no automated oversight in the meantime.
- Gumroad account/payout holds — unverified, no sales yet to surface a hold.
- Ad disapprovals — unknown this run (couldn't query); last confirmed clean 07-29 (all 3 RSAs approved).

---

## Summary
- Revenue this week: **$0** (0 sales)
- Lifetime revenue: **$0** (0 sales)
- Articles shipped: **8** total since launch (07-30 added), no gaps this week
- Ad spend: **unknown this run** — refresh token expired (`invalid_grant`); last known 07-29: $25.67/7-14d
- $/Spencer-hour: **$0.00** (70 min logged, $0 lifetime gross)
- Top attention items: (1) **new** — ads refresh token expired, budget guard is now blind to live spend; (2) GSC API still disabled, 3rd week blocking organic + paid/organic split; (3) zero sales after 11 days live with real ad spend flowing — economics still unproven
