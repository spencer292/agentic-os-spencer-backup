# Route Ready Weekly Digest — 2026-08-05

## Revenue-blocking check (mandatory, step 2b)

All 4 Gumroad products are **published**:

| Product | Permalink | Published |
|---|---|---|
| Cleaning Business Starter Kit | `xolvu` | true |
| Lawn Care Business Starter Kit | `hjesto` | true |
| Pressure Washing Business Starter Kit | `hntns` | true |
| Service Business Pricing Cheat Sheet (Free) | `vvgis` | true |

Note: this run's `/v2/products` response returned raw product IDs instead of the custom slugs in the `permalink` field (an API response quirk, not a config change) — cross-checked by curling all 4 `gumroad.com/l/{slug}` URLs directly, all returned HTTP 200. No store-closed incident this week.

## 1. Readiness / health

`ztb-readiness.mjs`: `launch_ready: true`, `pending: []`.

Same caveat as last run: the readiness probe only checks that `ROUTE_READY_ADS_CUSTOMER_ID` and `ROUTE_READY_GSC_*` env vars are *present*, not that the tokens are *valid* — both `ads_account` and `ga_or_gsc` read `true` here while the live calls below both fail on expired/revoked OAuth tokens. Treat this probe as "configured," not "working."

## 2. Sales (Gumroad)

- **Last 7 days:** 0 sales, $0.00 gross, 0 refunds
- **Lifetime:** 0 sales, $0.00 gross

## 3. Content

**Shipped since the last digest (2026-08-04 → 2026-08-05), from `runs/content-log.md`:**

| Date | Slug | Word Count | Deploy Status |
|---|---|---|---|
| 2026-08-05 | what-to-include-in-a-cleaning-service-agreement | 1368 | Deployed |

1 article shipped this week, 0 failures. Lifetime total in the log: 11 articles (since 2026-07-20), all deployed.

### 3b. Book clicks (`rr-metrics.mjs`)

- `/book` (The Route on Amazon): **15 clicks last 7 days**, **68 lifetime**.
- Amazon-side conversions are not visible without an Associates account — clicks are the only signal.

### 3c. Organic search (`rr-gsc-report.mjs --json`)

**ERROR — AUTH_FAILED**: "Token has been expired or revoked." Fix per the script: re-run `rr-mint-gsc-token.mjs`.

Unchanged from the 2026-08-04 digest — the GSC OAuth token is still stale. No clicks/impressions/CTR/position/query/sitemap data available this run.

## 4. Ads

`_digest-ads-pull.mjs` — **ERROR: OAuth refresh failed** — "Token has been expired or revoked." No GAQL calls were made, no spend/clicks/conversions available this run.

This is the same `ROUTE_READY_ADS_REFRESH_TOKEN` issue first seen 2026-07-30, now unresolved for 6+ consecutive reporting days (7/30, 7/31, 8/4, and this run — 8/1-8/3 not separately checked). Per `runs/ads-log.md`, the daily budget guard has been hitting the identical AUTH_FAILED every day it's checked (2026-08-04 confirmed) — no entry yet for 2026-08-05 as of this digest.

Last known-good state, from `runs/ads-log.md` (2026-07-29 run): MTD spend $25.67 of $75 cap, 24 clicks over trailing 7/14 days, all 24 keywords enabled, 0 negative-keyword candidates, 0 pause candidates, all 3 RSAs approved. That snapshot is now 7 days stale.

### 4b. Paid vs organic split

Cannot be computed this run — both sides are blocked: ads reporting by the expired `ROUTE_READY_ADS_REFRESH_TOKEN`, organic reporting by the expired GSC token. This is at least the fourth digest in a row without a usable traffic-attribution number.

## 5. Spencer time / $-per-Spencer-hour

From `runs/spencer-minutes.md`: **70 minutes logged**, all from initial setup on 2026-07-19. No touches logged since — unchanged from last week's digest.

Lifetime gross = $0.00 → **$/Spencer-hour = $0.00** (0 lifetime sales ÷ 70 min logged). Formula is live; the number is zero because there have been no sales yet, not because the log is stale.

## 6. Three-bucket status

**Needs Spencer:**
- Re-mint `ROUTE_READY_ADS_REFRESH_TOKEN` (`node projects/briefs/zero-touch-business/scripts/rr-mint-refresh-token.mjs`) — blocks ads reporting and the daily budget guard. Unresolved since 2026-07-30 (7 days).
- Re-mint the GSC token (`node projects/briefs/zero-touch-business/scripts/rr-mint-gsc-token.mjs`) — blocks all organic-search visibility. Both mint scripts open an interactive browser consent flow — not cron-safe, must be run by Spencer directly.

**Paused (not cron-backed):** none identified — content publishing, the weekly ads-manager, and the daily budget guard are all cron-driven; current failures are external auth blockers, not missing automation.

**Stalled-silently watchlist:**
- **Ads OAuth failure is now a week old** and blocks two separate crons (weekly ads-manager + daily budget guard) simultaneously — while it's *erroring loudly* (not silent), the budget guard cannot enforce the $75/mo cap while blind, so a runaway-spend scenario would go undetected until Spencer re-mints the token. Treat as time-sensitive even though each individual failure is logged.
- Google Search Console / organic indexing status — unknown this run (auth failure). Cannot distinguish "not indexed" from "indexed but no clicks" until the token is re-minted.
- Ad disapprovals — unknown this run (auth failure blocked the check); last known-good (7/29) showed all 3 RSAs approved, 0 disapprovals.
- Gumroad account holds — none observed; sales API responded normally and all 4 products verified purchasable (published + live URL check).
