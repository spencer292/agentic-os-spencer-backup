# Route Ready Weekly Digest — 2026-08-07

## Readiness / health
`ztb-readiness.mjs` — all green, `launch_ready: true`, `pending: []`.
(gumroad_token, gumroad_product_url, lead_magnet_endpoint, site_deploy_hook, ads_account, ga_or_gsc all `true`.)

## Purchasability check (mandatory)
All 4 Gumroad products are `published: true` — no store-closed incident.

| Product | Short URL | Published |
|---|---|---|
| Cleaning Business Starter Kit (`xolvu`) | routeready.gumroad.com/l/xolvu | true |
| Service Business Pricing Cheat Sheet — free (`vvgis`) | routeready.gumroad.com/l/vvgis | true |
| Lawn Care Business Starter Kit | routeready.gumroad.com/l/hjesto | true |
| Pressure Washing Business Starter Kit | routeready.gumroad.com/l/hntns | true |

## Sales (Gumroad API — live)
- Last 7 days: **0 sales**, $0 gross, 0 refunds
- Lifetime: **0 sales**, $0 gross
- This is a genuine zero — store is open, all products published. A real demand signal, not an outage.

## Content
12 guides shipped since 2026-07-20 (most recent 2026-08-07: "how-to-get-first-cleaning-clients-without-ads"), all logged as deployed to route-ready-site / routereadykits.com. No failures in `content-log.md`. Cadence: `ztb-content-publisher` cron runs Mon/Wed/Fri 06:30, `active: true` — this pipeline IS cron-backed.

### Book clicks (`/book` — The Route on Amazon)
- Last 7 days: **15 clicks**
- Lifetime: **73 clicks**
- Amazon-side conversions not visible (no Associates account access) — clicks are the only signal.

### Organic search (Google Search Console)
**FAILED — AUTH_FAILED: "Token has been expired or revoked."** Fix: re-run `rr-mint-gsc-token.mjs`. This is the second consecutive week this token has died — worth checking whether the Google Cloud OAuth app is still in "Testing" publishing status, which auto-expires refresh tokens after ~7 days regardless of use.
No organic traffic figures available this week. Not substituting Cloudflare Worker request counts — those count crawlers/assets, not visitors.

## Ads
`ROUTE_READY_ADS_CUSTOMER_ID` is set, but `rr-weekly-ads-report.mjs` **FAILED — AUTH_FAILED: invalid_grant, "Token has been expired or revoked."** Same failure mode as GSC above (likely same root cause — OAuth app in Testing mode). No spend/click/conversion data available this week. `ztb-ads-manager` cron (Tue 07:30, active: true) will hit the same wall on its next run.

### Paid vs organic split
Not computable this week — both ad-click and GSC organic sources are auth-blocked. No traffic attribution number to report; do not treat this as "no traffic," it's "no visibility into traffic."

## Spencer time
`spencer-minutes.md` — 70 minutes logged (all from 2026-07-19 setup: domain, Gmail, Cloudflare, Gumroad).
Lifetime gross: $0. **$/Spencer-hour: $0.00** (0 ÷ 1.17 hrs) — no revenue yet to divide.

## Three-bucket status

**Needs Spencer:**
- Re-mint GSC token (`rr-mint-gsc-token.mjs`) — blocks all organic search visibility.
- Re-mint/fix Route Ready Ads refresh token — blocks all ads reporting and this week's `ztb-ads-manager` run.
- Consider moving the Google Cloud OAuth consent screen to "Published" (out of Testing) if these tokens keep dying weekly — that's the usual cause of a 7-day refresh-token expiry.
- No Phase 0 checklist items pending — readiness is fully green.

**Paused (not cron-backed):**
- This weekly digest itself is invoked as a scheduled job (per `ztb-weekly-digest` cron), so it's covered.
- No other manual-only steps identified this week — content and ads pipelines are both cron-backed (Mon/Wed/Fri and Tue respectively), even though ads is currently failing on auth.

**Stalled-silently watchlist:**
- Google Search Console — currently auth-failed, not just "not connected." Detected this run; will keep failing silently until token is re-minted.
- Route Ready Ads — same auth-failed state; `ztb-ads-manager` cron will fail quietly next Tuesday unless fixed first.
- Gumroad account holds — checked, none detected (all products live, sales endpoint responsive).
- Ad disapprovals — cannot check this week (auth blocked upstream of that data).

---
*Read-only run — no writes made to Gumroad or Google Ads.*
