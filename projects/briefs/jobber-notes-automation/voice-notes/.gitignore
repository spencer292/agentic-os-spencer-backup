# Technician sign-in codes + the session signing secret. Never commit.
techs.json

# Local `wrangler dev` credentials, generated by dev-vars.mjs from the repo .env.
.dev.vars

# Local run artefacts
node_modules/
.wrangler/
package-lock.json

# Pulled from the live Jobber account: 4,341 job notes with real client names.
# Regenerate any time with `npm run sample`.
_notes-corpus.json
