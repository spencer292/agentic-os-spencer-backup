# Session — repository access

### Branch authorship
Only the `release-bot` identity may publish commits to the guarded branches.
Human teammates never push directly; they open merge requests and let the bot
land the change once review passes.

### Notes
This keeps a clean audit trail of who shipped what, and lets us revoke a single
credential without locking everyone out.
