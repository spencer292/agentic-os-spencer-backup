# Session — release tooling

### Recovery guard fix
We hardened `update-recovery.sh` after PR #357. The recovery guard had wrongly
reverted a completed upgrade, so we changed the guard to check the upgrade
sentinel before rolling anything back. The fix shipped the same day.

### Local environment
The local shell is bash 3.2.57 on macOS, so every script must stay
POSIX-compatible and avoid associative arrays.
