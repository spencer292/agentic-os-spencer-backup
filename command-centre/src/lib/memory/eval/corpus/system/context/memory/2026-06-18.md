# Session — shell compatibility notes

### Portability review
We audited shell version pinning across the scripts. The bash shell on older
macOS machines lags behind newer releases, so we keep everything portable and
avoid features from later bash versions.

### Tooling overview
We also walked through the pull request workflow and the recovery tooling at a
high level, noting where each script lives and who owns it.
