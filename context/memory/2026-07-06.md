# 2026-07-06

## Session 1

### Project
technician-route-automation (`projects/briefs/technician-route-automation/`)

### Goal
Build a hands-off daily automation: pull jobs from Jobber → OptimoRoute optimizes → push
optimized day/time/order back into Jobber, twice daily, logged to Notion. Replace Spencer's
~hours/week of manual route hand-out.

### Deliverables
- n8n **child** workflow `gr8kf904tjC2ckcA` (single-date engine) + **parent** `XLhh2TB89NwSlBRX`
  (weekly loop, schedule 5am/6pm Pacific). BOTH DEACTIVATED.
- n8n credentials: OptimoRoute `cA1fagf9j84ZWxnu` (httpQueryAuth); Jobber **`NitrghZSAjZamvqp`**
  ("Jobber OAuth2 (native)", Authentication=**Body**) — the durable one; Notion `3O7pd2vMtCIPXUqu`.
- Notion DB **🚚 Route Sync Log** `19265c126f534b8fa59b98856c42531d`.
- `projects/briefs/technician-route-automation/`: brief.md, build-notes.md, reference-engine.mjs,
  n8n-workflow.json, n8n-parent.json, and recovery scripts (diagnose-and-plan / apply-fix /
  revert-backlog).

### Decisions
- Delivery = Notion (not email — Workspace admin-locked app passwords / OAuth-only).
- Durable Jobber auth = n8n-native OAuth (key fix: credential Authentication must be **Body**).

### ⚠️ INCIDENT (must read next session)
- Flipped to LIVE and ran once. It **scrambled the live Jobber board**: it (a) blanket-set ~535
  "anytime" visits to a 7am–7pm placeholder, and (b) MOVED visits onto wrong days (07-10 emptied,
  07-07 overloaded). Root cause: I only built the OR→Jobber write-back + a bad placeholder step;
  the **Jobber→OptimoRoute ingest + re-optimize step was never built**, and **tech assignments were
  never handled**.
- Recovery (partial, via scripts Spencer ran): moved 302 visits back to correct OR day/time,
  reverted 135 exact-7am–7pm placeholders to anytime. Residual left: 19 recurring-job duplicates +
  wrong tech assignments. **Spencer is fixing the rest MANUALLY.** He was (rightly) very frustrated.
- **Automation is OFF. Do NOT reactivate.** OptimoRoute remains the correct source of truth.

### Open threads
- REBUILD required, done right: Jobber → OptimoRoute (ingest new jobs, start_planning/re-optimize:
  correct day + route + order) → back to Jobber with real OR times in sequence + correct tech.
  7am–7pm placeholder is ONLY for far-out visits, never this week.
- Jobber CLI re-authed (app callback needs localhost:8734 alongside n8n's oauth.n8n.cloud).

---

## Session 2

### Goal
Set up a Chrome window Claude can drive, package it as a reusable skill, then use it to find corporate LinkedIn prospects and build a LinkedIn audience-growth engine for Got Moles.

### Deliverables
- `browser/launch.mjs`, `browser/cdp.mjs` — zero-dependency Chrome DevTools Protocol driver (Node 24 built-in WebSocket/fetch; no Playwright/installs). Launches a visible Chrome on a persistent profile at `C:\Users\spenc\.agentic-chrome-profile`.
- `.claude/skills/tool-browser/SKILL.md` — new skill wrapping the driver; platform-agnostic login pattern; read-free / write-confirmed rules.
- `projects/tool-browser/2026-07-06_linkedin-corporate-prospects.md` — Segment 2 shortlist (golf supers, HOA managers, landscapers) + warm-path analysis.
- `projects/tool-browser/2026-07-06_linkedin-outreach-drafts.md` — 22 bespoke connection notes + follow-ups.
- `projects/tool-browser/2026-07-06_linkedin-growth-engine.md` — profile rewrite (headline + About), 3 content pillars, 3x/week cadence, 2 sample posts, safe connect/engage plan.
- `projects/tool-browser/2026-07-06_linkedin-connection-list.md` — ~55 categorized targets with profile URLs + week-one sequence.

### Decisions
- Zero-dependency CDP driver chosen because package installs are blocked and Node 24 has built-in WebSocket/fetch. Scripts kept at repo-root `browser/` (mv into skill folder was permission-blocked; root paths verified working).
- tool-browser deliberately NOT registered in AGENTS.md/README (shipped files — consumer branching policy). Skill still triggers from `.claude/skills/` frontmatter.
- Never bulk-blast LinkedIn connection requests via automation (ban risk) — Claude researches/tees up; connect/follow clicks stay human-paced.
- User's real LinkedIn = `/in/spencer-hill-2132b92ab/`. An empty duplicate account was accidentally created via Google SSO (spencer.hill1111@gmail.com) then signed out of — may want deleting later.

### Open threads
- Profile near-blank (headline only, no About, 30 connections) — paste new headline + About, add Services section, turn off "Open to work"; banner still to generate.
- Draft week-one content posts (Mon/Wed/Fri) — not yet done.
- Decide how to send connection requests (recommended: Claude opens each profile, user clicks Connect).
- Fill the 4 connector bracket-openers (Patrick LaCroix, Nick Granberg, Brandon Sechrist, Mark Pyrah).
- Untapped niches for future list batches: hotels/resorts, cemeteries, developers, sports clubs.

---

## Session 3

### Project
jobber-notes-automation (`projects/briefs/jobber-notes-automation/`)

### Goal
Stop the technicians doing the same info three times (visit note + checklist + manual visit
scheduling). Make the tech's Jobber **note** the single source: auto-fill a status **report** and
auto-manage the **follow-up visit** from the note. Then run it **independent of this machine**.

### Deliverables
- `projects/briefs/jobber-notes-automation/`: brief.md (spec + decoded note shorthand + canonical
  scheduling rules), parse-note.mjs, decide.mjs, engine.mjs, test.mjs (14 tests passing),
  report-preview.mjs, report-sync.mjs
- `cron/jobs/jobber-visit-followups.md` — local daily cron (report live + scheduling dry-run)
- 8 **Job Custom Fields** created in Jobber (Spencer, by hand): Latest Activity, Moles Caught (last
  visit), Misses (last visit), Trap Inventory, Next Action, Customer Shown, onX Mapped, Total Caught
- **Report is LIVE** — synced from notes for all 31 of today's visited jobs via `report-sync.mjs --write`
- Notion proof DB: https://app.notion.com/p/e88c63ce39b84da4922972a7309010e7
- n8n report workflow: `2dxtg73X1JUvLUTr` — https://gotmoles.app.n8n.cloud/workflow/2dxtg73X1JUvLUTr

### Decisions
- Notes stay; **checklist retired**; report target = **Jobber Job Custom Fields** (writable via
  `jobEdit` under Jobs:write — the custom-fields *config* scope is NOT needed; read field ids off
  the job even though the config list is scope-blocked).
- Visit scheduling (canonical, in brief): `Add visit`→+7d, `2 weeks`→+14d, `Monthly`/none→leave.
  **Same calendar month → pull next visit earlier (`visitEditSchedule`); later month → leave it, ADD
  a new visit (`visitCreate`).** Never pull a future-month visit back (the Annette Wood error,
  corrected + tested). Report live daily; scheduling dry-run until trusted then flip `--execute`.
- Host machine-independently on **n8n** via **manual token refresh** (n8n's built-in Jobber OAuth
  errored at token exchange). Workflow refreshes the token itself (form-encoded like the CLI) →
  Bearer header.
- **Refresh-token rotation turned OFF** on the app → refresh token now permanent/stable.

### Open threads
- **n8n report workflow `2dxtg73X1JUvLUTr` — finish verifying.** Auth solved (token node green);
  read/write nodes patched for an empty-body bug (static read body + `$json.body` write ref).
  PENDING: Spencer re-runs **Execute Workflow** → confirm read/build/write all-green → **activate**
  (daily 6pm) → then **disable the report half of the local cron** to avoid double-writes.
- **⭐ Next session, try the EASIER auth:** Session 1 (route-automation) discovered a WORKING n8n
  Jobber credential — **`NitrghZSAjZamvqp` "Jobber OAuth2 (native)", Authentication = Body**. My
  report workflow used manual token refresh because I hit the broken `414IO8xZGgF2ztgx` "auto" one.
  Consider switching the report workflow's Jobber calls to the native `NitrghZSAjZamvqp` credential
  (Authentication=Body) — simpler, and avoids the pasted-secret problem below.
- **SECURITY cleanup (deliberate, coordinated):** the app `client_secret` and the Jobber
  `refresh_token` got exposed in the chat transcript (n8n echoed them). Regenerate the client secret
  + re-mint the refresh token, then update `.env` + the n8n token node. Breaks CLI + n8n until
  updated — do as one step. (Moot if we switch to the native credential above.)
- **Scheduling half still local-cron dry-run.** Phase 2: port pull/add into a second n8n workflow.
  Both `visitEditSchedule` (pull) + `visitCreate` (add) proven live.
- Note: Session 1 also re-authed the Jobber CLI and I re-authed again (minted a fresh token, rotation
  now off). Watch for cross-session token contention — rotation-off mitigates it.
- Hardening TODO: paginate the jobs pull (`first: 40/50`) so busy days never miss a visit; null-value
  policy for fields the note omits; a few custom-field *names* have trailing spaces (harmless).

---

## Session 4

### Project
cash-flow-projection (`projects/briefs/cash-flow-projection/`)

### Goal
Build a repeatable cash-flow projection from live Jobber invoice data (bill at start of month → invoices → paid in due course), then stand up weekly automation for it.

### Deliverables
- `projects/briefs/cash-flow-projection/` — brief.md, `scripts/pull-invoices.mjs` (read-only paginated Jobber pull, throttle backoff, deposit field), `scripts/build-projection.mjs` (deposit-aware model: collection curve, forward projection, new-vs-existing segmentation, collections chase list, seasonal new-billings forecast), the dated report, and `CRON-RUNTIME-ISSUE-for-roy.md`.
- Notion: "Cash-Flow Projection — 2026-07-06" page + "Cron runtime… for Roy" page + **"Cash-Flow Snapshots — Got Moles"** database (data source `8020d3e9-b096-4d08-a9a5-eeb848ab4c1d`).
- `cron/jobs/weekly-cash-flow-projection.md` — local cron job, STAGED INACTIVE.
- n8n workflow **"Weekly Cash-Flow Projection (Jobber → Notion)" id `Aly1V11tqwSQhTls`** (INACTIVE).

### Findings
- Collection curve ~70% same-month, ~96% within one month; bad debt ~0.1%. Open A/R $49,386 ($33,966 past due). Avg billed ~$66.8k/mo. New customers = 7% of jobs but ~18% of revenue; acquisition strongly seasonal (fall peak).
- Deposits ($150 up-front, NEW customers only) aren't in `paymentsTotal`/`invoiceBalance`; crediting them makes aged months reconcile to ~100% collected.

### Decisions
- Automate via **n8n Cloud, not local cron** — n8n is always-on; the local cron runtime is broken on this Windows box (missing `run-hidden-command.ps1`; extensionless-shim spawn ENOENT; `command-centre` deps were uninstalled). Escalated to Roy.
- n8n design: reuse existing "Jobber OAuth2 (native)" credential (`NitrghZSAjZamvqp`); weekly pull OPEN invoices only + STORED curve (from local `projection.json`); output to the Notion snapshots DB.

### Open threads
- **n8n `Aly1V11tqwSQhTls`:** user to connect a Notion credential on `Write Snapshot to Notion` + share the snapshots DB, then Execute. Then "check executions" and I debug node-by-node.
- Local cron `weekly-cash-flow-projection` stays inactive until Roy fixes the Windows launcher.
- Fixed-page headroom = 300 open invoices/status (fine at 155/133 now); stored curve MODEL drifts — refresh from local `projection.json` periodically.

---

## Session 5

### Project
technician-route-automation (`projects/briefs/technician-route-automation/`)

### Goal
Pull up the Jobber ⇄ OptimoRoute route automation and check its current status.

### Notes
- Reviewed brief.md + build-notes.md, then checked LIVE n8n state (per ground-truth rule —
  build-notes still read "LIVE IN PRODUCTION", which is stale).
- **Verified current state:** parent `XLhh2TB89NwSlBRX` = **active:false (paused)**; child
  `gr8kf904tjC2ckcA` = active:true. Schedule triggers live in the parent, so with the parent off
  the automation does NOT run — effectively paused. (Matches Session 1's "do not reactivate".)
- Spencer confirmed he **paused it deliberately** — "there are things that need to get fixed first."
  Did not enumerate the fixes yet.

### Open threads
- **Tomorrow: get the fix list from Spencer and work through it before any reactivation.** The
  Session 1 INCIDENT note is the context — the rebuild must add the missing Jobber→OptimoRoute
  ingest + re-optimize step and correct tech-assignment handling; keep parent OFF until dry-run
  validated against a hand-routed day.
