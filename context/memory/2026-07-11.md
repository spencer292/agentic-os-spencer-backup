# 2026-07-11

## Session 1 (continuation of 07-10 route marathon)

### Goal
Finish the route rebuild: territory grid, final board, write-back to Jobber.

### Deliverables
- **WRITE-BACK COMPLETE: 508/508 visits live in Jobber** (434 first pass + 74 tech-assign repaired —
  correct mutation: visitEditAssignedUsers(visitId:, input:{assignedUserIds:[...]}) NOT id/assignedUsers).
- **home-slots.json** — THE template: 508 customers locked to approved (day,tech); future weeks =
  home-slots first, territory-grid.json for new customers, optimizer sequences only.
- territory-grid.json (rough-draft-v1, ~125 ZIPs), zone-map artifacts (grid page + map w/ ZIP hulls).
- Backfill: canary 21 OK (incl Spencer job 7778), near-term 1479 written (41 fails = deleted visits).
  TAIL PENDING: 62,909 — run: node projects/briefs/technician-route-automation/backfill-time-windows.mjs live --max 70000
- Winning method: mirror-lastweek.mjs + Spencer's ~90 manual straggler moves (moves.mjs batches).

### Open threads (next session)
- Kick/verify backfill tail (command above; resumable).
- Rebuild WF-1/WF-2 in n8n around home-slots + grid; schedule 04:30+13:00 (needs dedicated Jobber
  app for n8n — credential died 2x). Email-freeze rule: date locks 14:00 D-1.
- Density metrics in Notion log; cadence A/B alignment (Phase 3); Tavis return = revert Cory temp ZIPs.
- Placeholder convention: new jobs get 7:00AM-7:00PM window (Spencer does manually at booking).

- CONFIRMED: Jobber jobEdit mutation accepts arrivalWindow (JobEditInput has arrivalWindow field) — per-job arrival-window sweep for ~5k existing jobs is fully automatable. Pattern: scan jobs -> canary #7778 -> drain. Spencer sets company default for NEW jobs himself. TOP TASK NEXT SESSION.

- BACKFILL COMPLETE (overnight into 07-12): 64,156 future visits written with 7:00AM-7:00PM windows, 0 pending, 273 failed (deleted visits, ~0.4%). Every future visit in Jobber now has a time window.
