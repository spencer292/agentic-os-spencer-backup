# 2026-07-09

## Session 1

### Project
technician-route-automation (`projects/briefs/technician-route-automation/`)

### Goal
Supervised ONE-DAY manual route run for Friday 2026-07-10 (NOT the paused automation, NOT the
rebuild): Jobber techs already correct → push to OptimoRoute → optimize → write optimized
order+times back to Jobber. This is the correct end-to-end flow done by hand, checkpointed.

### Deliverables
- 90 OptimoRoute orders created on 07-10 (unique orderNo `<job>-0710`, `operation:CREATE` via
  `create_order` so they geocode + never collide with recurring-job orders), each locked to its
  tech via `assignedTo.serial = <full name>` (serial == Jobber tech full name — exact match).
- `start_planning` → 90 stops routed, 0 unassigned, 0 tech reassignments. Cammeron 34 (06:48–15:06),
  Cory 27 (07:04–14:50), Luke 29 (07:13–14:02).
- **90 Jobber visits updated** via `visitEditSchedule` (start = optimized Pacific time, +3h window,
  TZ America/Los_Angeles) — 90/90 ok, verified against the board. Routed through sanctioned
  `jobber-api.mjs` (no direct .env).
- `projects/briefs/technician-route-automation/2026-07-10_optimized-routes-REVIEW.md` +
  `2026-07-10_writeback-plan.json`.

### Decisions
- orderNo=jobNumber is UNSAFE here: recurring customers reuse the same job number across dates →
  `create_or_update_orders` update-matches by orderNo and can drag another date's order onto 07-10
  (2 orders 8061/8084 did this on the first attempt; deleted + redone cleanly). Fix = unique
  `-0710` orderNos + per-order `create_order` (only endpoint that geocodes).
- Bulk `create_or_update_orders` does NOT geocode (needs lat/long); `create_order` does.
- Window = +3h (matches the prior automation's convention). Customers not notified (confirmed prior).

### Open threads
- **Spencer to delete the duplicate Jobber visit for job 7494** — kept `…MjI0MDE2Njc0NA==` (now 11:10),
  delete `…MjI0MDE2Njg0Ng==` (still 07:51). Same house/tech, double-booked.
- One address (job 6028) geocoded partial → created with explicit coords; fine.
- The scheduled AUTOMATION (parent `XLhh2TB89NwSlBRX`) remains OFF — untouched. This was a manual run.
- Reusable script pattern for future single-day runs lives in the scratchpad (fetch-visits /
  build-orders / create-all / writeback) — could be promoted into the rebuild.
