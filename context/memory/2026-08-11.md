# 2026-08-11

## Session 1

### Project
got-moles-scale (Ninety scorecard) + tool-jobber (TMCP audit)

### Goal
Run the TMCP audit for a true job-level total, then explain why the Ninety.io scorecard number is higher.

### Deliverables
- `projects/tool-jobber/scripts/tmcp-tag-audit.mjs` — added multi-job-client reporting: `clientsWithMultipleJobs`, `extraJobsFromMultiClients`, and a full `multiJobDetail` block, so every future run reports job-level truth alongside the client count
- `projects/tool-jobber/data/2026-08-11_tmcp-tag-audit.json` — audit run: 812 live jobs scanned, **674 live TMCP jobs** across 653 distinct clients, MRR-equivalent $74,162.19
- `projects/briefs/got-moles-scale/scripts/ninety-weekly-push.mjs` — `tmcActiveOn` now takes the week being computed and excludes archived jobs from the cutoff forward; one-time step-explanation notes pushed to `tmcp_active` and `tmcp_mrr`; stale "silent cancellation" wording in the recurring gap note replaced for corrected weeks
- `projects/briefs/got-moles-scale/scripts/ninety-kpi-map.json` — added `archivedJobFixFrom: "2026-08-10"` + `_archivedJobFixNote` recording why history cannot be restated

### Findings
- **674 live TMCP jobs, not 653.** The 653 figure everywhere else is CLIENTS — "TMCP - Active" is a tag on the Client record. 11 clients carry more than one live TMCP job, worth 21 extra jobs. Prologis - Sumner Landing alone runs 10 (one per building, $1,984.75/mo).
- **The Ninety number (687) was inflated by 26 archived jobs.** The guard only dropped an archived job once its `endAt` had passed, but TMCP jobs carry a 10-year end date (earliest affected `endAt`: 2030-07-02), so it never fired. Also inflated TMCP MRR by $2,507.49/mo. Constant 26 in every pushed week, so week-over-week growth (+24, +18) was never wrong — only the level.
- **Gap fully reconciled:** 687 − 26 archived = 661 as of Sun 08-09; + 4 jobs starting after the week end + ~9 created after the data pull (cache tops out at job #8335; audit saw #8350/#8351) = 674 live today. Ninety is also RECURRING-only; the audit includes 1 one-off TMC job.
- Four multi-job clients look like possible duplicate jobs rather than second properties — identical price, no title, nothing distinguishing: Clark Potter (#5536/#5676, $85), Tom Schlimme (#6720/#6890, $85), SLPPLLC (#5511/#6653, $115), Ganesh Thirumalai (#5087/#7834, $85 — has a Repeat Customer Discount line, leans genuine). ~$370/mo at stake.
- Tag hygiene: 3 TMCP jobs whose client lacks the tag (#8332 Nancy Collinsworth, #8340 Ken Lohse, #8219 Trent Bryan — all new since the 08-05 run); 3 clients tagged Active with no live TMCP job (Tyler Smythe, Barbee Mill HOA, Larena Walshe). Churned tag clean.
- Jobber's `Job` type has **no `archivedAt`** — only `updatedAt`, which moves on any edit. Schema-checked.

### Decisions
- **Freeze history, correct forward** (Spencer chose option 2 over restating history). Cutoff set to `2026-08-10`, the first week the cron has not yet pushed, so no value already on the board moves. Verified: a dry run reproduces the published weeks exactly — 645 / 669 / 687.
- The correction is annotated on the board rather than left to be read as churn: one-time notes on `tmcp_active` and `tmcp_mrr` state the true size (26) and that earlier weeks are deliberately left as published.
- Nothing pushed to Ninety this session — dry run only. The corrected number lands on its own when the cron runs Monday 2026-08-17.

### Open threads
- Corrected value first publishes 2026-08-17: expect ~672 vs 687, a visible step of about −15 (26 removed, offset by a week of real signups), MRR ~$66.7K vs $67,991. Confirm the step notes actually landed on the board.
- 6 tag mismatches unfixed (3 missing the Active tag, 3 tagged with no job) — offered, not actioned.
- 4 possible duplicate TMCP jobs need Spencer's eyes (Clark Potter, Tom Schlimme, SLPPLLC, Ganesh Thirumalai).
- Live tag sweep on 08-11 read 652 Active / 19 Churned vs 653/18 in the audit the day before — one client moved Active → Churned, so Monday's run posts a churn delta of 1.

## Session 2

> Reconstructed at wrap-up on 2026-08-11 evening after the PC shut down mid-session.
> Source: `context/memory/2026-08-11.aos.md` turn captures + live file state. Covers every
> interactive stream after Session 1's wrap-up (~10:15 PT). Several streams ran concurrently.

### Project
Multiple: jobber-text-automation (new) · quo-account-appeal (new) · gmail-draft-triage (new) ·
technician-route-automation · lead-alerts · callrail-faq (call grading) · tool-jobber (Quick Fix audit)

### Goal
No single goal — the day broke into seven parallel streams, most of them reactive
(Quo shut the phone account down at 10:18 PT and reset the day's priorities).

### Deliverables

**Past-due collections by SMS — first live run** (`projects/briefs/jobber-text-automation/`)
- `brief.md` — full Level 2 brief: verified that Jobber's texting is closed at the API layer
  (no inbound webhook, no send mutation, `MessageInterfaceConnection` exposes only `totalCount`),
  so a browser robot on the `tool-browser` profile is the only door. Carries the hard-won
  selector map, the 670-char cap, and the two-factor targeting rule.
- `scripts/` — `build-collection-queue.mjs`, `send-collection-texts.mjs`, `check-recent-activity.mjs`,
  `enrich-payment-methods.mjs`, `autopay-audit.mjs`, `autopay-target-tiers.mjs`, `find-thread.mjs`,
  `mark-contacted.mjs`
- **Live run ~17:00–17:50 PT: 40 clients texted, $5,914 of past-due invoices, zero failures.**
  Every message matched the right thread, passed name verification, confirmed accepted by Jobber.
  21 of 40 have a card on file and were offered `reply CHARGE`.
- Held back deliberately: 8 commercial/HOA/municipal ($3,580 — phone instead), 28 rolled to the
  next run ($2,475), 3 already hand-texted by Spencer.

**Quo account termination + appeal** (`projects/briefs/quo-account-appeal/`)
- `brief.md`, `consent-evidence.md`, `callback-pool-audit.md`, `appeal-email.md` + all three as PDFs
  in the project folder and `~/Downloads/`
- `scripts/audit-callback-pool.mjs`, `build-consent-evidence.mjs`, `probe-lead-sources.mjs`

**Gmail draft triage** (`projects/briefs/gmail-draft-triage/`) — `brief.md`,
`inbox-rules-got-moles.md`, `cron/jobs/gmail-draft-pass.md` (inactive). Two real drafts written
and threaded live (Sarah Jeffers, Steven @ Peak Landscape).

**Lead-alert repair** (`projects/briefs/lead-alerts/`) — `lead-alert.mjs` + `brief.md`,
`recipients.json`, `gmail-leads-filter.xml`

**Quick Fix overrun audit** (`projects/tool-jobber/`) — `scripts/quickfix-overrun-audit.mjs`
(read-only), `data/quickfix-overrun-{2026-08-11.json, report-2026-08-11.md, visits-2026-08-11.json}`,
`reports/2026-08-11_quick-fix-overrun-audit.pdf` (12 pages, also in `~/Downloads/`)

**Routing — rest of week routed** (`projects/briefs/technician-route-automation/`) —
`write-times-from-plan.mjs`, `week-visits-*.json`, plus edits to `assign-by-territory.mjs`,
`rebalance-week.mjs`, `jobber-to-optimo-sync.mjs`, `territories.json`

**Muhammad call grading — daily system** (`projects/briefs/callrail-faq/`) —
`GRADING-RUBRIC.md`, `scripts/fetch-day-calls.mjs`, `call-grading/{2026-08-10.md, 2026-08-11.md,
_tallies.json, _briefings/}`, `cron/jobs/muhammad-call-grading.md`

**Service-day lookup fix** — 98444 (Parkland) now returns Tuesday; the page now distinguishes
"missing from grid" from "outside service area".

### Findings

- **Autopay is at 31% on TMCP, not the ~50% assumed.** 809 active jobs, 212 on autopay. 463 TMCP
  jobs across 449 clients are billed by hand — and **277 of those already have a card on file**,
  roughly **$32k/month collected manually**. That is worth more than the entire $19.8k past-due
  balance. `Job.willClientBeAutomaticallyCharged` is readable; there is **no mutation to set it**,
  so every enrolment is a manual UI toggle.
- **Quo's termination was an automated fraud call, and the data explains it.** The email's own
  `utm_content = Suspected+Cold+Calling+Email` gives it away. Root cause: an 8-day-old WA VoIP
  number, one seat signing in from Pakistan, dialling out of a lead feed that had only **4 missed
  inbound calls** against **75 new Jobber leads** — 20 outside WA area codes, 23 with no inbound
  call at all. The website form has been taking bot submissions and nothing filtered them.
  Spencer's first reply to Quo ("the only people we have called are calls we missed") is not
  supported by the data; the appeal corrects it rather than repeating it.
- **Lead alerts had a 9-hour nightly blind spot.** Cron ran 07:00–19:00 on a fixed 3-hour lookback,
  so 19:00→04:00 was never scanned; `state.lastRun` was written every run but never read.
  Five leads dropped silently (Leslie Postovoit, Jay Hickenbottom, Mo Brown, Dick Keiser,
  Maryanne Zukowski). Also an "existing customer" rule that filtered legitimate leads, and
  `leadSource` ignored in favour of guessing from record shape. All three fixed and verified.
- **Quick Fix overruns are systemic, not incidental.** 117 open Quick Fix jobs; only **1** stopped
  cleanly at 5 visits. 11 are past the threshold with 31+ extra visits already delivered, and
  **60 visits are booked past week 5** across the portfolio. The cadence engine flags at visit 5
  for a human decision, but Jobber's recurring series keeps generating weekly visits regardless —
  there is no gate. #7964 Barbee Mill HOA is misclassified (commercial, 28-day cadence).
- **Only ~1% of Spencer's inbox is human mail.** A returning customer had waited 8 days and a
  commercial referral the same, buried under ~200 automated messages a week.
- **Cory's personal cell is going out on his On My Way texts** — "(253) 569-4822" instead of the
  main line. A number in a Jobber user profile becomes a selectable callback. UI-only fix.
- Route load for Wednesday after the re-run: Alias 9h31, Luke 9h15, Cory 8h35, Robert 7h11.
  Alias's is genuinely long given he runs slowest (21.3 min/stop), but Robert finishes 14:16.

### Decisions

- **Send mechanism for texts: browser robot inside Jobber**, not Twilio. Keeps the conversation on
  the number customers already have, replies land in the existing inbox, $0. Accepted cost:
  unofficial, breaks on UI changes, needs a machine left running.
- **Inbound autonomy at launch: draft everything, send nothing.** Promote intents to auto-send only
  on measured accuracy after 1–2 weeks.
- **Autopay is a consent campaign, not a settings sweep** (Spencer: "if autopay is not switched on
  then we need to get their approval"). A stored card authorizes the agreed charge, not an
  open-ended recurring one. Wording must stay cause-neutral — never assert a decline.
  T2 (237 current clients with a card) must never ride along on a past-due nudge.
- Route re-run covered **Wed/Thu/Fri only** — Monday's live route deliberately untouched.
- `route-drift-check` stays **off** for the manual editing week.
- Phase 0 of the route-scheduling rebuild approved: read-only audit measuring seven metrics before
  any build, framed as a go/no-go — the numbers either validate software or say the answer is a
  fifth tech.

### Open threads

- **Autopay consent campaign is the biggest unstarted item.** 277 warm clients (card on file,
  never asked) ≈ $32k/mo billed by hand. Enrolment is a manual per-job toggle — a few hours of
  clicking, one time. Progress trackable by re-running `autopay-audit.mjs`.
- Collections: 28 clients rolled over ($2,475), 8 commercial/HOA need phone outreach ($3,580),
  1 needs a direct ask ($89). Scripts are cron-ready for 7/11-day scheduling — recommended a week
  of human oversight before unattended.
- **Quo appeal not sent** — three PDFs are built and in Downloads, awaiting Spencer.
- **Gmail draft triage is blocked on credentials** — the `GMAIL_*` OAuth client vars are not set.
  ~10 minutes to set up. Cron job exists but is `active: false`.
- **Week of Aug 17–21 still has ZERO routes planned** — `optimize-plan.json` for 08-10..14 is
  unwritten, which blocks `extend-horizon`. Grows worse daily.
- **Tavis's Territory 2 handover is 2026-08-17** and his home address is unknown, blocking commute
  calc. Also: run `assign-by-territory` before that week deploys.
- **onX waypoint cleanup is gated on Spencer signing into onX** (2FA) on the persistent Chrome
  profile. 808 active properties identified against 6,183 archived jobs of likely clutter.
  Nothing destructive started; export-before-delete is step 2 of the plan.
- Cory's On My Way callback number — Gear → Manage Team → clear the profile phone, then check the
  other techs the same way.
- 14 zips have active customers but no assigned route day (98020, 98036, 98087, 98103, 98107,
  98122, 98208, 98258, 98292, 98323, 98370, 98408, 98584, 98596).
- Quick Fix visit-5 gate not built — offered, not actioned.
- Two staged-but-unclicked Chrome gates: the Gmail "Leads" filter backfill button, and Roy's
  `gmail-daily-triage` cron still failing daily on missing credentials (shipped file, left alone).
- Carried from Session 1: corrected Ninety value first publishes 08-17 (~672 vs 687); 6 TMCP tag
  mismatches; 4 suspected duplicate TMCP jobs.
