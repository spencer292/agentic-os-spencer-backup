# 2026-08-16

## Session 1

Ran the evening of 2026-08-15 into the early hours of 08-16.

### Project
technician-route-automation

### Goal
Push Spencer's hand-built Jobber board for the week of 2026-08-17 into OptimoRoute, optimise the sequence and times, review before anything is written, then write the times back to Jobber on approval.

### Deliverables
- `projects/briefs/technician-route-automation/tech-service-times.json` — all techs to 15/30 min, Cory 12/24 (10 first, raised after his measured 12.7 was shown), plus a new `clusters` block pricing Barbee Mill at 120 min across 11 jobs
- `projects/briefs/technician-route-automation/service-time.mjs` — cluster lookup: a job in a cluster is priced by the PLACE, splitting `totalMinutes` across its jobs, beating the per-tech number
- `projects/briefs/technician-route-automation/week-review.mjs` — NEW. Week shape per tech per day; reports PAID hours (clock-in at job 1 to clock-out at last job), commute, drive, miles, unscheduled
- `projects/briefs/technician-route-automation/replan-day.mjs` — NEW. Re-sequences one day against the Jobber board. No Jobber credentials at all, so it structurally cannot write to Jobber
- `projects/briefs/technician-route-automation/check-times.mjs` — NEW. Pre-write sanity gate: weekend stops, outside 07:00-18:00, out of sequence, impossible gaps, unscheduled, 3h+ movers
- `projects/briefs/technician-route-automation/overtime-relief.mjs` — NEW. Finds the cheapest stops to move off an over-long day, same tech only
- `projects/briefs/technician-route-automation/move-list.mjs` — NEW. Turns a proposed move into the exact jobs, sized by a nearest-neighbour re-solve scaled onto real road miles
- `projects/briefs/technician-route-automation/mileage-split.mjs` — NEW. Road miles split into unpaid commute vs paid between-job driving
- `projects/briefs/technician-route-automation/payroll-vs-fuel.mjs` — NEW. Measures the gap between OptimoRoute's loop objective and the payroll objective
- `projects/briefs/technician-route-automation/tech-home-infer.mjs` — added a hard warning that its core assumption is unsafe
- Live: 565 visits pushed, all five days re-planned, 560 times written to Jobber, 34 arrival windows applied, 4 duplicate visits deleted, 7 ghost OptimoRoute orders pruned

### Decisions
- Service time is a DIRECTED number, not a measurement — Spencer set 15 min for everyone and 12 for Cory, superseding the 2026-08-11 calibration. SETs stay at 2x the check.
- Barbee Mill (11 Renton jobs inside a 0.18-mile radius) is priced as one PLACE at 120 min, the top of Spencer's "90 minutes to 2 hours". Planning short is what makes techs run late against emailed windows.
- Tavis on the Snoqualmie Valley route is INTENTIONAL and contradicts `territories.json`, where it is Alias's T1. `assign-by-territory.mjs` would silently hand it back.
- Cory Ventura is SALARIED. This flipped the overtime plan: the ceiling on loading him is his day length, not his 40-hour week, so work was moved onto him that would otherwise have been left alone.
- Robert Norton takes no additional work — Spencer: he is by far the slowest.
- Overtime is measured on PAID hours (job 1 to last job), not OptimoRoute's route duration, which includes the unpaid commute at both ends.

### Open threads
- **Crons are still stopped** (`bash scripts/start-crons.sh` to restart). Turned off so manual editing would not be re-sequenced underneath Spencer. Next week's arrival windows were applied by hand, but anything booked over the weekend gets no window until the sweep runs.
- Alias Franks keeps ~3.4h of overtime with no routing fix available — Territory 1 is 85.9 paid hours across two people. Headcount signal, not a routing problem.
- Barbee Mill is unlikely to be the only cluster. The `clusters` mechanism is built and every order writer reads it; the client list still needs a sweep for co-located jobs.
- Open-route (payroll-optimal) routing: measured gap of up to 9.4 paid hours a week, but heavily inflated by straight-line geometry crossing Puget Sound on Luke's peninsula days. Realistic 3-6h. Test is to set ONE driver to an open route in the OptimoRoute UI and re-plan one day. Cannot be done via API — `get_drivers` returns AUTH_KEY_UNKNOWN.
- Tech home locations are unknown. `tech-home-infer.mjs` was shown to be wrong (Robert is Maple Valley, not Buckley), so every commute figure is really "distance from the configured start location". Spencer offering the five home cities would make the commute numbers real.
