# 2026-07-10

## Session 1

### Project
technician-route-automation (`projects/briefs/technician-route-automation/`)

### Goal
Review the n8n route automation state, set safety rules, and rebuild the Jobber ⇄ OptimoRoute
route sync as multiple small workflows (Spencer's v2 architecture).

### Deliverables
- n8n instance defused: child `gr8kf904tjC2ckcA` DEACTIVATED (was still ACTIVE with an open
  `route-sync-test` webhook that accepted dryRun/maxWrites overrides — contradicted memory);
  parent `XLhh2TB89NwSlBRX` `Days` defaults flipped from dryRun=false/maxWrites=500 to
  **dryRun=true/maxWrites=0** so a stray activation can't write.

- `projects/briefs/technician-route-automation/brief.md` — v2 architecture + rules section added.
- `projects/briefs/technician-route-automation/build-notes.md` — v2 build log (top section).
- **n8n WF-1 created INACTIVE:** `YxKaiU1IAAmMkDLh` "Route v2 WF-1 — Push Jobber → OptimoRoute"
  (dryRun=true/maxOrders=0 defaults, no webhook trigger). Updated in place ×2: email-freeze +
  week-window + pin rule (v2.1), then window-width sentinel pin rule (v2.2).
- **n8n WF-2 created INACTIVE:** `QEKz72NTP8YRZsUS` "Route v2 WF-2 — Optimize + write-back"
  (start_planning dateRange/balanceBy WT/clustering → poll → get_routes → delta guard →
  visitEditSchedule; dryRun/maxWrites=0 defaults; skipPlanning bypass).
- **WF-3 backfill script:** `projects/briefs/technician-route-automation/backfill-time-windows.mjs`
  (scan/report/live --max N; state in backfill-state.json). SCAN DONE: 64,429 of 64,823 upcoming
  visits are "anytime", spread ~600/mo through 2036. ~6-7h of writes, resumable, nearest-first.
  Placeholder convention: **07:00–19:00 PT** (12h wide = floats; ≤6h = committed). AWAITING
  Spencer's go for canary (live --max 20 + check tech notifications) then drain.

### Decisions
- Spencer approved both defusal actions ("yes, do both").
- **v2 split:** WF-1 push (Jobber→OR), WF-2 optimize+write-back (OR→Jobber), WF-3 evening
  time-fix (parked), WF-4 tech-visit watcher (parked).
- **Rules:** techs 07:00–18:00 hard ceiling (all 3, <10h target but busy season overruns OK);
  jobs slide to ANY day in window; sets (first visits) date-pinned priority C — unfit sets come
  back unscheduled + flagged for Spencer, never silently moved; every cross-day move reported in
  Notion log; 10 min/check, 20 min/set (set = "a little longer", adjustable).
- **Cadence/freeze v2.1 (supersedes above):** the real freeze line = Jobber's day-before ~14:00 PT
  customer email with arrival windows. Date D locks at 14:00 on D-1; today never writable. Runs
  ~04:30 + ~13:00. Week window: Mon–Thu → this Sunday; Fri–Sun → next Sunday (Friday = whole-next-
  week baseline). Pin rule: visit with real Jobber time = day-pinned; only anytime visits float.
  New clients fill into existing routes; slated people never move days.
- orderNo scheme = `<jobNumber>-<visitNumericId>` + operation SYNC (visit-unique → idempotent,
  collision-proof). Set detection = visit date == job.startAt date (verified live).

### Deliverables (evening — baseline execution)
- **Full baseline pushed + optimized in OptimoRoute (509 orders, week 07-13..17).** OR upgraded
  to Pro (Spencer) for dateRange planning — Lite is single-date only, ignores allowedDates.
  Driver params via API: Tavis disabled all week (broken leg), Spencer Tue/Thu/Fri, all 4
  drivers workTime 07:00–18:00 (`update_drivers_parameters`, items = `{driver:{serial},date,…}`).
- Script engines (n8n Jobber cred died 2x → script-side, sanctioned .env auth): `push-week.mjs`
  (WF-1 equiv), `optimize-week.mjs` (WF-2 equiv: plan/write, --no-replan, --fresh = startWith
  EMPTY re-sequence, email-cutoff recheck at write), `backfill-time-windows.mjs`,
  `history-zone-calendar.mjs`; scratchpad: incremental-push (additive diff), reassign-orders,
  rebalance-edits, ghost-diff.
- Spencer's board corrections applied (256 reassignments): Cory↔Luke all days; Spencer↔Cammeron
  Tue; Cam↔Luke Mon/Wed/Thu; Spencer↔Cory Thu; Fri Olympia cluster → Luke Monday; Ravensdale →
  Cammeron. Ghost/dupe orders deleted (23 + 3 + 4-analysis). ON_FORCE balancing (use all trucks).

### Decisions (evening)
- lockTechs=false for baseline (Jobber techs wrong/missing); flip to locked after write-back.
- Zone calendar = structural fix for area scatter (25 cities split across days; Kent/Puyallup on
  5 days each). Built from 12-mo completed-visit history; Spencer marks up → zone-map.json.
- OR facts learned: Pro gate error = ERR_UNSUPPORTED_BY_PRICING_PLAN; balancing ON benches
  trucks → use ON_FORCE; force-pinned overloads VIOLATE workTime; Jobber visit date-filter =
  interval-OVERLAP semantics (daily counts double-count spanning visits); n8n native pagination
  can't merge cursor into raw GraphQL body → explicit Fetch→Step→IF→Wait loop.

### Open threads (evening)
- **WRITE-BACK NOT RUN YET.** Late-night arc: zone v1 (strict 1-day) overloaded days → v2 (2-day
  areas) scattered areas → Spencer frustrated ("no rhyme or reason") → pivot to MIRROR-HISTORY
  board (each customer keeps own historical weekday, 93% have clear habits; sets/committed pinned).
  Loaded in OR now (days mirrored, techs flex). **Root diagnosis: Mon=111/Wed=106 stops vs only 3
  trucks those days (Tavis out, Spencer Tue/Thu/Fri) ≈ 95 capacity → Mon/Wed run to ~20:00.**
  Morning decision doc: `projects/briefs/technician-route-automation/2026-07-11_morning-review.md`
  — options: (A, recommended) Spencer drives Mon/Wed instead; (B) move whole small areas Mon/Wed→Tue;
  (C) accept overtime. Then re-sequence → Spencer eyeballs → write-back. Deadline Sun ~13:30 PT.
- Lesson logged: mirror the existing operation, don't re-solve it; optimizer re-invention loops
  don't converge against 8 years of tacit knowledge. Zone calendar shelved for incremental later.
- n8n Jobber credential died 2x (rotation confirmed OFF) — durable fix = dedicated second Jobber
  app for n8n (Spencer to create). n8n WF-1/WF-2 blocked on it; scripts carry the load.
- WF-3 backfill: 64,429 anytime visits scanned, state saved; canary + drain not run.
- Cammeron Friday nearly empty (7 stops → 09:46). Jobber double-book #7735 to clean.
- 12-mo history scan running → zone-map-draft.md.

- Mystery solved: ACTIVE `gFYppNw0cFZKuHpA` "My workflow" = live Jobber webhook subscription
  (QUOTE_UPDATE etc., HMAC-signed, app c87e538e…) into a bare webhook node — receives, does
  nothing. Left running; potential future event-driven trigger.
- Key structural finding: the deployed child NEVER creates OptimoRoute orders — it only reads
  `get_routes` + writes times back. The ingest step (Jobber → OR orders) isn't in the automation
  at all; the rebuild must add it using the proven-safe method (unique orderNo `<job>-<date>`,
  `operation:CREATE` via `create_order`, `assignedTo.serial=<full name>`).
- Next: agree rebuild rules with Spencer, then rebuild + re-prove (dry-run vs hand-routed day →
  single-visit live → scale) before any reactivation.
- Old inactive workflows `fGNPaCBprD82GG02` / `gAn73X31rEMQ2cpL` (Spencer's originals) + 6
  tutorial leftovers = clutter, untouched.

---

## Session 2

### Project
jobber-notes-automation (`projects/briefs/jobber-notes-automation/`)

### Goal
Resume the Jobber notes automation: get the stalled n8n report workflow working and the
report caught up after 4 days of nothing running (cron runtime stopped, n8n wf inactive).

### Deliverables
- **Root cause of the 07-07 n8n failure found:** Jobber's partial "RequestNote hidden due
  to permissions" errors killed the old build node → empty body → "Invalid query".
- **Coverage bug found (bigger):** the `jobs(first: N)` unfiltered read missed most of a
  day's visits — 07-09: 0 of 65 found; the "proven" 07-06 run synced only 31 of 85.
- `report-sync.mjs` REWRITTEN v2: visits-first read (page day's visits → dedup jobs →
  aliased 15-job batch fetches), direct-fetch auth (not the CLI, which dies on partial
  errors), throttle backoff.
- `engine.mjs` REWRITTEN v2: same read + auth; decision logic untouched; 14 tests pass.
- **Report backfilled LIVE:** 07-06 re-sync (85 jobs), 07-07 (77), 07-08 (68), 07-09 (65).
- **n8n wf `2dxtg73X1JUvLUTr` rebuilt v2, INACTIVE:** native Jobber credential (secrets
  node REMOVED), trigger fixed (was hourly, now daily 18:00 PT, wf tz pinned), visits-first
  read, all GraphQL bodies built in Code nodes (empty-body gotcha), retry + request spacing.
- Scheduling dry-run evidence: `runs/2026-07-09-dryrun.txt` — 65 jobs: 33 match techs'
  manual scheduling, 3 ADDs they missed, 0 pulls, 0 errors.
- brief.md Status updated; learnings logged (tool-jobber coverage trap + partial errors,
  tool-n8n rebuild patterns).

### Open threads
- ✅ DONE: n8n report wf `2dxtg73X1JUvLUTr` ACTIVATED (Spencer's go, ~13:09 PT 07-10) —
  first scheduled run tonight 18:00 PT covers today's notes. Verify tomorrow via
  "check executions 2dxtg73X1JUvLUTr" (expect success + jobEdit writes).
- ✅ DONE: `cron/jobs/jobber-visit-followups.md` trimmed to scheduling-dry-run only
  (moved to 18:15; report half removed — n8n owns it now; double-write guard noted).
- Security cleanup still pending: regenerate Jobber app client secret + re-mint refresh
  token (exposed in 07-06 transcript; no longer in any workflow). Update `.env` same step.
- Scheduling half: dry-run evidence is strong — next step is Spencer reviewing a few days
  of dry-run logs, then flip `--execute` (and/or port to a second n8n workflow).

### Session 2 additions (audit + scope)
- Spencer's new requirements: (1) misspelled/nonconforming notes must not fail silently →
  exceptions lane (designed, not built); (2) evaluate job-form-with-dropdowns input as an
  alternative (parked pending exception-rate evidence + whether job form submissions are
  API-readable — unverified); (3) the checklist DB also did week-ahead verification +
  double-visit detection → built `audit-schedule.mjs` (report-only).
- Jobber CLI auth died mid-session (401 chain-kill, stale rotating token) → Spencer
  re-authed via `jobber-api.mjs auth`. Direct-fetch scripts fine throughout.
- **First audit run (`runs/audit-2026-07-10.md`):** 12 missing follow-ups ("Add visit"
  notes never scheduled), 12 recurring jobs visited recently with NOTHING upcoming,
  **18 same-day double visits**, 23 close pairs ≤5d. Key learning: active jobs run
  legitimate WEEKLY cadences — 6-10d gaps are normal service, NOT dupes (351 suppressed);
  dupe thresholds = same-day definite, 1-5d probable. `jobType` filters one-off jobs out
  of cadence gaps; `jobberWebUri` gives clickable links in reports.
- engine.mjs now exposes jobType + jobberWebUri in plans.
- Per-tech audit for Cammeron Monday (`runs/tech-audit-2026-07-10.md`): 109 visits, week
  mostly clean; his flagged items resolved below.
- **Spencer domain corrections folded in:** (1) "Quick Fix" = 5-week program that ENDS —
  archived jobs are NOT cadence gaps (10 of 12 first-run stranded were closed Quick Fixes);
  (2) follow-up check must use DATE-filtered visits, not status:UPCOMING (Mike Coile's
  follow-up was in progress today and got false-flagged). Both fixed in engine/audit.
- **Final audit numbers:** 7 real missing follow-ups, Lance Steffey #7982 serviced-never-
  billed (requires_invoicing, no invoice), 2 open one-offs (Adam Patton #8114 Equipment
  Retrieval, Kyle Hardtke #8105 — both action_required), 18 same-day doubles, 23 close
  pairs. Kelly Wan + Audra Day Quick Fix invoices past due (collections).
- **FIRST LIVE SCHEDULING RUN (Spencer-approved, 2026-07-10):** engine --execute fixed all
  7 — 5 ADDs (Alex Alexeev 07-14, Jeff Taylor 07-15, John Luger/Paul Klansnic/Danielle
  Steele 07-16), 2 PULLs per same-month rule (Westly Wright 07-20→07-13, Chad Peterson
  07-28→07-15). 0 failures; verified 0 outstanding on re-run. Logs: runs/*-executed.txt.
- Kalhan Ajay #7910 noted "Convert to annual" 07-06 — flagged as TASK (engine doesn't
  create Jobber tasks yet); his Quick Fix is archived → Spencer to action the conversion.
- **Spencer's "74 missing follow-ups (wk 7/13-17)" report TRACED (all 74 checked live):**
  54 = follow-up EXISTS but parked on SUNDAY 07-12 (outside his Mon-Fri window — asked
  Spencer if techs work Sundays / how to redistribute); 7 = monthly-cadence correct
  (report over-flags); 2 = 2-week follow-ups correctly at 07-21+; 2 = archived Quick
  Fixes; 9 = real issues, now fixed or flagged (below).
- **"Ad visit" MISSPELLING (systematic, Cammeron):** parser only knew "Add visit";
  Cammeron writes "Ad visit" → his follow-up instructions were INVISIBLE since day one.
  Fixed regex to /\bad{1,2}\s+visit/i in parse-note.mjs + n8n Code B (deployed, active;
  careful — TWO bash-escaping mangles ate the backslashes; final fix via String.raw
  patch file + verified the DEPLOYED code). Recovered + executed 6 more 07-06 follow-ups
  (Peter Kupu, Justin Muir, Aaron Carriveau, HyperGreen, Theresa Hall, Lynn Clapp) and
  3 on 07-10 (Louis Chirillo, Tim Saiz, Mike Coile — Mike visited again today).
- **Late-note gap fixed:** engine visit scan now reaches 2 days back (Anne Nguyen: visited
  07-09, note written 07-10 → invisible to both days). Anne's 07-17 visit added.
- **Still needing humans:** 5 jobs visited this week with NO note written (Chris Doll
  #5369, Jan Stanfield #5438, Rachel Brouhard #5672, Jeff Hardman #5442, Randy Redding
  #5047 — automation blind, techs must say outcome); Charles #8095 (no notes ever,
  nothing upcoming, open); the 54 Sunday-07-12 visits (need Spencer's routing decision).
- **NOTES ORDERING BUG (the big one, Spencer caught it):** Jobber `job.notes` returns
  OLDEST-first; `notes(first:40)` on 40+-note jobs (oldest/most loyal clients) read
  2024 notes as "latest" → those jobs were INVISIBLE to report + engine since day one
  (Chris Doll #5369: 61 notes, real 07-06 note "Add visit" missed). Fix = `notes(last: 40)`
  in engine.mjs, report-sync.mjs, n8n Code A (deployed, verified, active). Week re-run:
  report counts jumped (07-06: 85→93, 07-07: 77→92, 07-08: 68→102, 07-09: 65→93, 07-10: 90);
  engine recovered 8 more follow-ups (Chris Doll pull→07-13, Jan Stanfield 07-13,
  HyperGreen#4926 07-21, Kathy Lewis 07-23, Jeff Hardman pull→07-16, Plemmons 07-17,
  Randy Redding 07-17, Rhonda Sweatman 07-24).
- **WEEKEND SWEEP (Spencer: Mon-Fri ONLY, no Sundays ever):** built weekend-sweep.mjs —
  81 visits sat on Sun 07-12 (+1 each next two Saturdays). Executed (Spencer approved
  after permission gate): 80 moved to following Monday, 3 deleted (weekday visit existed),
  0 failed; re-scan clean. Mon 07-13 now ~180 visits/5 techs. Weekly-audit candidate:
  add weekend-visit check.
- **DEDUPE EXECUTED (Spencer's rule: ONE visit per job per week, Mon-Sun):** built
  `dedupe-visits.mjs` (keeper = completed > earliest pending; same-day ties by
  clientConfirmed > timed window > has-tech > oldest). 29 planned → **28 deleted**;
  1 API-refused: Peter Kisbye #7999 07-21 dupe returns HTTP 422 on visitDelete every
  time (both visits look identical/deletable) → Spencer deletes manually in UI.
  Learning: batched visitDelete 422s the WHOLE batch on one bad id — delete per-visit.
  Root cause pattern: most dupes were Cammeron Anderson creating visits 07-07/07-09/
  07-10 (incl. TODAY) on top of already-scheduled ones — ongoing behavior, not historic.
  Reports: runs/dedupe-2026-07-10-*.md (note: same-day reruns overwrite these files).

---

## Session 3

### Goal
Accurate count of total TMCP (mole control program) jobs in Jobber — 577 clients tagged
but some hold multiple properties/jobs — and find clients missing the `TMCP - Active` tag.

### Deliverables
- `projects/tool-jobber/2026-07-10_tmcp-job-audit.md` — full audit: **597 active TMCP
  jobs** across 577 distinct clients / 597 distinct properties (10 multi-property clients
  hold the extra 20 jobs; Prologis Sumner Landing alone has 10). Plus 88 active Quick Fix
  jobs and 13 barter/warranty/friends-family recurring jobs (likely program-in-trade →
  610 total if counted).
- **8 clients missing the tag** (7 are recent signups #8030–#8147 → tag isn't being
  applied at signup lately; plus Mike Kaiser #5390). **8 stale tags** (6 with no active
  recurring work at all; Tyler Smythe = barter; Barbee Mill HOA = only a Quick Fix now).

- **Monthly value added to the audit:** TMCP cash = **$66,813/mo (~$801.8k/yr)** — 571
  monthly ($64,378) + 12 yearly ($566/mo) + 4 quarterly ($1,869/mo); median $100/mo.
  Barter/F&F/warranty 13 jobs = $0 recurring (~$1,300/mo in-kind at median → ~$68.1k all-in).
  **9 regular TMCP jobs are $0/never-invoice** (Mcdonald, Randall, R. Porter, Newby, Gasser,
  Hewitt, Heimbigner, K. Porter, Hunter) — possible ~$900+/mo leak, Spencer to review.

- **Tags APPLIED (Spencer-approved):** all 8 missing clients tagged via
  `clientEdit(tagsToAdd:["TMCP - Active"])` — additive, existing tags kept, read-back
  verified. Live tag count now 584 (Bob Miller's tag removed on Spencer's side mid-session).
- **Zero-invoice deep-dive:** Karen Porter #5007 = the real leak candidate ($100/mo line,
  running since 2024-02, NEVER invoiced ≈ $2,900 to date). Susan Mcdonald #7701 = fine
  (annual prepay, $900×1). Gasser/Hewitt invoiced manually. R. Porter, Heimbigner, Newby,
  Hunter, Randall = $0 items + 0 invoices ever (comps or long-running freebies).

### Open threads
- Signup workflow gap: recent TMCP signups aren't getting tagged (7 of 8 missing were #8030+).
- Mike Kaiser has BOTH `TMCP - Active` + `TMCP Churned` (job #5390 live) — remove Churned?
- Stale tags still on: Marcus Andy, Connie Schlimgen, Kim Suver, Bac Walker, Aziz El-solh,
  Tyler Smythe (barter, arguably fine), Barbee Mill HOA (Quick Fix only) — Spencer to confirm removals.
- Spencer to review zero-invoice comps, esp. Karen Porter #5007.

---

## Session 4

### Project
jobber-notes-automation (`projects/briefs/jobber-notes-automation/`)

### Goal
Bring back the Jobber notes automation — get the nightly loop actually running unattended.

### Deliverables
- Today's engine dry-run: `runs/2026-07-10-dryrun.txt` — 0 PULL / 0 ADD outstanding, 85
  already handled, 0 errors (pipeline healthy after Session 2's live fixes).
- **Local cron runtime FIXED (stopgap):** root cause = daemon spawned the extensionless npm
  `claude` shim with `shell:false` → ENOENT (npm shim is now GONE entirely — migrated to
  native install). Fix: `AGENTIC_OS_CLAUDE_BIN=C:\Users\spenc\.local\bin\claude.exe`
  (persisted via `setx` for future logons + set in the daemon's launch env). `.exe` takes
  the "direct" launch path in cron-runtime.js so the missing `run-hidden-command.ps1`
  wrapper is bypassed. Roy's proper fix still pending (report already filed 07-06).
- Cron daemon started, leader, pid 28820. `jobber-visit-followups` scheduled 18:15 daily.

### Decisions
- Stopgap env var over waiting for Roy — automation returns tonight instead of stalling.

### Open threads
- ✅ VERIFIED (wrap-up 07-12): cron `jobber-visit-followups` healthy — 4 runs, 0 failures.
- ❌ n8n report wf `2dxtg73X1JUvLUTr`: ALL scheduled runs failed (07-10/11/12) —
  "Authorization failed" at the first Jobber node = dead native Jobber OAuth2 credential
  (same failure route session hit twice). UI-only fix: Spencer reconnects the credential /
  creates the dedicated n8n Jobber app, before Mon 07-13 18:00 PT. Gap closed manually:
  report-sync 07-10 --write → 91 jobs (07-11/12 = weekend, 0 visits). Report current.
- Daemon only lives while this box is on — if rebooted, rerun `start-crons` (env var
  persists via setx, so a plain restart now works).
