# 2026-07-31

## Session 1

### Project
got-moles-scale

### Goal
Dig into Jobber reports for close rate, call→job conversion %, and Quick Fix vs Total Mole Control mix.

### Deliverables
- `projects/briefs/got-moles-scale/2026-07-31_close-rate-call-conversion-mix.md` — the report
- `projects/briefs/got-moles-scale/scripts/close-rate-analysis.mjs` — quote close rate + job source + call→job + product mix (read-only Jobber/CallRail pull, caches to `data/_close-rate-cache_*.json`)
- `projects/briefs/got-moles-scale/scripts/close-rate-callers.mjs` — segments callers existing/new/no-record against the full 4,336-client phone book
- `projects/briefs/got-moles-scale/scripts/close-rate-diag.mjs` — match-quality diagnostics (lag distribution, phone coverage)
- `projects/briefs/got-moles-scale/scripts/close-rate-mix.mjs` — unclassified-bucket resolver + revenue-weighted mix
- `projects/briefs/got-moles-scale/2026-07-31_close-rate-and-mix.txt`, `2026-07-31_caller-segmentation.txt` — raw run output

### Findings
- Close rate 68.5% of quotes issued / 76.4% of decided (12 mo, n=1,101). Confirms the 69% on the Ninety scorecard.
- QF closes 74.6%, TMC 62.3% — the weak number is on the product carrying 74% of annualized value.
- New-lead call→job 24.5% (27.8% on answered ≥30s calls). Trend May 18.8% → June 24.2% → July 37.4%.
- The July jump came from QUOTE-ISSUE rate (0.41 → 0.75 quotes per unique caller), not from closing harder. Close rate stayed flat 66-74%.
- Product mix 58.5% QF / 41.5% TMC by job count; 26% / 74% by annualized value.
- Call handling: 93.2% answered, 6.8% missed, 18.5% under 30s. Sources: GBP 52.9%, Google Ads 19.6%, organic 14.3%.

### Decisions
- Measured close rate on a CREATED-cohort basis (not week-of-conversion) — a quote can convert months after issue, so week-matching distorts. Lag check: 80% of bookings land within 3 days of first call, none past 60 days.
- Counted quote status `approved` as won alongside `converted`.

### Open threads
- Dead quotes aren't being archived (only 5-7 lost/month since May vs 28-37 Feb-Apr; 89 open in the last 3 months) — close-rate KPI will drift upward silently. Needs an archive cadence.
- CallRail `lead_status` is null on all 790 calls — qualification layer unused.
- Only 47% of jobs trace to a tracked call; ads still bypass CallRail, tracker 253-331-2772 swap pending.
- Report not pushed to Notion yet — offered to Spencer, awaiting word.

## Session 2

### Project
jobber-duplicate-cleanup

### Goal
Get notified when website-form leads arrive (they were reaching Jobber silently), then fix the duplicate client records polluting the client list.

### Deliverables
- `projects/briefs/jobber-duplicate-cleanup/brief.md` — L2 brief: evidence, root causes, constraints, acceptance criteria
- `projects/briefs/jobber-duplicate-cleanup/scripts/jobber-duplicate-audit.mjs` — full-account duplicate scan, clusters by normalized phone + email, classifies each record's origin
- `projects/briefs/jobber-duplicate-cleanup/data/duplicate-report.json` — 97 groups / 114 extra records across 4,311 clients
- `projects/ops-lead-alerts/2026-07-28_uncontacted-website-leads.md` — 20 real website leads since Jul 14 (1 struck out as bot spam)
- n8n workflow `LGD33gS2IupDhUi0` "Got Moles — New Lead Alert (Jobber → Email)" — 10-min poll, phone dedupe, emails Spencer. Built INACTIVE; Gmail credential attached programmatically.
- `site/src/lib/jobber.ts` — fixed `createJobberRequest` (wrong input shape), added `createJobberRequestNote`
- `site/src/app/(frontend)/api/contact/route.ts` — Request creation wired in after client create. STAGED for Roy, not deployed, NOT compile-checked (no node_modules, installs blocked)

### Findings
- The website form creates a Jobber **Client** only — a silent record. Jobber notifies on **Requests**. `createJobberRequest` already existed in the repo but was disconnected on a false premise ("Jobber removed request mutations Aug 2023" — `requestCreate` is live on 2025-04-16).
- **Duplicates are 82% CallRail, not the website form.** The native CallRail→Jobber integration (`JOBBER`, id 25967) writes a client at call START (caller ID) and another at call END (name captured in conversation). The gap between paired records equals the call duration. It fires per call, so callbacks add more.
- 97 duplicate groups / 114 extra records, accelerating: 44 groups' newest member in July, 77 since May, vs ~19 across all of 2023–Apr 2026.
- Jobber **cannot merge clients** — no API mutation, no UI feature. Cleanup = keep the record holding history, copy data across, archive the loser.
- Form spam is getting through: 4 submissions since May, one phone (8054002077), four fake names, all `@jmailservice.com`. The honeypot only catches hidden-field bots and the per-IP rate limit is an in-memory Map that resets on every serverless cold start.
- **n8n's Jobber credential is dead** — the daily Visit Notes workflow `2dxtg73X1JUvLUTr` has failed 7 nights running, silently. Cause: one Jobber app shared by the local CLI and n8n; re-authorizing either invalidates the other's refresh token (rotation itself is OFF — token static since 07-26).

### Decisions
- Alert scope = all new inbound leads (~7/day, not the 2-4 first estimated), one email per 10-min batch, website leads sorted first. Spencer chose email + Jobber mobile push.
- Poll Jobber on a rolling 3h look-back rather than a watermark, so a missed run or outage can't lose a lead; dedupe on client id + phone instead.
- Hold caller-ID stubs for 70 minutes before alerting so the repaired record wins the name.
- Give n8n its own dedicated Jobber app rather than reconnecting the shared one — reconnecting risks killing the local CLI and the hourly CallRail sync.

### Open threads
- **BLOCKED: n8n Jobber credential.** New Jobber app created; connect fails with "client id and secret do not match an existing application" even after regenerating the secret. Next step offered: Spencer adds `JOBBER_N8N_CLIENT_ID` / `JOBBER_N8N_CLIENT_SECRET` to the local secrets file himself and I test the pair against the token endpoint to isolate app-vs-n8n. Also unverified: whether n8n's Authentication field is on **Body** (it can revert to Header when saved with a blank Scope).
- Lead alert workflow still INACTIVE — cannot activate until Jobber auth works. Gmail credential attached but never exercised, so its health is unknown (dates from April).
- Visit Notes workflow still broken — same credential, same fix.
- Jobber app still lacks **Requests read+write** scope, so `requestCreate` is unverified and the site change must not go to Roy yet.
- CallRail integration fix undecided: inspect its UI settings for client matching, or turn off its client creation and extend `callrail-jobber-sync.mjs` to cover all calls.
- Backlog cleanup not started — needs `jobber-duplicate-resolve.mjs` (pull attached jobs/quotes/invoices per group, pick keeper, dry-run review sheet).
- 20-lead callback list not yet worked.

## Session 2

### Project
technician-route-automation

### Goal
Run the Thu/Fri route automation, then fix the zigzagging/fuel waste and split next week into four routes with Elias solo and Spencer out of the field. (Work ran 7/28 evening → 7/29; wrapped 7/31.)

### Deliverables
- `projects/briefs/technician-route-automation/fetch-window-visits.mjs` — live Jobber visit snapshot for a date range
- `projects/briefs/technician-route-automation/lock-techs-to-jobber.mjs` — times-only lock (Jobber's tech + own day, optimizer may only re-sequence)
- `projects/briefs/technician-route-automation/analyze-route-quality.mjs` — per-route road miles, feasibility, double-pass and spread diagnostics
- `projects/briefs/technician-route-automation/week-miles.mjs` — totals OR's own road miles for a date range (config comparison)
- `projects/briefs/technician-route-automation/design-grid-v4.mjs` — four-way territory designer (barrier-aware, compactness-scored)
- `projects/briefs/technician-route-automation/territory-grid-v4.json` — proposed four-way grid (NOT applied to the live grid)
- Modified: `push-week.mjs` (`--to=`, `--grid=<file>`), `optimize-week.mjs` (`--to=`), `lock-techs-to-grid.mjs` (`--visits=`, `--grid=`, per-grid `works`, `serialAliases`)

### Results
- Thu 7/30 + Fri 7/31 optimized and written: 195 orders pushed, 71 Jobber writes, 0 failures, both days reconciled exactly.
- Freeze overridden for Thu 7/30 on request: 154 time-only writes, 0 tech changes, 0 day moves. Cammeron 10.7h → 8.7h; nothing over 8.8h.

### Decisions
- **Jobber is authoritative over the territory grid when the office hand-assigns work.** 15 proposed tech moves (14 off Spencer) were discarded on Spencer's "everybody is on their correct technician".
- **Spencer out of the field permanently**; Elias (Jobber user "Alias Franks", spelling left as-is) becomes the 4th truck at full load; peninsula → Luke; standing grid to be updated rather than a one-week override.
- Optimizer tuning abandoned as a lever after measurement (~1% available); effort redirected to territory/day structure.

### Open threads
- **Next week in OptimoRoute is INCONSISTENT — a grid-v4 push died part-way through 434 orders and my retry was blocked.** The 7/31 20:11 drift run reports 492 visits vs 409 stops, 90 missing, 80 day-drift, 189 time-drift, 7 orphans — with 0 writes so far. Must be re-pushed and re-planned cleanly before Sun 8/2 14:00, when Monday's customer emails go out.
- **grid v4 is UNMEASURED** — no road-mile number yet against the 1,983 mi baseline.
- **Elias needs an OptimoRoute driver record named exactly "Alias Franks"** — web UI only, `create_driver` 404s. Until then his route only builds via the `serialAliases` shim on Spencer's serial.
- **Ride-along rule still needs `franks` stripped** from `/norton|franks/i` in `optimize-week.mjs` and `drift-check.mjs`, or the automation keeps gluing Elias to Cory's truck. Held deliberately so it lands with the switchover.
- Luke's grid-v4 Wednesday is 36 stops / 50 mi wide (peninsula + west Tacoma) — consider moving Federal Way off it. Cory has two loose days (Buckley+Sammamish 29 mi, Enumclaw+North Bend 22 mi).
- Unanswered from earlier: real on-site minutes per stop (10 is a guess), whether the driver app can be rolled out, and whether techs start from home or the shop (12.1h/wk commute).

## Session 3

### Goal
Pull every review from all 3 Got Moles Google Business Profiles — verbatim text, author, and a total count — for use on the website.

### Deliverables
- `scripts/gbp-scrape-reviews.mjs` — CDP scraper: opens each of the 3 GBP listings in the persistent Chrome profile, scrolls the reviews pane to exhaustion, expands every "More", extracts author/rating/date/text/owner-response
- `scripts/gbp-format-reviews.mjs` — turns the scrape into markdown + CSV + a 5-star testimonial pool
- `projects/str-gbp-optimization/reviews/2026-07-31_google-reviews.json` — full structured export (270 reviews)
- `projects/str-gbp-optimization/reviews/2026-07-31_google-reviews.md` — all 270 verbatim, grouped by profile
- `projects/str-gbp-optimization/reviews/2026-07-31_google-reviews.csv` — CMS/import format
- `projects/str-gbp-optimization/reviews/2026-07-31_testimonials-5star.json` — 231 five-star reviews with written text

### Results
- **270 total reviews** across the 3 profiles — Enumclaw 27 (5.0), SeaTac/Burien 167 (5.0), Tacoma/UP 76 (4.9). Average 4.96.
- 264 five-star, 4 four-star, 1 three-star, 1 one-star. 236 have written text; 34 are star-only.
- Scraped counts match Google's own displayed counts on all three (Tacoma renders 76 distinct review IDs vs a displayed 75).

### Decisions
- Scraped the public Maps review panes rather than the GBP API — no Places/SerpApi key on this install and the API caps at 5 reviews per place anyway.

### Open threads
- **Claims guardrail in GOT-MOLES.md is stale**: it says "219+ five-star Google reviews". Real number is 264 five-star / 270 total at 4.96 average. Worth updating the shipped guardrail (Roy's file) and any site copy using 219+.
- Tacoma's 4.9 is driven by a single untexted 1-star ("D D", a year ago) — the only non-positive rating in the whole set.

## Session 4

### Project
technician-route-automation

### Goal
Fix the OptimoRoute⇄Jobber automation issues left open on 7/29 — next week (Aug 3-7) was inconsistent and had to be rebuilt cleanly before Sun 8/2 14:00 PT, when Monday's arrival-window emails go out. (Session ran 7/31 evening → 8/1; paused for a computer restart.)

### Deliverables
- `prune-stale-orders.mjs` — NEW. Deletes OptimoRoute orders with no live Jobber visit (own `<job>-<visit>` orderNo pattern only; foreign orders reported, never touched; aborts if >50% of the window looks stale). 8 found in next week.
- `reassign-offroster-visits.mjs` — NEW. Sweeps future visits off techs who aren't driving onto the territory grid. `--only=<regex>` scopes it, `--max N` canaries, ride-alongs preserved, aliased batches of 20 with per-visit fallback.
- `push-week.mjs` — fixed `weekMonday()` (the root cause, see Decisions)
- `optimize-week.mjs` — ride-along rule, serial-alias reversal + abort guard
- `lock-techs-to-jobber.mjs` — roster from grid `works`; `--fallback=grid`, `--tech-only`
- `verify-week.mjs` — `--grid=` / `--week=` args, alias-aware, generalised off-roster reporting
- `set-driver-days.mjs` — `--grid=` / `--week=` derive availability from `works` + `notWorking`
- `territory-grid-v4.json` — 8 zips added, `serialAliases` removed, `notWorking: [Spencer Hill]`
- Snapshots: `week-visits-0803-live2.json`, `week-visits-0727-live2.json`, `tavis-scan/horizon/tail.json`, `offroster-reassign-plan.json`

### Findings
- **Root cause of the broken week: `weekMonday()` rolled a weekend start date backwards six days.** `--grid` mapped grid days onto the week just gone, they fell outside the window and were discarded — 0 of 434 orders on a grid day, everything floated. Reported as "grid-day frozen", not an error. After the fix: 433 of 434. It was NOT a push that died part-way.
- **221 of next week's 502 visits had no working tech** — Tavis 100 (worked zero visits this week), 61 unassigned, Spencer 53, Norton 7.
- **Tavis has 3,982 future visits to Dec 2027**, flat ~230/month, across 240 recurring jobs. Jobber has **no job-level assignment mutation** — `visitEditAssignedUsers` per visit is the only path, so new visits keep appearing until the office re-points the recurring jobs in the UI.
- **Serial alias was a write-back hazard**: OR reported "Spencer Hill" for all of Alias's stops, so `optimize-week write` would have assigned Spencer those visits in Jobber. Now reversed pre-write, with an abort if both techs are active.
- **Spencer was still enabled as an OR driver** (2/3/7/9/16 stops next week) because availability was hardcoded to the week of 07-27. OR gives stops to any enabled driver regardless of balancing mode.
- `get_drivers` returns `AUTH_KEY_UNKNOWN` on this key — the roster can't be read back; `update_drivers_parameters` (by serial) is the practical existence check.

### Decisions
- **Four trucks on grid v4, Alias solo** (Spencer). Spelling is **Alias**, not Elias.
- **Tavis is not working; all his jobs get redistributed** (Spencer) — Cammeron 1,469 / Alias 1,421 / Cory 1,075 / Luke 17.
- **Hybrid tech resolution**: Jobber stays authoritative where the office assigned someone actually driving (249 next week); the grid only fills gaps (177). Preserves the 7/29 rule without leaving a third of the board unplaceable.
- Day ownership stays with `push-week --grid` + the pin rule; the lock step writes tech only (`--tech-only`) so the two scripts don't fight over `allowedDates`.
- 8 new zips placed against adjacent gridded neighbours. Federal Way east (98003) → Luke/wed to keep the city whole with 98023, despite Luke's Wednesday already being the wide day.
- Left alone deliberately: 3,201 unassigned + 767 still on Spencer, out to 2027 — the weekly run now self-heals these; sweeping them 17 months out is churn.

### Open threads
- **DEADLINE Sun 8/2 14:00 PT.** Next week is still NOT rebuilt — every live command below was blocked by the permission classifier. Nothing has been written to OptimoRoute or Jobber this session.
- Approved-but-unrun sequence (order matters — driver days unschedule routes, so they precede planning):
  1. `node set-driver-days.mjs live --grid=territory-grid-v4.json --week=2026-08-03`
  2. `node prune-stale-orders.mjs live 2026-08-03 2026-08-07 --visits=week-visits-0803-live2.json`
  3. `node push-week.mjs live --grid=territory-grid-v4.json`
  4. `node lock-techs-to-jobber.mjs live --visits=week-visits-0803-live2.json --grid=territory-grid-v4.json --fallback=grid --tech-only`
  5. `node optimize-week.mjs plan --grid=territory-grid-v4.json` → `week-miles.mjs 2026-08-03 2026-08-07 grid-v4` → `verify-week.mjs --grid=territory-grid-v4.json --week=2026-08-03`
  6. Review with Spencer, then `optimize-week.mjs write` (the only step that reaches customers)
- Tavis sweep, separately: `node reassign-offroster-visits.mjs live 2026-08-01 2027-12-31 --grid=territory-grid-v4.json --only=tavis --max 5` (canary first — unverified whether a tech reassignment triggers any customer notification), then the full 3,982.
- **Office must re-point the 240 recurring jobs off Tavis in the Jobber UI** — no API path. Until then Jobber keeps generating his visits.
- grid v4 still UNMEASURED against the 1,983 mi baseline — that's step 5.
- `drift-check.mjs` still has `franks` in its ride-along regex (the other three files are fixed); its cron will also keep aborting at maxCreate 15 until the re-push lands.
- Snapshots on disk are from 7/31 — re-fetch before the live run if the board moved overnight.

## Session 5

### Project
technician-training-program

### Goal
Rebuild the technician training program from a reference document into a real zero-to-hero
onboarding program — start by auditing what exists and interviewing Spencer module by module
on the gaps (parking, walking a property, talking to people, dress, selling, how to work,
and the Jobber/quote system).

### Deliverables
- `projects/briefs/technician-training-program/brief.md` — Level 3 brief: goal, gap map, 11-module map, acceptance criteria, constraints
- `projects/briefs/technician-training-program/modules/03-park-and-approach.md` — COMPLETE bar war stories. Parking decision order, commercial/complex rules, truck locking, first-visit vs repeat-visit approach, nobody-home + locked-gate protocol, dog judgment call, field checklist, trainer sign-off
- `projects/briefs/technician-training-program/modules/04-property-walk.md` — SKELETON. Walk geometry captured, 8 interview questions outstanding. Explicitly marked not-ready-to-train-from

### Audit findings (what exists today)
- **Field Guide v1.1** (`brand_context/technician-field-guide-FULL-VERBATIM.md`) is a reference manual, not a program. §1 biology is excellent; §2 assessment is 7 bullets; §4 communication is principles only.
- Against Spencer's six asks the guide has: parking = 6 words; dress = one line; walking = 7 bullets; talking = framework only; selling = one paragraph; how to work = **absent entirely**; Jobber/quotes = 5 bullets with nothing on building a quote.
- **The phone/FAQ pack (`projects/briefs/callrail-faq/`) is the model to copy** — built from 203 real calls with role-play, scenarios, answer key and progress log. That is a program. The field guide is not.
- **Activity codes (M/A, L/A, N/A) appear nowhere in the field guide** despite driving follow-up cadence since 2026-07-26. The 2026-07-10 Jobber audit shows four competing note conventions in use, most visits blank. Highest-cost gap found.
- Also missing and unrequested: safety, photo standard, review asks, onboarding sequence, truck/trap inventory, route-system orientation, and the rule to hand commercial/5+ acre bids to Cory.

### Decisions
- **Do not patch the Field Guide.** Keep it as the reference manual; build a separate program on top — modules, ride-along cards, checklists, competency sign-off, role-play drills. Zero-to-hero needs a path with checkpoints, not a longer PDF.
- Audience = **new tech, zero experience** (existing crew re-certifies against the same standard).
- Capture method = **interview Spencer module by module.** Nothing invented; every standard traces to his stated practice.
- Level 3 project — 11 modules, sequenced so a day-1 path exists before the whole system is finished.
- Every module must end in something observable a trainer initials, not a lesson read.
- **Repeat visits get a FULL property evaluation, same sequence as a first visit** (Spencer) — not just a trap check. Changes the current Field Guide standard.
- Repeat visits: no knock, go straight to work. First visits only: walk up empty-handed, confirm the house number, ring/knock.
- Aggressive dog = leave and reschedule, no exceptions. Friendly dog = tech's judgment, proceed.

### Open threads
- **Module 4 interview incomplete — 8 questions outstanding**, listed in `modules/04-property-walk.md`. This is the pivot module: the activity code that drives follow-up cadence gets decided on this walk, so Module 8 is blocked behind it.
- Module 3 still wants war stories — real parking/approach incidents to train against.
- Modules 0, 1, 2, 5–10 not started. Spencer's stated priority order: park, walk, talk, dress, sell, work + Jobber.
- **Door hangers do not exist yet** — the "nobody home" text is currently the only notification. Worth ordering; tracked in Module 3.
- Trainer role undecided — who runs ride-alongs (Cory? Cammeron?). The program needs a named trainer that isn't Spencer.
- GOT-MOLES.md still says "219+ five-star reviews"; actual is 270 at 4.96 (Session 3). Training content should not inherit the stale number.
