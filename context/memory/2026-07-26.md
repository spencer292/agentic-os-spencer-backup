# 2026-07-26

> Four Claude sessions ran concurrently today. This file was created by session
> `9b5457a3` (Codex CLI setup) at wrap-up. The other three — `36bc731c` (route
> automation / Jobber dedup), `a306fec8` (Route Ready ads budget), `68dc649d`
> (model tiering) — were still in flight and have not written blocks here. Their
> turn-level capture is in `2026-07-26.aos.md`. **Number your block after the
> highest existing one, don't renumber mine.**

## Session 1 — Codex CLI review harness

### Goal
Install the OpenAI Codex CLI on this machine so Codex can act as an independent
second reviewer of Claude's work.

### Deliverables
- `~/.codex/AGENTS.md` — Codex's global instruction file (outside the repo, so
  `git pull` can never touch it). Mirrors the `CLAUDE.local.md` rules Codex cannot
  see: US English, no I-713 claims, claims guardrails, mole-disposal wording,
  chemical-free-first positioning, the full residential pricing table + Cory owning
  all bids, shipped-file / no-deploy / never-push-to-origin safety, and the
  Jobber/OptimoRoute sharp edges (SYNC unschedules, notes are oldest-first,
  CallRail key read-only).
- `.claude/commands/codex-check.md` — `/codex-check` slash command. Runs Codex
  against the working tree, then Claude verifies and triages each finding
  (confirmed / already handled / wrong / style) instead of relaying raw output.
  Review-only: no Edit/Write in `allowed-tools`.
- `CLAUDE.local.md` — new `## Local Commands` table registering `/codex-check`
  (gitignored, so not in the commit).

### Decisions
- **Codex reads `AGENTS.md` natively** — that is why no wrapper was needed. The
  gap was `CLAUDE.md`/`CLAUDE.local.md`, which Codex never reads.
- **Bridge lives at `~/.codex/AGENTS.md`, not in the repo.** A repo-root
  `AGENTS.md` is a shipped file and must not be edited on this install; the global
  file is user-owned and pull-proof. **Cost: the two must be synced by hand when a
  `CLAUDE.local.md` rule changes.**
- **`/codex-check` is review-only by design.** Keeps "what a second reviewer
  thinks" and "what we changed because of it" as two separate approved steps.
- **Auth via `codex login` (ChatGPT OAuth)**, not the API key — though
  `--api-key` against the existing `OPENAI_API_KEY` remains the fallback if the
  Amex limit makes another subscription awkward.

### Verification — smoke test found 3 things, only 1 of them Codex's win
- **Wrong (Codex):** flagged `model: fable` in `ssc-designer.md` /
  `ssc-template-builder.md` as an invalid alias. It is valid in this Claude Code
  version, both agents registered fine, and it is the deliberate 2026-07-26
  tiering choice from session `68dc649d`. Codex is on an older alias list.
- **Confirmed (Codex):** the command documented a fix path while granting no
  Edit/Write. Fixed — now explicitly review-only.
- **Missed by Codex, caught by Claude:** the invocation
  `codex review --uncommitted "<prompt>"` is rejected by the CLI — a scope flag and
  a prompt are mutually exclusive, despite the usage line implying otherwise, and
  stdin `-` collides the same way. Would have failed on every run. Fixed.

### Open threads
- **Sync debt:** any future `CLAUDE.local.md` rule change must be hand-copied to
  `~/.codex/AGENTS.md`. No automation for this yet — a hook or a wrap-up check
  would close it.
- Codex is sandboxed here: its shell calls were refused by its own policy
  (`blocked by policy` on PowerShell). It reviews by **reading**, not by running
  tests. Do not expect it to verify behaviour.
- `/codex-check` has not yet been used on a real feature diff — only on its own
  mixed working tree.

### Cross-session notes (not this session's work)
- **Cron runtime is STOPPED** (`leaderState: absent`) and has been since ~7/21.
  Missed the 7/24 content publish plus Monday digest/article and Tuesday ads
  manager. Session `a306fec8` is blocked on restarting it via
  `scripts\start-crons.ps1` — permission classifier refused. **This is the single
  highest-impact open item on the machine.**
- Session `a306fec8` also has a $75/month ads budget guard built but unexecuted,
  and a pending $2.47 → $8.00/day budget move on campaign 24059425574.
- Session `36bc731c` deleted 18 duplicate Jobber visits (480 → 462 for w/c 7/27)
  and has open decisions on interim visits (Belmont, Bender) and grid-day moves.

---

## Session 2 — Model tiering (`68dc649d`)

### Goal
Audit how models are actually routed across this install, then re-wire it: Fable 5 as orchestrator/designer/thinker, Opus 5 demoted to a worker tier, cheaper tiers on the grunt work — then lift the Jobber/OptimoRoute field-ops lane up to Opus.

### Deliverables
Config only, no project files:
- `.claude/agents/ssc-designer.md` — `model: sonnet` → `fable` (owns slide-plan + visual-inventory judgment)
- `.claude/agents/ssc-template-builder.md` — `model: sonnet` → `fable` (template cards, hero images, HTML)
- `.claude/agents/l2s-content-packager.md` — added `model: sonnet`; the field was **blank**, so it was inheriting the session model
- `cron/jobs/route-drift-check.md` — `sonnet` → `opus`
- `cron/jobs/jobber-visit-followups.md` — `haiku` → `opus`
- `cron/jobs/jobber-arrival-window-sweep.md` — `haiku` → `opus`
- `cron/jobs/callrail-jobber-sync.md` — `haiku` → `opus`
- `CLAUDE.local.md` — two new rules: model tiering, and the field-ops Opus floor (gitignored, not in the commit)
- Auto-memory `model-routing-policy.md` — rewritten; it was a version behind (named Opus 4.8, didn't know Fable 5 existed)

Cron fleet after: **11 sonnet / 6 opus / 3 haiku** — haiku is now only memory-backup, memory-index, skill-update-check.

### Decisions
- **Hierarchy (Spencer):** Fable 5 = orchestrator, designer, real thinker. Opus 5 is a *worker* tier — deliberately demoted, "hasn't proved itself." Sonnet 5 below, Haiku 4.5 for volume. Opus 4.8's future undecided.
- **Did NOT set `CLAUDE_CODE_SUBAGENT_MODEL`.** It sits above frontmatter in the resolution order, so a blanket floor would have dragged the two design agents straight back off Fable. Explicit per-agent pins instead, which preserves tiering.
- **Session default left at `opus[1m]`.** Spencer said "*if* I open it on Fable 5," implying per-session choice via `/model`; model switches also invalidate the prompt cache (caches are model-scoped).
- **Field-ops floor is Opus even where the work is mechanical.** I flagged that Haiku was genuinely correct for `jobber-arrival-window-sweep` (single-field `arrivalWindow=180`, ~4 runs/month); Spencer selected all four Jobber/OptimoRoute jobs anyway. Cost accepted deliberately — these make irreversible writes to real routes, client records, and customer-facing arrival texts.
- `ssc-image-generator` stays Sonnet — plumbing to Gemini/GPT-image, not aesthetic judgment.

### The load-bearing mechanism (worth not re-deriving)
Subagent model resolves: (1) `CLAUDE_CODE_SUBAGENT_MODEL` env var → (2) per-invocation `model` param → (3) definition frontmatter → (4) main session model. **Frontmatter defaults to `inherit`**, so a blank `model:` silently bills at whatever the session is on. Independently corroborated by session `9b5457a3`: Codex flagged `model: fable` as an invalid alias, was wrong, and both agents registered fine.

### Open threads
- **Cron changes are inert right now.** Verified live this session: `runtime: stopped`, `leaderState: absent`. All seven model changes above are correct on disk but nothing is firing until `scripts\start-crons.ps1` runs. Restarting the dispatcher is the prerequisite, and session `a306fec8` is blocked on it (permission classifier refused).
- **Session default** still `opus[1m]` in `~/.claude/settings.json` — one line to `"model": "fable"` whenever Spencer wants Fable everywhere by default.
- **Two stale hardcoded model pins** (flagged, not approved): `scripts/meetings/extract-meeting.cjs:39` defaults to `claude-sonnet-4-6`; `scripts/meetings/n8n-build-extractor.cjs:163` bakes `claude-opus-4-8` into every n8n workflow it generates. The meetings lane can't run here (no Notion token, no Zoom creds), so not urgent — but both take effect silently the moment it gets credentials.
- **`ztb-ads-budget-guard`** (new cron from session `a306fec8`) is on `haiku` but pauses/re-enables a real Google Ads campaign on a spend threshold. Decide whether the field-ops Opus floor extends to money-touching ads mutations.
- **Built-in `Explore`** could be shadowed by a project-level definition at `model: haiku` (docs recommend exactly this) to push exploration below its Opus cap. Not done — it would replace a well-tuned built-in system prompt.
- **Forks always inherit the parent model** — no override exists. Use a named subagent, not a fork, when the work should be cheap.
- **Watch spend on `callrail-jobber-sync`** — 12 runs/day, ~360/month, the fleet's highest-frequency job. Haiku → Opus is 5× per-token across all of it; that one job is most of this batch's added cost.

## Session 3 — Route Ready status check + funding + GSC (`a306fec8`)

### Project
zero-touch-business

### Goal
Answer "where's Route Ready at, has anyone been to the site, any luck yet" from live data — then act on what it showed.

### What the live pull actually found
- **Sales: 0.** Lifetime 0, $0. Both Gumroad products confirmed published (not a store-closed repeat). Day 7 of the 45-day first-dollar clock.
- **Ads: 0 impressions in 3 days ENABLED.** Full serving diagnostic: account ENABLED, billing APPROVED, campaign SERVING/LEARNING, 3 RSAs APPROVED, keywords ELIGIBLE. Nothing broken — **priced out**. $2.47/day at a $1.25 CPC ceiling can't clear ~$2-5 CPC template keywords. Search impression share <10% on 7/24, no auction rows at all 7/25-7/26.
- **Site traffic exists but is unattributable.** Cloudflare Worker requests 641-1,419/day; `/book` KV clicks 41 over 6 days. Worker requests count crawlers AND assets — calling that "visitors" was the trap.
- **Cron runtime was STOPPED** (found live, `leaderState: absent`) — nothing had fired since 7/23; content publisher missed Fri 7/24. Same blocker the concurrent model-tiering session was stuck behind. Spencer restarted it: daemon 31480, leader, heartbeat fresh.

### Deliverables
- `projects/briefs/zero-touch-business/scripts/rr-fund-campaign.mjs` — budget $2.47→$8.00/day, MaxClicks ceiling $1.25→$3.00. **Applied live** by Spencer; verified after: 8/3, ENABLED, LEARNING.
- `projects/briefs/zero-touch-business/scripts/rr-budget-guard.mjs` + `cron/jobs/ztb-ads-budget-guard.md` (daily 08:00) — enforces the $75/mo cap that Google does NOT enforce. Pauses at cap, warns at 80%, re-enables on the 1st. Ledger-backed, idempotent.
- `projects/briefs/zero-touch-business/scripts/rr-mint-gsc-token.mjs` — read-only GSC OAuth (webmasters.readonly, port 8766) reusing the Route Ready ads OAuth client. **Awaiting Spencer** — needs browser consent.
- `projects/briefs/zero-touch-business/scripts/rr-gsc-report.mjs` — organic clicks/impressions/CTR/position vs prior period, top queries, top pages, sitemap submitted-vs-indexed.
- `cron/jobs/ztb-weekly-digest.md` — new steps 3c (organic search) and 4b (paid vs organic split).
- `projects/briefs/zero-touch-business/scripts/ztb-readiness.mjs` — `ga_or_gsc` now requires the token, not just a URL string.
- `runs/ads-log.md` — funding decision + diagnostic recorded.

### Decisions
- **Fund the ads rather than pause them.** $2.47/day was generating neither spend nor learning. Sized to $8/day (not the $8-10 first suggested) because Spencer's $75/mo cap is policy-only — Google would happily bill ~$243/mo on a $8/day budget.
- **Guard built before the money moved.** Told Spencer to start crons FIRST, then fund, so enforcement was live before spend could begin.
- **Cloudflare request counts are never to be reported as traffic** — written into the digest job as an explicit prohibition, since that number is what made "has anyone been to the site" unanswerable.
- **GSC window ends 3 days back** — GSC finalizes on a 2-3 day lag; ending at yesterday would manufacture a decline every single run.
- **Skipped `.env.example`** for the new `ROUTE_READY_GSC_REFRESH_TOKEN` key. AGENTS.md says document new services there, but no `ROUTE_READY_*` key was ever added (precedent), and it's upstream-managed — editing it risks a `git pull` conflict on this split-remote install.

### Open threads
- **Spencer runs `node projects/briefs/zero-touch-business/scripts/rr-mint-gsc-token.mjs`** (browser consent, pick routereadykits@gmail.com). Needs the Search Console API enabled in the same GCP project as the ads OAuth client — the report script surfaces Google's enable link verbatim if not. Until then `ga_or_gsc: false` and organic is still a blind spot.
- **Property-format unknown:** `ROUTE_READY_GSC_SITE_URL` may be a URL-prefix or `sc-domain:` property. The report resolves against the live `/sites` list and warns on mismatch rather than reporting a false zero.
- **`ztb-ads-budget-guard` model** — concurrent session asked whether the field-ops Opus floor covers money-touching ads mutations. Left on haiku: the pause/resume decision is deterministic **in the script**, the model only runs it and reports. The better upgrade candidate is `ztb-ads-manager` (Tue, sonnet), which makes *judgment* mutations — adding negatives, pausing keywords.
- **First real read is Tue 7/28** (`ztb-ads-manager`) — first search-term data ever for this experiment. Don't judge the campaign off Monday's digest: fresh bid ceiling + LEARNING makes 2-3 days of CPC unrepresentative.

## Session 4

### Project
got-moles-scale

### Goal
Backfill the Ninety weekly scorecard all the way back to Aug 1 2025 — exact TMCP job counts plus every other metric — and add a weekly Missed Calls KPI.

### Deliverables
- `projects/briefs/got-moles-scale/2026-07-26_ninety-52week-backfill.md` — full results + caveats.
- `projects/briefs/got-moles-scale/scripts/ninety-weekly-push.mjs` — `--cache` for raw pulls (past-due always live), quote window now reaches 180d behind the range (was hardcoded 2026-02-01), per-metric `dataFloor` replaces the hardcoded CallRail date, `floored` tally in the run log, week-start name no longer hardcoded "Sunday".
- `projects/briefs/got-moles-scale/scripts/ninety-kpi-map.json` — missed_calls wired live (`6a663cd93bb8238f576331cf`), dataFloor on the 2 call + 4 Quick Fix metrics.
- **Live push: 597 scores, 52 weeks (2025-07-28..2026-07-20), 0 failures.** Run log `scorecard-runs/2026-07-26T17-13-42-622Z.json`.
- Missed Calls KPI created in the Ninety UI (weekly, ≤2 goal, orientation lte).

### Decisions
- **The 2026-07-23 backfill was silently wrong** — Sunday-keyed dates, and Ninety snaps a date back into its containing Monday week, so all 166 scores sat one week early. Confirmed Monday-start visually in the browser. The 52-week range covers every affected week, so it self-healed; only the point-in-time past-due score/note stranded at 2026-07-13 needed deleting.
- **Ninety's public API cannot create KPIs** (`POST /scorecard/kpis` → 404) and has **no GET for scores** — verification has to be the UI or reconciling the board's Total column. Created Missed Calls via `tool-browser`.
- **dataFloor instead of pushing 0s.** Quick Fix line items first appear in Jobber 2026-01-20; CallRail's first call is 2026-04-30. Pre-floor weeks are skipped, not zeroed.
- **Kept Spencer's manual pre-floor entries** rather than deleting them. The 4 Quick Fix rows' totals exceed the automated sum by exactly the Dec 29–Jan 18 hand-entered values (26/18/34/11) — that's real tracking from before the automation had a source, same as the manual Phone Calls numbers in Jan–Apr. Every unfloored metric reconciles to the board total exactly.
- Deleted nothing else; `rm` is denied on this install so the 3.7MB `data/_pull-cache_*.json` and `data/_kpis-live.json` scratch files remain untracked on disk.

### Open threads
- **No Monday cron yet** — the board is current through this week but nothing refreshes it. This is exactly how the scorecard died last time (28/41 weekly KPIs never scored; maintained ones stopped 2026-06-26). `ops-cron` registration is the next step.
- **"Total TMCP Jobs active" has almost no churn in it** — only 6 of 659 TMCP recurring jobs have an end date in the past, because Jobber keeps a future `endAt` after a cancellation. 645 is really "started and never formally closed". Blocks `TMCP Cancellations` too (computes ~0). Needs Jobber churn-tagging hygiene, not code.
- Spencer's audit UI pass still outstanding: archive TMCP MRR dupes ×2, "quick fix jobs created" dupe, "Leads of Phone", the 12 never-scored lead-source KPIs; set owners + goals (TMCP active goal still 524 vs actual 645).
- Missed Calls goal ≤2 vs actual 4.6/wk average — one click to loosen if Spencer wants ≤4.

### Session 4 addendum — churn tag + cron registered

- **Jobber tags live on Client, not Job** (`Job` has no tag field). Across 4,270 clients: `TMCP - Active` 613, `TMCP Churned` 20, `TMCP - BARTER` 6.
- **`Tag` type is `{id, label}` — NO timestamp.** Churn can never be backfilled or dated from the tag, no matter how well it gets applied. Fix: the cron creates the history the tag lacks — snapshots counts to `data/tmcp-tag-snapshots.json` each run, pushes the week-over-week delta. Delta is attributed to the week the PRIOR snapshot was taken in (on a Monday cron = the week that just closed); attributing to the current week would shift every number a week late.
- `tmcp_cancellations` now **enabled**, source `jobber-tag`, replacing the endAt version that computed 0 forever. Baseline recorded 2026-07-26 at 20 tagged; first real number lands on the Monday cron (first run deliberately pushes nothing — the standing total isn't one week's churn).
- **613 tagged-active vs 645 job-derived** — not directly comparable (jobs are per-property, clients per-account) but it's the honest measure of the overstatement. Each run now writes a note on the "Total TMCP Jobs active" KPI carrying both numbers so the gap shows on the board.
- **`cron/jobs/ninety-scorecard-push.md` registered** — Mon 05:30, sonnet, active. Verified via `cron-runtime.js` `listAllCronJobs()`. Daemon re-scans `cron/jobs/` every tick (cron-daemon.cjs:129 inside `tick()`), so no restart needed. Pushes last 3 completed weeks (late Jobber data self-heals), NOT the in-progress week.
- **Model choice: sonnet, not the opus field-ops floor** — Jobber is read-only in this job, computation is deterministic in the script, only writes are Ninety scores. Reasoning documented in the job file; moves to opus if it ever writes back to Jobber.
- Extra run 2026-07-26 17:5x: 61 scores (last 3 weeks + current refreshed). Current week is still filling — values moved vs the backfill (e.g. total_jobs_created 49→54).

## Session 3 (cont.) — Kits 2-3 built to sellable

Spencer cleared the credit limit ("week has restarted") and authorized finishing both kits.

### Deliverables
- **PW kit — 14 files** (`products/pw-kit/deliverables/`): wrote the 3 missing src docs (13-liability-waiver, 14-job-checklists, 15-startup-checklist), built 10 docx + 4 xlsx. Verifiers green.
- **Lawn kit — 14 files** (`products/lawn-kit/deliverables/`): wrote the 4 missing src docs (12-policies-and-letters, 13-property-waiver, 14-seasonal-checklists, 15-startup-checklist), built 10 docx + 4 xlsx. Verifiers green.
- `products/_xlsx_engine.py` + `products/_verify_xlsx.py` — shared OOXML writer/verifier so PW and Lawn don't each carry a third copy of the 240-line engine. **cleaning-kit left untouched** (it shipped and works — no refactor risk taken on a live product).
- `products/{pw,lawn}-kit/build-xlsx.py` + `xlsx-expect.json` — trade-specific workbook builders.
- `site/content/pages/kits-{pressure-washing,lawn-care}-business-starter-kit.md` + config tokens CTA_PW/CTA_LAWN/GUMROAD_*_KIT_URL. Site rebuilds clean with 3 kit pages.
- `products/{pw,lawn}-kit/listing-copy.md` — Gumroad listings, $49 each.
- `scripts/rr-gumroad-publish-kit.mjs` — generalized publisher (any kit; refuses --publish with zero deliverables).
- `scripts/rr-activate-kit.mjs` — activation guard.
- Gumroad products CREATED UNPUBLISHED: PW `hntns`, Lawn `hjesto`.

### Decisions
- **Dropped the Google Drive copy-link delivery** the cleaning kit uses. Ship .docx/.xlsx direct from Gumroad instead — they import into Google Docs/Sheets anyway, and Drive upload needs a signed-in CDP browser session, i.e. a Spencer-blocking manual step on every future kit. Page + listing copy describe delivery accurately; no copy-link promise made.
- **Trade-specific pricing models, not a reskin.** PW prices on surface area x production rate with setup+drive counted; Lawn prices on turf area x obstacle factor with a route-density tab (drive time as % of day). Biweekly lawn clients price HIGHER per visit — the thing new operators get backwards.
- **Waivers are the anchor doc in both kits.** PW: cleaning reveals pre-existing damage (chalking, failed paint). Lawn: irrigation heads + invisible-fence wire. Both are the #1 dispute in their trade.
- **PW/Lawn ad groups stay PAUSED** until products are purchasable — advertising an unpublished product is the 2026-07-20 store-closed incident in reverse. `rr-activate-kit.mjs` enforces it: won't write a live URL into the site unless Gumroad confirms published AND files attached.
- Dated new files 2026-07-26 rather than backdating to match the kit's existing 2026-07-23 files.

### Open threads
- **Spencer's only manual step: attach 14 files + cover art per kit in the Gumroad dashboard** (the Gumroad API cannot upload files). Then `rr-gumroad-publish-kit.mjs {pw,lawn}-kit --publish` → `rr-activate-kit.mjs {kit} --write` → `site/build.mjs` → `cf-deploy.mjs` → enable the two paused ad groups.
- Covers already exist at `products/{pw,lawn}-kit/art/cover.png` (generated 7/23).
- Kit-page copy claims "instant delivery" — true only once files are attached.
- The site already has 4 PW/lawn guides live with no kit to sell; once these publish, those pages convert instead of pointing at the generic freebie.

## Session 3 (cont.) — Kits 2-3 published live via browser

Spencer asked me to drive the Gumroad dashboard directly. Both kits are now LIVE and purchasable.

### Result
- **PW kit LIVE** — `routeready.gumroad.com/l/hntns`, $49, 14 files attached, verified purchasable.
- **Lawn kit LIVE** — `routeready.gumroad.com/l/hjesto`, $49, 14 files attached, verified purchasable.
- Site config swapped PENDING -> live URLs, rebuilt, **deployed** (routereadykits.com + www). Buy buttons confirmed live on both pages.
- **All 3 ad groups ENABLED/ELIGIBLE** (Cleaning, Pressure Washing, Lawn) — `scripts/rr-enable-adgroups.mjs`. The $8/day now covers three products.

### Gumroad gotchas found the hard way (do not re-learn these)
- **The API cannot publish.** `PUT /products/{id}` with `published=true` returns success and the product stays unpublished. Only the dashboard's **"Publish and continue"** button (on the *Content* tab — it does not exist on the Product tab) actually publishes. `rr-activate-kit.mjs` must always VERIFY, never trust the publish call.
- **API v2 `file_info` is useless** — reports zero attached files while 14 are visibly attached. It cannot gate activation; downgraded to advisory.
- **Descriptions must be HTML, not markdown.** Raw markdown rendered literal `**` and collapsed the bullets into a run-on paragraph on the live sales page. `rr-gumroad-publish-kit.mjs` now converts md->HTML; both listings re-pushed and verified clean.
- **Multi-file upload creates an "Untitled" FOLDER**, collapsed by default. Its filenames are NOT in `innerText` while collapsed — I misread that as "the save failed" and re-uploaded twice, creating 3 duplicate folders (42 files). Had to delete two via each folder row's Actions -> Delete, then Save. **Verify uploads with a screenshot, not a text regex.**
- Upload flow that works: set files -> poll until zero `aria-label=Cancel` buttons AND Save enabled -> click Save -> reload -> confirm exactly one folder.
- `element.click()` does nothing on Gumroad's React buttons; use `browser/cdp.mjs click` (trusted event). And never bulk-click every `aria-expanded=false` element — that opened five menus at once including a Delete.

### New tooling
- `scripts/rr-cdp-upload.mjs` — project-local CDP `DOM.setFileInputFiles` helper (JS cannot set file inputs). Kept out of the shipped `browser/` driver.
- `scripts/rr-enable-adgroups.mjs` — gated ad-group enabler.

### Open threads
- **Cover art DONE on both kits** (2400x1792, live on both public pages). My earlier "not scriptable, needs a native dialog" claim was wrong — Spencer pushed back and he was right. The Cover section's `input[type=file]` simply doesn't exist until its "Upload images or videos" button gets a TRUSTED click (`browser/cdp.mjs click`); after that, plain `DOM.setFileInputFiles` works exactly like the deliverables did. Lesson: check whether the input appears after the click before declaring an uploader unscriptable.
- **Both content folders still read "Untitled"** — the ONE thing automation couldn't crack. Tried: the row's Edit pencil (focus stays on BODY), clicking the H4 label directly (no focus), and selecting the text node + `Input.insertText` (ProseMirror ignores it — the label is a NodeView, not editable text). Rename by hand in the UI; buyers otherwise see 14 clearly-named files under it with Download all.
- First real test of the whole funnel is now live: 3 products, 3 ad groups, $8/day. Tue 7/28 `ztb-ads-manager` is the first search-term read.
