# 2026-08-12

> **Multi-session day.** Four session IDs appear in `2026-08-12.aos.md`
> (`24e02a1c`, `f96431eb`, `01e5456b`, `fc629178`) and this file did not exist until the
> route-sync session wrapped at ~14:30 PT. Earlier sessions produced real deliverables
> (callrail-faq analysis, route-engine five-territory cut, service-time calibration) that are
> on disk but were never logged here and remain uncommitted.
> **Later sessions: number your block after the highest existing one — do not renumber.**

## Session 1

### Project
technician-route-automation

### Goal
Get the Jobber visits for Wed 2026-08-13 that were missing from OptimoRoute into the route plan, and push the optimized times back to Jobber.

### Deliverables
- `projects/briefs/technician-route-automation/week-visits-0813.json` — live Jobber snapshot, 134 visits on 08-13
- `projects/briefs/technician-route-automation/drift-runs/push-new-2026-08-13-2026-08-12T21-19-42-021Z.json` — run report: 2 orders created, 26 Jobber time writes, 0 failures

### What happened
- 134 Jobber visits on 08-13; 132 already had OptimoRoute orders. `prune-stale-orders` dry: **0 ghosts** — nothing to prune.
- 2 visits had no OR order, both Cory Ventura's, both SET: `8360` Liz Silke (98040 Mercer Island, 20 min) and `8366` Denise Froatz (98027 Issaquah, 10 min).
- Ran `push-new-visits-replan.mjs live 2026-08-13 --override-freeze`. Created both orders, locked all 132 existing stops to day+tech, re-planned with `balancing:OFF` / `startWith:CURRENT`, verify passed (0 lost, 0 tech-moved, 0 unplaced).
- **26 Jobber time writes, 0 failed.** Independently re-fetched Jobber and spot-checked 5 — all match the plan.
- Churn stayed entirely on Cory: 24 stops shifted ~+30 min, 1 moved -14 min. Alias (36 stops 07:24→16:04), Robert (33, 07:28→15:55) and Luke (26, 07:40→15:45) byte-identical before and after.

### Decisions
- **Overrode the arrival-window email freeze** (Spencer approved). Freeze had been active 14 min — it flips at 14:00 PT for next-day and is a pure clock rule, not a check of what Jobber actually emailed. Trade accepted: up to 37 of Cory's customers could see a window shift; 24 actually did.
- **Commit scoped to this session only** (Spencer chose). The earlier sessions' backlog was left for whoever owns it.
- **Left `Voip Passwords.txt` and `.gitignore` untouched** (Spencer chose). Already logged under MEMORY.md Pending Decisions.

### Open threads
- **`8366` Denise Froatz sits at 16:45 as Cory's stop 39** — the optimizer appended her rather than fitting her, stretching his day from 15:56 to 16:45. Decide: leave it, or move her to Cory's next Issaquah day. Nothing has been done either way.
- **24 of Cory's customers now have windows ~30 min later than the email they already received.** Unconfirmed whether Jobber re-notifies on `visitEditSchedule` — worth checking, or 24 people expect him half an hour early.
- Temp probe files left in `projects/briefs/technician-route-automation/`: `.probe813.mjs`, `.push813.log`, `.verify813.json`. Deletion was denied by the permission prompt; not committed.
- Crons remain **stopped** (they were already stopped on entry). `route-drift-check` stays retired.

---

> **Note on this file:** three sessions wrote it tonight and it was overwritten twice. The blocks
> below were restored from commit `4185107` after a concurrent rewrite replaced them. Session
> numbering here is chronological by wrap-up, not by start time. Later sessions: append, never
> rewrite.

## Session 2

_Session ID `5a76ca5e`. Status/advisory session — no files produced, verification done live._

### Project
zero-touch-business (Route Ready)

### Goal
Answer where the Route Ready kits stand.

### Deliverables
- None — read-only status review. Findings verified live against the Gumroad API and the public
  product pages, not read off the last digest.

### Decisions
- Reported the kit status as **built and live, but commercially unproven**: all 4 products published
  (3 x $49 kits + free cheat-sheet lead magnet), files attached, site live, 21 articles shipped —
  and **0 sales lifetime** at day ~24 of the brief's 45-day first-organic-dollar gate.
- Did NOT report the lawn/PW kits as broken. First read of the Gumroad API (`file_info: {}`) looked
  like "no files attached, buyers get nothing"; that contradicted the 2026-07-26 memory, and a
  second check against the public product pages proved the files are there. `file_info` is
  single-file metadata, not a file list. Logged to learnings.
- Framed the two dead OAuth tokens as the blocking issue rather than a reporting nuisance: with Ads
  and GSC both expired 14 days, "0 sales" cannot be split into no-traffic vs no-conversion, so the
  next diagnostic step is unavailable until Spencer re-mints them.
- Recommended splitting the content engine to give lawn and pressure washing their own article
  clusters before publishing a 22nd cleaning post — 21/21 articles are cleaning topics, so two of
  three products have no organic funnel at all.

### Open threads
- **Needs Spencer (interactive, ~5 min each, cron cannot do these):**
  `node projects/briefs/zero-touch-business/scripts/rr-mint-refresh-token.mjs` (Google Ads —
  `invalid_grant` since 2026-07-30, 11 blocked weekly runs + 11 blocked daily budget-guard runs;
  last known spend 2026-07-29 $25.67 of the $75/mo cap, now 14 days stale, cap breach unknown) and
  `node projects/briefs/zero-touch-business/scripts/rr-mint-gsc-token.mjs` (Search Console — blocks
  all organic clicks/impressions/indexing reporting, expired several weeks).
- Content-engine split for lawn + pressure washing — proposed, not scoped or scheduled.
- First-organic-dollar gate expires ~2026-09-02 with 0 sales on the board.
- `Voip Passwords.txt` at the repo root — flagged by this session and seconded by the story-mining
  session; **resolved** by the phone-systems session, which added `*passwords*.txt` to `.gitignore`
  (verified: `git check-ignore` now matches it).
- **`context/MEMORY.md` is over cap at 2,817 / 2,500 chars (113%)** after a concurrent session
  consolidated it. Deliberately not trimmed tonight — three sessions were writing it and a fourth
  rewrite risked clobbering facts that had just been added. Needs a ~320-char trim next quiet
  wrap-up; the longest lines (Routes, CRONS OFF, Muhammad-vs-Spencer conversion) can lose filler
  without losing facts.

---

## Session 3

_Session ID `bc358390`. Story-mining session on Spencer's own recorded sales calls._

### Project
callrail-faq

### Goal
Find the stories Spencer tells on his own sales calls so Muhammad can use them on his.

### Deliverables
- `projects/briefs/callrail-faq/2026-08-12_spencers-story-bank.md` — 16 recurring stories mined
  verbatim from all 47 of Spencer's inbound sales calls (CallRail 2026-04-30 → 07-20), ranked by
  how much selling work each one does, each with its trigger, where it belongs in the call, and a
  third-person rewrite for Muhammad
- `projects/briefs/callrail-faq/2026-08-12_spencers-story-bank.pdf` — 6-page render for the
  portable pack (also copied to `~/Downloads/`)

### Decisions
- **Muhammad tells these in the third person, not the first.** "Spencer — the owner —" is a
  stronger delivery than a new hire narrating experience he doesn't have, and it keeps every claim
  true. Four of the sixteen (the wagyu-steak analogy, the Harry Potter light analogy, the $40k
  crawl space, "nobody calls once") are impersonal enough for him to own outright.
- **Pinned one version of the drifting numbers.** Spencer says 15 / 15+ / nearly 20 / 20 years
  across calls, three vs four acres at his own place, and 700 / several hundred / nearly a thousand
  TMCP customers. The doc locks: "close to 20 years" (his personal experience, never attached to
  the company — founded 2017), four acres, "several hundred yards on the year-round program",
  "219+ five-star Google reviews", and disposal as double-bagged into the customer's own can.
- Ranked by measured frequency rather than feel: the Enumclaw "you picked the wrong house" story
  appears in 10 of 47 calls and carries the whole TMCP pitch on its own; the $40k crawl-space story
  appears once and is materially under-used.
- Found and noted the strongest unrecognised move in Spencer's calls — he talks callers *out of*
  the more expensive plan on the record, in three separate calls. Highest-trust moment on the board
  and it costs nothing, since Quick Fix converts to TMCP later. Flagged for deliberate teaching.

### Open threads
- Story bank is not yet in `muhammad-portable/` — it's the third doc he'd need uploaded to the
  claude.ai project, and it pairs directly with the close-verbatim doc from earlier today.
- `.tmp_stories.txt` / `.tmp_stories2.txt` left in the repo root — `rm` is denied by permissions on
  this install.
- Security flag from session 1 now **closed**: `.gitignore` gained `Voip Passwords.txt`,
  `*[Pp]asswords*.txt` and `.tmp_*` at wrap-up, so the credential file can no longer be swept in
  by a `git add -A`. The file itself is still sitting in the repo root — ignoring it stops the
  leak, but moving it to a password manager is still the right end state.
