# 2026-07-13

## Session 1

### Project
technician-route-automation

### Goal
Status check on the Jobber ⇄ OptimoRoute route automation + brief correction.

### Deliverables
- `projects/briefs/technician-route-automation/brief.md` — fixed WF-1 sets rule: date-fixing now via `allowedDates` (was `priority: C`, which contradicted the 07-12 all-priority-M policy); added explicit always-M priority rule with rationale; documented the live drift-check cron.
- `projects/briefs/technician-route-automation/drift-check.mjs` — deterministic drift check + auto-fix (check/fix modes, all guards hard-coded: email freeze, planned-days-only, own-orders-only, max 15 creates, post-replan verify aborts on day/tech change, priority M, never deletes). Existing-order drift is report-only.
- `cron/jobs/route-drift-check.md` — cron every 2h 09:00–17:00 PT daily, model sonnet, runs the script in fix mode. Daemon confirmed leader (pid 28820); first tick today 09:00 PT (job created 07:50 PT).
- First supervised fix run: caught + fixed 2 real new bookings missing from OR — #6116 (Wed 07-16, Spencer, slotted 10:45) and #4979 (Thu 07-17, Luke, slotted 11:05). 2 created, 44 Jobber time write-backs, 0 failures, 0 aborts.

### Decisions
- Spencer: drift check runs automatically every 2h between 9am–5pm PT, EVERY day, with AUTO-FIX authorized (create missing OR orders + re-plan that day + write times to Jobber unattended). Existing-order drift stays report-only.
- Spencer: drop the "regenerate leaked Jobber secret" task permanently — not worried about it, do not resurface it (older logs still mention it; it is closed).

### Deliverables (continued)
- n8n Jobber credential `NitrghZSAjZamvqp` ("Jobber OAuth2 (native)") reconnected via browser + Spencer's OAuth approval — report wf `2dxtg73X1JUvLUTr` had failed its last 3 nightly runs (01:00 UTC = 18:00 PT) on the dead cred. Verify tonight's 18:00 PT execution succeeds (18:15 cron will also see it).

### Open threads
- WF-1/WF-2 rebuild (drift-check logic folds into WF-2).
- Week 07-20 not yet pushed to OR (needed by ~Friday per v2 cadence).
- Arrival-window sweep ~5k jobs (canary #7778) — parked behind rebuild.
- Watch the first few unattended drift-check runs (job created 07:50 PT, so today's 09:00/11:00/13:00/15:00/17:00 PT ticks all fire while the daemon is up).
