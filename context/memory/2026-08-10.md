# 2026-08-10

## Session 1

### Goal
Cron run — Ninety weekly scorecard push (last 3 completed weeks, self-healing re-push).

### Outcome
Clean run. Weeks 2026-07-20..2026-08-03, pushed=47, failed=0, skipped=none. TMCP tags:
647 "TMCP - Active", 18 "TMCP Churned", delta=0 vs the 2026-08-03 snapshot (no churn this cycle).
No all-zero weeks, no failed POSTs. Full detail in
`projects/briefs/got-moles-scale/scorecard-runs/2026-08-10*.json`; one-line entry appended to
`runs.log`.

### Decisions
- None — routine self-healing push, no anomalies requiring a call.

### Open threads
- None.

## Session 2

### Project
technician-route-automation

### Goal
Cron run — extend the OptimoRoute planned horizon ahead of the Jobber booking window.

### Outcome
**ABORTED at the dry run — nothing was pushed, planned or written.** `extend-horizon.mjs dry`
correctly identified the window to add (2026-08-17 .. 2026-08-28, 10 weekdays, all 0 stops;
planned through 2026-08-14 at 109-128 stops/day) but refused to proceed because
`optimize-plan.json` still holds an un-written plan for **2026-08-10..2026-08-14 — 2 Jobber
writes, 2 tech changes, generated 2026-08-07T00:22:00.812Z**. Planning the new window would
overwrite it. Per the job's rule 3b, `--discard-plan` was NOT used and `optimize-week write`
was NOT run — a live Jobber write across the current week is Spencer's call.

### Decisions
- None taken autonomously. Blocked pending Spencer.

### Open threads
- Spencer must resolve `optimize-plan.json` for 2026-08-10..2026-08-14: either
  `node optimize-week.mjs write` (if the 2 writes still need applying) or archive it
  (`mv optimize-plan.json optimize-plan.2026-08-10_2026-08-14.done.json`) if already applied.
  This matches the "optimize-plan.json 08-10..14 unwritten" note already in MEMORY.md.
- Until it clears, the horizon stays at 2026-08-14 and the week of 08-17 has **no routes at all**
  — new Jobber bookings landing there get no OptimoRoute stop, and `route-drift-check` skips
  unplanned days, so nothing self-heals. Every day this stays blocked is a day of uncovered work.

## Session 3

### Project
callrail-faq / technician-route-automation

### Goal
Cron run — rebuild the two office phone-lookup files (address day lookup + service day sheet).

### Outcome
Both builders ran clean against `territory-grid-v5.json` (pinned). All 5 local outputs written
2026-08-10 06:48 with a `2026-08-10` build stamp; builder took 2m36s (not the 106s false-success
signature). Counts: 3,982 addresses (803 with a scheduled visit), 126 zips, routes planned through
2026-08-14, 75 addresses in an uncovered zip. **Zip route days: no change since 08-09** (daymap
byte-identical bar the date) — no correction calls needed. Unrouted 2 (down from 5), off-day 384
(down 20 from 404), tech-clash 0 (down from 30 — assign-by-territory landed), date-clash 0 (was 2),
duplicates 82 addresses / 91 extra Jobber records (unchanged).

Three things did NOT go clean:
1. **Artifact tool unavailable in this environment — steps 1b and 2b could not run**, same as
   2026-08-09. Local files are current; the two hosted links the office actually opens are now
   ≥3 days stale (last successful publish 08-07 or earlier).
2. **`--overlay-week=next` returned "0 of 0 serviced zips differ" — that is empty, not clean.**
   OptimoRoute has no routes at all for the week of 08-17 because the horizon ends 08-14 (same
   root cause as Session 2's aborted `extend-horizon`). The office page therefore carries no
   next-week flags.
3. **v5 grid roster is out of date**: it names Cory/Cammeron/Alias/Luke, while live assignments
   are Alias/Cory(→Tavis 08-17)/Robert/Luke. Robert Norton appears as assignee on an unrouted
   visit but does not exist in v5.

### Decisions
- Did not repoint `--grid` off v5. The flag is pinned deliberately and swapping it is the exact
  failure this job exists to prevent; promotion of v8 is Spencer's call and must update both
  `--grid` flags in the same change.

### Open threads
- Unrouted, needs Spencer today: #8336 Dale Hoff (3402 N 35th St, Tacoma 98407, Wed 08-12,
  Luke — on its grid day, so a post-plan booking) and #4962 Maureen Haley (35215 253rd Ave SE,
  Auburn 98092, Tue 08-11, Robert — off-day too, grid says mon).
- Hosted artifacts cannot be refreshed from a cron session without the Artifact tool. Either
  publish them from an interactive session or move the office onto a file/host path this job
  can write.
- Promote v8-highway into the phone-lookup tool. Yesterday's overlay measured 50 of 111 zips
  running a different day than the standing grid for the week of 08-10 — that week is now, and
  today's sheet no longer flags it because the overlay moved to the empty 08-17 week.
