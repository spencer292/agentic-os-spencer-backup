## Session 1

### Project
technician-route-automation

### Goal
Push next week (2026-08-31..09-04) to OptimoRoute with Jobber's hand-corrected days and techs as truth.

### Deliverables
- `projects/briefs/technician-route-automation/_sync_0831.log` — live sync run, 555 orders, 554 Jobber time writes, 0 failures
- `projects/briefs/technician-route-automation/drift-runs/j2o-sync-2026-08-29T23-41-19-960Z.json` — sync report
- `projects/briefs/technician-route-automation/_visits_post.json` — post-push snapshot

### Decisions
- 3 Wed 9/2 Maple Valley visits sat on "Courtney" (an admin, no OptimoRoute driver record) — the sync pre-flight aborted on it. #5427 turned out to be the duplicate copy and was deleted; #7819 + #7684 moved to Cory, who already had 18 stops in 98038 that day.
- Deleted 8 surplus duplicate visits (same job, same day) before pushing: #4608, #7902, #8437, #6239, #5427, #7285, #8417 x2. Keeper = committed arrival window, else oldest visit id.
- Cory's empty Friday 9/4 is intentional (Spencer) — pushed as-is.
- Used `jobber-to-optimo-sync.mjs` (Jobber = truth for day + tech), not `push-week.mjs` (grid-driven, lockTechs=false), because the board was hand-corrected.

### Open threads
- `verify-week.mjs` is BROKEN against the current `territory-grid.json`: it reads its roster from a `works` map that no longer exists in that file, so every tech reports "not driving" and every Jobber count reports 0. It cried mismatch on all 5 days when the sync's own verification passed. Fix the roster source or the script is useless as a check.
- 3 Tuesday stops finish 1-11 min past 18:00 (Alias #8036, Luke #4425, Tavis #8372). Tuesday is the heaviest day at 131 visits.
- RESOLVED 2026-08-30 (Spencer): Courtney's profile is a deliberate STAGING BUCKET for mass-moving jobs, not a mis-assignment. Nothing upstream to fix. But the sync pre-flight aborts on her every time (no OptimoRoute driver record), so a week staged through her must be moved onto real techs before any push.
