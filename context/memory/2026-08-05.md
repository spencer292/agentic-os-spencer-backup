# 2026-08-05

## Session 1

### Project
technician-route-automation (with jobber-notes-automation)

### Goal
Push newly-added Jobber jobs through the Jobber↔OptimoRoute workflow (overriding the arrival-window
freeze), fix customers getting exact times instead of windows, then re-evaluate the whole routing
approach — Spencer: "this right now is a failure."

### Deliverables
- `projects/briefs/technician-route-automation/2026-08-04_routing-redesign-plan.md` — full diagnosis + proposed model, awaiting decisions
- `projects/briefs/technician-route-automation/grid-alignment-audit.mjs` — three-way Jobber/OptimoRoute/grid diff
- `projects/briefs/technician-route-automation/grid-tech-realign.mjs` — force OR + Jobber onto grid tech (superseded by Jobber-truth model)
- `projects/briefs/technician-route-automation/jobber-to-optimo-sync.mjs` — **the keeper**: Jobber is truth for day+tech, OptimoRoute only re-sequences and hands times back
- `projects/briefs/technician-route-automation/move-order.mjs` — move one order to another day/tech without re-planning the source day
- `projects/briefs/technician-route-automation/area-frequency.mjs` — repeat approaches + stops/miles/minutes per tech-day
- `projects/briefs/technician-route-automation/continuity-check.mjs` — upcoming tech vs. who actually served the customer
- `projects/briefs/technician-route-automation/restore-continuity.mjs` — put visits back on the previous tech (built, NOT run — see Decisions)
- `projects/briefs/technician-route-automation/week-buildout-audit.mjs` — volume vs. baseline week, tech-map coverage, follow-up gaps
- `projects/briefs/jobber-notes-automation/decide.mjs` — rewritten: cadence from product + activity + catch, not free text
- `projects/briefs/jobber-notes-automation/engine.mjs` — product classification, write cap, SET refusal, audit logging
- `cron/jobs/jobber-visit-followups.md` — **flipped to `--execute`**; the engine now books follow-ups nightly
- `cron/jobs/jobber-arrival-window-sweep.md` — weekly Monday → twice daily (06:10 + 13:30)

### Decisions
- **Jobber is the source of truth for day and tech; OptimoRoute is the optimizer** (Spencer). It may re-sequence within a day and hand times back — never reassign or move days. `jobber-to-optimo-sync.mjs` implements this; `grid-tech-realign.mjs` does the reverse and will fight it.
- **Arrival-window freeze removed as a blocker** (Spencer: "the route changes all the time and it is what it is"). Customers are not re-notified on re-times.
- **Cadence comes from what the tech found, not what they typed.** Quick Fix = always weekly for its 5-week series; finished + activity → Task (sales call), finished + quiet → nothing owed. TMCP = catch or ANY activity (L/A, M/A, H/A) → weekly; `N/A` with no catch → monthly.
- **Product is identified by line item, not `jobType`** — every Got Moles job is RECURRING, Quick Fix included.
- **Continuity is measured from 2026-08-03 forward, not from history.** Before the 3→4 route split Alias rode with Cory, 21% of visits carried two assignees, and Spencer Hill / Robert Norton were still on the board — "who served them last" is unanswerable before that date.
- **Did NOT run the 110-visit continuity restore.** Measured against the correct baseline (the hand-assigned week) next week is 260/268 = 97% correct, only 8 differ. The apparent "50% break" was an artifact, and restoring would have reversed Spencer's Aug 1 territory re-cut and given Cammeron a 44-stop Tuesday.
- Territory grid v5 kept as-is; v6 stays unpushed.

### Open threads
- **16-visit catch-up for Aug 3–4 not run** — needs an `--execute` approval prompt. 5 of those customers caught a mole and sit 7–36 days out (worst: #4975 Brian Beans, 36 days). Last of the backlog; the nightly engine covers everything from 2026-08-05 on.
- **Next week (Aug 10–14) is still on grid rules**, including 240 Jobber tech writes made before the Jobber-truth decision. Re-sync with `jobber-to-optimo-sync.mjs` once Spencer lays it out.
- **8 open redesign decisions** in the plan doc — region rhythm map is the one only Spencer can answer.
- Region-split rule to encode: an overloaded area splits across two days at **opposite ends of the week**, never adjacent, so new callers are reached sooner.
- OptimoRoute `get_drivers` returns `AUTH_KEY_UNKNOWN` — driver home locations can't be audited via API. 22% of all miles are the drive to the first job (Cory 36%); needs eyeballing in the OptimoRoute UI.
- `#7639` (Aug 12) still missing its OR order — the 09:00 drift cron will create it.
- Orphan OR stops with no active Jobber visit: `6986-1845637645`, `8055-2229493540`, plus 3 new today.

---

## Session 2

### Project
callrail-faq (Muhammad training) + technician-route-automation (Cory training)

### Goal
Get Muhammad able to create clients and send quotes in Jobber; then get Cory able to turn an
approved quote into a correctly-scheduled job.

### Deliverables
- `projects/briefs/callrail-faq/2026-08-05_muhammad-jobber-training.md` (+ .pdf, + copies in `muhammad-portable/`) — client creation + quoting SOP, built from the real product list and the last 40 quotes
- `projects/briefs/technician-route-automation/2026-08-05_cory-scheduling-training.md` (+ .pdf) — approved quote → scheduled job, with territory card and the Spencer-reassign cleanup list
- `projects/briefs/callrail-faq/2026-08-03_service-day-scripts.md` — scoped: booking rules now explicitly cover existing customers only
- `projects/briefs/callrail-faq/muhammad-portable/README.txt` — Jobber section + who-schedules-what added (Spencer rewrote the whole README this session)

### Decisions
- **Scheduling split (Spencer):** newly-approved quotes → Cory. Existing customers (next visit, reschedules, on-file-nothing-booked) stay with Muhammad off the address lookup. The 2026-08-03 booking rules still stand, now scoped.
- **Fixed two product defaults in live Jobber:** Military Discount and First Responder Discount were saved at **+$50** and were adding money to quotes. Both set to **-$50**. Evidence: Military appeared 9× in 300 quotes, negative every time — the sign was being hand-corrected on every veteran's quote.
- **Friends and family stays +$250** (Spencer): it is a replacement rate, not a discount line — used *instead of* the Quick Fix line.

### Findings
- **68 upcoming visits across 24 jobs are still assigned to Spencer Hill**, out of field permanently since 2026-07-29. Earliest Aug 17. Full list is in Cory's doc as his first cleanup task. Scan covered jobs #8201–#8325 only — a full-account sweep may find more.
- Grid-v5 conformance on the 45 jobs created since 2026-08-01: recurrence day **64%**, tech 80%, first visit `(SET)` 84%, **TMCP 5-weekly ramp only 50%**. The two weak numbers (recurrence day, TMCP ramp) are what Cory's SOP targets.
- Job build conventions confirmed from live data: Quick Fix = Weekly ×5, first `(SET)`, last `(5th Visit)`, $150 deposit. TMCP = 5 weekly visits then `Monthly on the {nth} {weekday}`, ~10-year horizon, qty 12 on the quote, $0 deposit. Quote title/message left blank — the product description is the customer-facing text.

### Open threads
- Full-account sweep for visits assigned to not-working techs (Spencer/Tavis/Robert) beyond the #8201–#8325 window.
- TMCP weekly-ramp miss rate (50%) has no automated guard — the nightly engine corrects cadence after the first note, but a new TMCP with no weekly ramp waits a month for visit 2.
- Cory's doc is untested against a real conversion — worth walking one live approved quote with him and correcting the SOP from what actually happens in the UI.
- Cory's doc updated 2026-08-06 for the new miss-counts-as-activity rule; the cadence table now states activity is binary (fresh sign OR catch OR miss) and that old mounds don't count.
- `context/learnings.md` has **two `## tool-jobber` sections** (lines ~84 and ~175) — pre-existing drift, not merged this session. New entries went in the first (active) one.
