# 2026-08-13

## Session 1

### Project
sip-softphone-line

### Goal
Fix the Telnyx number reading "not in service" and get Muhammad live on Zoiper from his MacBook.

### Deliverables
- `projects/briefs/sip-softphone-line/brief.md` — Level 2 brief, verified account state, scope decision
- `projects/briefs/sip-softphone-line/2026-08-13_muhammad-zoiper-setup.md` — Telnyx build + as-built config + 7-step test ladder
- `projects/briefs/sip-softphone-line/2026-08-13_muhammad-mac-setup.md` — Muhammad-facing setup sheet (plain language, sendable)

### CallRail state found at end of session
- **The cutover already went to EVERY number, not the scoped Facebook-only pilot.** Website pool (9),
  Facebook, Google Ads Assets, GMB Enumclaw/Seattle/Tacoma, Bing, Mole Busters, Voice Assist Fallback —
  all on call flow **"Muhammad Workflow"** (`fcea6711-ffd9-4a82-9f30-d19ba34ca88c`).
- Flow shape is correct: Greeting → **Dial: SIP `gotmolesmuhammad@sip.telnyx.com`** → 20s timeout → Voice Assist.
- **"Prevent voicemails and automated systems from answering a call" is UNCHECKED** — screening is OFF, so
  the long-running "unsafe resting state" thread is resolved: a human can accept calls.
- Whisper is per-NUMBER: **On** only on Google Ads Assets (253-331-2772, id `205474854`) — that is the
  "Got Moles call from Google Ads" announcement. Facebook (`497381872`) and Website pool (`975919498`) are Off.
  Path to change: Numbers → Edit → Call options → Whisper message. CallRail account `550369647`, company `438678888`.

### Decisions
- **Both faults have one cause.** Read live from the Telnyx portal: account is **PRETRIAL**, **0 phone
  numbers**, **0 orders** ("feature not permitted at this account level"), **no voice application**.
  The DID reads "not in service" because no number was ever provisioned; the Zoiper `403 Forbidden R18`
  on REGISTER is the same gate. Nothing was misconfigured — the account was never opened far enough.
- **No DID purchase for now.** CallRail dials a SIP URI, which addresses the credential directly, so the
  number is off the critical path. A fresh DID dialing many unique US numbers from an offshore agent is
  the exact fraud pattern that killed Quo on 08-11. Outbound callbacks stay in CallRail's dialer.
- Upgrade path requires a **LinkedIn or GitHub identity connect** — Spencer's hands, cannot be automated.
- **Spencer believed he upgraded on 08-12; he had not.** Account email is `spencer@got-moles.com` (right
  account), but account-levels shows only "Email is verified" checked, and the upgrade page still shows the
  first-step identity-connect prompt, not a pending-review state. Profile company name / contact phone /
  service address are all blank — those are Paid-level prerequisites. The purchase he remembers was the
  Zoiper Pro license.
- **Corrected target: `Paid` level, not `Trial`.** Trial cannot buy numbers or run production voice. Paid
  needs: identity checks + verified phone + service address + 2FA + a credit-card payment. Five gaps open.

### Resolved this session
- **Telnyx account is now `Paid`** (all six criteria green, balance $19.95). Path taken: LinkedIn identity
  connect → Trial, then verified phone + service address + 2FA + card payment → Paid.
- **Portal traps found the hard way** (all cost time, all now in the setup sheet):
  - "Verified phone number" is **NOT** the profile Contact Phone field — it is a separate ownership check
    at `#/numbers/verified-numbers` → *Verify Number*.
  - "Service address" is **NOT** the profile address — separate record at `#/billing/service-address` → *Create*.
  - `#/account/account-levels` is a **lagging indicator** — it showed a red X for the phone while
    `#/numbers/verified-numbers` already listed it verified at 09:26. Read the underlying page when they disagree.
  - Billing lives at account icon → **Manage billing** → `#/billing/payment`; every guessed billing URL 404s.
  - **At Trial, PayPal and Bitcoin are DISABLED — credit card is the only method.** Catch-22: the card is the
    only route to Paid. Payment is a 3-step wizard; the Billing Address on step 1 must match the CARD, not the business.
- Got Moles is a **DBA**; legal entity is **Rainier Power Wash LLC**, 718 Griffin Ave #905, Enumclaw WA 98022.
  Saved to auto-memory. Use the LLC for EIN/tax/carrier registration; set CNAM to "Got Moles" per-number later.

- **SIP connection BUILT** (10:34 AM): `muhammad-softphone`, ID `3025744455840826576`, Credential type, Active.
  SIP address **`gotmolesmuhammad@sip.telnyx.com`**. Receive SIP URI calls = **From anyone** (required —
  "Only from my Connections" blocks CallRail). DTMF RFC 2833 was already default. Added OPUS to the codec
  list for the India link. No outbound voice profile and no DID, both deliberate.
- **Telnyx username must be alphanumeric** — underscores and hyphens are rejected.
- **Portal automation notes:** Telnyx comboboxes ignore `.click()` and native-setter input — they need REAL
  CDP mouse events (`Input.dispatchMouseEvent`) at the element's centre, AND `scrollIntoView` first or the
  click lands off-viewport on these long forms. Text inputs accept the native-value-setter + input/change
  technique fine. Helper written at scratchpad `cdp-pin.mjs` (also pins the tab by URL, since `cdp.mjs`
  grabs whichever page target is first and that flips between calls).

- **REGISTRATION PROVEN 2026-08-13.** MicroSIP/3.22.12 on Spencer's Windows box registered to
  `sip.telnyx.com` over UDP — Telnyx-side status: Registered, IP 192.168.1.224. **The 08-12 `403 Forbidden
  R18` was never a credentials or connection-type problem — it was a pretrial account that could not
  register anything.** Test-ladder step 1 passes.
- **Zoiper Pro is one seat per account, so Spencer could not test on his own machine.** Used **MicroSIP**
  (free, portable, no install, no licence) instead — extracted to `C:\Users\spenc\Downloads\MicroSIP\`.
  Registration is registration: any SIP client proves the line. **Linphone** is the free macOS equivalent
  if the Zoiper seat/platform blocks Muhammad.
- **`Check registration status` on the connection's Authentication tab is the verification instrument** —
  but it goes STALE in place. It read `Unregistered` twice while MicroSIP was already online; a full page
  reload showed `Registered` with IP, transport and user-agent. Same lag as the account-levels page.
  Always hard-reload before believing a negative reading.

### Open threads
- **MUHAMMAD'S REGISTRATION WAS NEVER VERIFIED.** He got as far as Zoiper's wizard reporting
  **"SIP TCP found, UDP untested and not found"** on his network. Never confirmed registered from the
  Telnyx side. **First action next session:** connection → *Check registration status* (hard-reload first)
  and confirm User Agent = Zoiper with an Indian IP — NOT MicroSIP/192.168.1.224, which is Spencer's
  now-exited test client.
- **UDP 5060 looks blocked on Muhammad's network.** TCP carries signaling fine, but **RTP media is UDP
  regardless** — so he can register successfully and still have NO AUDIO. The two-way audio test is the
  real gate, and if it fails the cause is his network, not Zoiper config. Fastest discriminator: retry on
  a phone hotspot.
- Test-ladder steps 2-7 all outstanding; only step 1 (registration, from Spencer's machine) passed.
- Whisper on Google Ads Assets — Spencer asked to disable; not confirmed done.
- Muhammad still cannot dial out from CallRail (`Manager` role did not fix it). **Needs a screenshot of
  his screen** — 08-12 was diagnosed blind and that is what made it expensive.
- **CallRail screening still ON** — Voice Assist answers, no human can accept. Not a safe resting state.
- **CallRail screening is still ON** — Voice Assist answers, no human can accept a call. Not a safe
  resting state; do not leave it over a working day.
- Muhammad still cannot dial out from CallRail (`Manager` role did not fix it). Next suspect is
  per-company access on his user record. **Needs a screenshot of his screen** — 08-12 was diagnosed
  blind and that is what made it expensive.
- Untracked `Voip Passwords.txt` in repo root — gitignored now, but never `git add -A`.

## Session 2

### Project
route-engine / technician-route-automation

### Goal
Build next week's routes (2026-08-17..08-21) for five techs — the week Tavis returns and Cory stays in the field.

### Deliverables
- `projects/briefs/route-engine/2026-08-13_five-way-cut-EXECUTED.md` — as-built record, final week shape, structural finding
- `projects/briefs/route-engine/data/day-override-2026-08-17.json` — week-level day map adjustment with per-block reasoning
- `projects/briefs/route-engine/scripts/project-week.mjs` — read-only ownership + day preview for a real week
- `projects/briefs/technician-route-automation/fix-defects-0817.mjs` — postal-code fix + Pacific-time weekend check
- `projects/briefs/technician-route-automation/territories.json` — v9 five-highway cut over live (v8 backed up)
- `projects/briefs/technician-route-automation/rebalance-week.mjs` — rhythm date-awareness, `--rhythm-override`, minute-weighted day balance, post-greedy balance sweep

### Result
- 216 ownership writes, 498 day+time writes (311 changed weekday), 0 failed, 0 unscheduled.
- Board verified clean: 0 unassigned, 0 on Spencer, 0 weekend, 0 untimed.
- Hours: Alias 47.9, Tavis 46.6, Robert 45.2, Luke 35.2, Cory 30.9 (205.9 total).

### Decisions
- Executed the 08-12 decision (Option A highway map, churn absorbed in one move) rather than re-opening it.
- Day map fixed at the WEEK level via an expiring override file, not by editing the standing v9 rhythm — the standing map is tuned on census volume and should not drift week to week.
- Balanced by estimated minutes rather than stop count; `driveMinPerStop` spans 4.5–18, so equal visit counts produced a 10.9h day next to an 8.2h one.

### Open threads
- **Tavis's home address still unknown** — his Monday route start is a guess; not readable via the OptimoRoute API.
- **Re-run ownership + re-plan before 14:00 PT Friday 08-14** — Monday's arrival windows email then, and new visits kept landing all session (495 → 501, five of them onto the wrong tech mid-run).
- North/east is over capacity (Alias 47.9 / Tavis 46.6 / Robert 45.2) against southern slack (Cory 30.9 / Luke 35.2). Proposed: Enumclaw block (27 visits, ~8h) Robert → Cory, which is highway-legible and puts Cory on his own doorstep. Alias's Eastside is more than one tech at this volume.
- `prune-stale-orders` cannot see UNDATED leftover OptimoRoute orders — planning pulls them in. Prune should also run AFTER planning.
- 3 visits marked complete on future dates (#6400, #5511, #6241); 23 addresses never geocoded (run `geo-cache-build.mjs --write`); Federal Way loan to Luke still open-ended.
- Corrected the 08-12 review: there are NO Saturday visits — that was a UTC-vs-Pacific error in reading `startAt`.

### Session 2 — corrections after Spencer's review
- **Robert is the SLOWEST tech, not Alias** (Spencer called it; re-measured and confirmed). The
  17.7 min/stop figure in the 08-10 pace note came from 07-13..08-09, BEFORE Robert was in the
  field — a thin sample treated as fact. Re-measured over 08-10..08-13 (494 completed visits, 419
  gaps): **Cory 15.3, Alias 19.7, Luke 20.9, Robert 21.2** median min/stop, all-tech 19.4. Cory is
  28% faster than Robert. Lesson: check the sample window covers the days the tech actually worked
  before quoting a per-tech rate.
- **Tavis's home was never missing — it is set in OptimoRoute as his start location.** Next week's
  routes were planned from his real door. The API key cannot read the string back
  (`get_drivers` → AUTH_KEY_UNKNOWN, `get_routes` has no start field), so it is recorded as
  "set, not readable" plus triangulation from first legs.
- **Commute measured off the planned week** (`tech-home-infer.mjs`): Cory 42 mi/1.0h, Alias 81/2.1,
  Robert 87/2.4, Luke 99/2.4, **Tavis 109 mi/2.8h — the highest on the board.** He got Seattle+Renton
  because that ground is central; it is not central to him. His first legs bracket home SE of Renton.
- Roster homes for Robert (Maple Valley) and Luke (Puyallup) do not match their first legs
  (Enumclaw 8.1 mi and Federal Way 4.9 mi) — their OptimoRoute starts may be set elsewhere.
- Enumclaw block → Cory is now the strongest available move: it takes ~8h off the SLOWEST tech and
  gives it to the FASTEST, whose door is 8 miles from that work.
