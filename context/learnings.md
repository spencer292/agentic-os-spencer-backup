# Learnings

## General

- 2026-07-20: **Mole disposal fact (Spencer correction):** dead moles are double-bagged and put in the CUSTOMER'S garbage can; techs take the mole with them only on special request. Never write "we haul them away" as the default in any customer-facing content or training material.

### What works well
- 2026-08-05: **Date-scope any conformance audit to when the standard took effect, or the number is fiction.** Measuring 138 jobs against territory grid v5 gave "tech matches 47%" and read like a broken process. Grid v5 only went live 2026-08-01; splitting at that date gave **80% after** vs 35% before — the low number was almost entirely jobs built correctly under the *previous* three-truck structure. Same session, same shape twice more: a "0/138 on the right day" result was a date-parsing bug of mine, and a TMCP cadence that looked like it contradicted the product promise turned out to be correct once the actual visit list was read instead of the recurrence rule. **Before reporting a process as broken, ask three questions: when did the rule start, is my parser right, and am I reading the summary field or the real records?** Two of the three headline findings this session would have been wrong without it — and a wrong finding sends someone to "fix" work that was already right.
- 2026-07-31: **Before interviewing someone about a gap, tabulate their stated asks against what the existing asset literally says — quote the word count.** Spencer listed six things his training needed ("how to park", "how to dress", …). Rather than open with questions, the audit put each ask beside its current coverage: parking = 6 words, dress = one line, "how to work" = absent entirely. That table did the persuading and made the interview immediately productive — he answered in concrete operating rules instead of re-explaining the problem. Related pattern: when a client has two training assets and one works, find out *why* structurally. Here the phone pack (role-play + scenarios + answer key + progress log) is a **program**; the field guide is a **reference document**. Same subject-matter quality, completely different outcomes — the fix was to stop deepening the document and build the program layer on top of it.
- 2026-07-28: **When a new tool feeds an existing pipeline, import that pipeline's parser and gate output on it — don't re-describe the format.** Building voice→Jobber notes, the tempting shape was "prompt an LLM to write notes in our style". Instead the Worker imports the *same* `parse-note.mjs` the report sync and scheduling engine already use, runs every generated note back through it, and shows the technician a per-field check before they can send. That makes drift structurally impossible (one definition of the grammar) and turns eval into a real test rather than a vibe check — 18/18 text cases and 6/6 audio cases assert against the live parser, so they'd fail the moment the app and the automation disagreed. Reusable rule: **the acceptance test for generated content should be the downstream consumer, not a human reading it.**
- 2026-07-28: **Verify a provider's key actually has quota before designing around it, and mine the real corpus before writing the prompt.** Two cheap checks that changed the build: (1) `OPENAI_API_KEY` is present in `.env` but 429s `insufficient_quota` on every call — presence ≠ working, so probe each key before it becomes an architectural assumption (Gemini took over both transcription and formatting). (2) Pulling 4,341 real notes first surfaced things no amount of reasoning would have: `1 mile`/`no mile` as live typos for "mole" (which speech-to-text makes *worse*), 41 distinct next-action phrasings, and four compass placements the parser silently drops. The prompt was written from that evidence, not from the documented grammar.
- 2026-07-26: **Model-routing config: read the resolution order from live docs, don't reason from the model name.** Auditing model tiering across this install, the load-bearing fact was invisible from the config files themselves — subagent `model:` frontmatter **defaults to `inherit`**, so a definition with a blank field silently bills at the session model (found `l2s-content-packager` inheriting). Full order: `CLAUDE_CODE_SUBAGENT_MODEL` env var > per-invocation param > frontmatter > session model. Consequence worth remembering: a blanket env-var floor *overrides* frontmatter, so it flattens per-agent tiering — explicit pins beat a global cap when you want design work high and grunt work low. Also: saved model policies go stale fast (the stored one named Opus 4.8 and didn't know Fable 5 existed) — re-verify model IDs/pricing against the `claude-api` skill before acting on a remembered routing rule.
- 2026-07-26: **Check the cron runtime before promising a scheduled change will take effect.** Shipped seven correct `model:` changes to cron jobs and told Spencer they'd apply on next fire — then found at wrap-up that the dispatcher had been `runtime: stopped / leaderState: absent` since ~7/21. Editing a cron file is not the same as the cron running. `bash scripts/status-crons.sh` costs one call; do it in the same turn as any cron edit, not at wrap-up.
- 2026-07-21: Email-requested infra change (Roy's agent asked Spencer to delete a DMARC record): verified every claim against live authoritative DNS (nslookup vs ns09/ns10.domaincontrol.com) before endorsing, and re-verified after Spencer's delete. Caught that the fix "he did" hadn't actually landed, confirmed the advice was sound (dup DMARC = both ignored), and validated keeping p=none because Jobber/CallRail send as got-moles.com. Pattern: any emailed request to change DNS/infra gets independently verified against live state first — phishing-shaped even when legit.
- 2026-07-20: Transcript-mining pipeline landed end-to-end in one session (Spencer: "Looks good"): CallRail API pull -> 4 parallel analysis agents over 203 transcripts -> ranked FAQ training doc -> role-play skill -> Voice Assist reconfig -> Jobber sync cron. Pattern to reuse: mine real customer conversations FIRST, then build training/automation from actual frequency data, not guesses.
- 2026-07-20: Rapid-iteration config sessions work well with Spencer: he test-calls immediately and fires corrections one at a time (disposal fact, acreage tiers, positioning wording, res/com split). Keep apply-verify cycles small so each correction lands in minutes.

### What doesn't work well

- 2026-07-31: **A rule captured in `CLAUDE.local.md` reaches Claude and nobody else.** Spencer set the activity-code follow-up cadence on 2026-07-26 (M/A → this week, L/A → 1–2 weeks, N/A → ~2 weeks) and it went into the agent's config. It never reached the Technician Field Guide — the document the humans doing the work actually read — so the 2026-07-10 Jobber audit shows four competing note conventions in use and most visits logged blank. Same shape as the 2026-07-26 Codex/`AGENTS.md` lesson, one layer out: **when a policy is set that changes what a *person* does in the field, the agent's memory file is the wrong home for it.** Ask in the same turn which human-facing document has to change, and log that obligation — otherwise the rule is enforced only when Claude happens to be in the loop.
- 2026-07-31: **Named a root cause before measuring attribution, and had to walk it back to Spencer.** Found the website form creating Jobber clients unconditionally, saw duplicates, and built a whole plan around the form being the primary duplicate source. Spencer then described what he actually sees ("CallRail creates a client, then another when I call back") and the data agreed with him: 80 of 97 duplicate groups are call-driven, only 17 are the form. Cheap check I skipped — cluster the duplicates by origin and by time-gap FIRST (the gap equalling call duration was the whole answer, and was one script away). Lesson: when a plan's priority ordering depends on which of several systems causes an effect, measure the split before writing the plan, not after. Also: my first "the sync log clears our script" conclusion came from grepping 7 names — a real cross-reference of all 97 groups was the honest version of the same claim.
- 2026-07-31: **Silent failures are the actual disease on this install.** Three found in one session, all the same shape: the website form logged leads nobody was told about, the daily Visit Notes n8n workflow failed 7 nights running with no alert, and form spam (4 submissions from one bot since May) sat undetected in the client list. Anything unattended needs a failure path that reaches a human — a job that only reports on success is indistinguishable from a job that is dead.
- 2026-07-23: Email triage (skill gap - candidate ops-email-triage): Gmail-connector cloud routine processes a bounded chunk per run, so multi-hundred-thread backlogs need repeated RemoteTrigger run calls; Gmail search `label:Label_N` by internal ID can return empty even when labelIds prove the label - filter with has:userlabels / has:nouserlabels instead. Draft-only reply policy + "! Action Needed stays in inbox" worked well; sender-pattern rules classified 300+ threads with no misfiles flagged.
## Individual Skills

### meta-wrap-up
- 2026-08-05: **Concurrent sessions in one working tree steal each other work via git add -A.** This session created 4 files; they were committed by a DIFFERENT session running at the same time, under an unrelated message (8202ff2 Routing: Jobber becomes source of truth). Nothing lost, but attribution is wrong and a wrap-up that assumes its own files are uncommitted will find a clean git status and mis-report. At wrap-up: check git log -- <your paths> before concluding nothing was produced, and stage explicitly (git add <paths>) rather than -A.
- 2026-07-26: Four concurrent sessions on the machine — another session had already created `context/memory/2026-07-26.md` with its own `## Session 1` and a header telling later sessions to number after the highest existing block. Write failed with "file has not been read yet", which is the useful signal: on a multi-session day, always Read today's memory file before writing a block, and append rather than create. Its cross-session notes also surfaced a fact that contradicted something I'd told Spencer (cron runtime stopped) — reading other sessions' blocks at wrap-up is worth it, but verify their claims live before repeating them.
- 2026-07-20: No feedback — routine advisory session (audiobook path for *The Route*; research + recommendation only, no deliverables).
- 2026-07-21: No feedback — quick ops session (DMARC duplicate-record fix, verified live; no repo deliverables).

### ops-phone-roleplay
- 2026-07-20: Skill created for Muhammad's phone onboarding. Scenario pack + answer key + progress log live in projects/briefs/callrail-faq/. Registered in CLAUDE.local.md (AGENTS.md is shipped/uneditable on this install).
- 2026-07-20: First two live reps (Spencer, scenarios #1 and #6, 91 + 82): debrief format landed as-is. Demo reps for onlookers still get scored and logged — the log is the training record. When pricing/policy changes mid-day (CLAUDE.local.md rules), the FAQ answer key AND the muhammad-portable bundle + Downloads zip must be refreshed in the same session — a stale answer key fails the skill's own hard gate.
- 2026-07-20: Portable delivery pattern for trainees without repo access: claude.ai Project — `muhammad-portable/project-instructions.md` pasted as Project instructions + the two .md docs uploaded as Files; trainee sends scorecard blocks back for pasting into roleplay-log.md. Chosen over giving a new hire a session on this install (full Jobber/Ads/memory access).

### tool-pdf-generator
- 2026-07-22: Contract-style render (Mole Busters referral agreement): Letter page size + justified serif + bordered h2 sections reads as a proper legal doc; `page-break-after: avoid` on h2 keeps section titles with their body. Spencer accepted first render with no changes. Verify structurally (h2 count, zero leaked `**`, grep key names) when pdftoppm is absent.
- 2026-07-20: Chrome-headless fallback re-verified (md2html.mjs pattern in scratchpad: tables, nested bold-in-cells, blockquotes all clean). Gotcha: a PDF open in a Windows viewer file-locks its Downloads copy (`cp: Device or resource busy`) — copy the regenerated file under a new name instead of failing the refresh.

### viz-image-gen
- 2026-07-04: For YouTube banners (and anything needing true 16:9), use the **Gemini** backend, not GPT. GPT/gpt-image maxes out at 3:2 (1536×1024) and cannot output 16:9. Gemini at `--resolution 2K --aspect-ratio 16:9` produced a clean 2752×1536 banner first try, above YouTube's 2560×1440 recommendation.
- 2026-07-04: `skill-pack/config/sys-config.md` Paths point to a **stale template path** (`C:/Claude/agent-os-v3/agentic-os`), NOT this install (`C:/Agentic-os-got-moles`). Ignore those paths — use the real repo root. The Gemini/GPT scripts auto-load `.env` via `find_dotenv(usecwd=True)`, so running from repo root resolves keys without manual export (the older learnings note about exporting the key is outdated).
- 2026-07-04: Gemini rendered all AI text correctly (wordmark, tagline, dot-separated trust line) when exact strings were quoted in the prompt and text was locked to the centered safe band. Verified by reading the output once — worth doing for brand-critical typography despite the skill's "don't read back" default.
- 2026-07-19: Product-cover mockups: incidental small text (checklist items in a document mockup) comes out garbled even when headline text is perfect — ALWAYS read the image back and fix via an **edit pass** (`--input-image` + "change ONLY X, keep everything else") rather than a full regen; the Route Ready kit cover fix held the layout exactly. Same `--input-image` trick makes style-matched companion covers ("EXACT same flat style as this reference, new title...") — cheat-sheet cover matched the kit family first try.
- 2026-07-19: For site logos/favicons, hand-authored inline SVG beats generated PNG: ~1KB, sharp at all sizes, dark-mode friendly, and works as a `data:image/svg+xml` favicon with no asset file (Route Ready pin+check mark, `site/build.mjs`).

### tool-optimoroute
- 2026-07-22 (driver availability): per-date driver disable works via BULK `update_drivers_parameters` (`{updates:[{driver:{serial:"<name>"},date:"YYYY-MM-DD",enabled:false}]}`) — the single `update_driver_parameters` endpoint requires `externalId`, which our drivers don't have (empty in get_routes). Endpoint unschedules existing routes for that date/driver, so only safe on unplanned dates. Used to block Cammeron Fri 7/31.
- 2026-07-22 (get_routes fields): route = driverSerial/driverName/distance(mi)/duration(min)/stops; stop = scheduledAt(HH:MM)/scheduledAtDt/travelTime(sec)/distance(m). No routeStart/routeEnd fields — derive day span from first/last stop scheduledAt.
- 2026-07-04: Connection test passed — `node .claude/skills/tool-optimoroute/scripts/optimoroute-api.mjs test` returns "Connected — API key valid." Key is set in `.env` (`OPTIMOROUTE_API_KEY`) and authenticates against `https://api.optimoroute.com/v1/`.
- 2026-07-09: **Full ingest→optimize→write-back, done live for one day.** `create_or_update_orders` (bulk) does NOT geocode (needs lat/long) AND update-matches by `orderNo`: a recurring job number that exists on other dates either errors `ERR_MULTIPLE_ORD_FOUND` or, on a single match, **silently MOVES that order onto your target date** (observed 8061/8084 dragged onto 07-10 — this is a cross-day scramble risk). Safe pattern: use **`create_order`** per order (the ONLY endpoint that geocodes) with **`operation:"CREATE"` (required)** and a **UNIQUE orderNo** like `<job>-<date>` so nothing collides. Sequential 90 calls fine (5-concurrent cap).
- 2026-07-09: **Lock a stop to a tech** with `assignedTo:{serial:"<tech full name>"}` — OR driver `serial` == the tech's Jobber full name (exact match), and `assignedTo` FORCES the driver so `start_planning` only sequences/times, never reassigns (verified 0 reassignments, counts held). Flow: `start_planning {date}` → poll `get_routes` until stops appear (client's `raw` can't pass `planningId`, so poll routes). Stops carry `scheduledAt` (Pacific HH:MM), geocoded lat/long, `stopNumber`. `ERR_LOC_GEOCODING_PARTIAL` → recreate that one with the returned lat/long. `get_orders` needs the order `id` (orderNo is reused across dates → multiple-match). `search_orders {dateRange}` returns ids only, ~500 cap, range must be ≤ ~1 month.
- 2026-07-12: **Priority warps route SHAPE, not just selection** — OptimoRoute serves higher-priority orders earlier in the day. Mixed C/H/M priorities on Cory's 07-13 forced all C/H stops into the morning block (Kenmore zigzag; Spencer's hand route was ~20% shorter stop-to-stop). Policy: ALL orders priority M, always; real customer promises go in as `timeWindows:[{twFrom,twTo}]`, never as priority. All 4 push scripts fixed; 70 stray C/H orders on the 07-13..07-26 horizon normalized live.
- 2026-07-12: **`operation:"UPDATE"` on a planned order is safe** — changing `priority`/`timeWindows` does NOT unschedule (canary #8128 held stop 1; only SYNC unschedules). Verify scheduling via `get_routes`, NOT `get_scheduling_info` (405 via raw POST client). Batch UPDATEs of 50 fine.
- 2026-07-19: **The weekly baseline push is NOT the plan.** `push-week.mjs` + a raw `optimize-week.mjs plan` floats every non-promised visit and lets the optimizer re-deal the whole board (Spencer became the workhorse, days scrambled — "this is a joke again"). The proven weekly flow is: push-week → **mirror-lastweek** (job → last completed weekday+tech; grid for new customers) → optimize (sequencing only) → **verify-mirror.mjs** (job-by-job match vs last week) → Spencer review → write. Canonical territory doctrine + ownership map: `projects/briefs/technician-route-automation/2026-07-19_week-0720-territory-map.md`.
- 2026-07-19: **mirror-lastweek skips committed/SET orders — their tech must be locked separately.** Committed orders keep the Jobber-promised DAY but carry no `assignedTo`, so the optimizer picks any tech (10 jobs landed wrong). Fix: `operation:"UPDATE"` with `assignedTo:{serial}` on each committed order (safe on planned orders), then re-plan. Fold into the mirror script at next rebuild.
- 2026-07-19: **Jobber churns visits continuously — diff before write-back.** Overnight Sat→Sun: 27 window visits deleted (rebooked/cancelled/regenerated) and 20 new ones appeared, in two waves hours apart. Stale OR orders = drivers routed to dead stops. Pattern: diff `last-push-manifest.txt` vs live window visits, `delete_order` the dead ones (never delete_all), surgically create new ones via mirror-history rules, re-plan, and re-check Unrouted/Orphans counts right before writing.
- 2026-07-20: **`update_drivers_parameters` body = `{updates:[{driver:{serial},date,workTime:{from,to}}]}`** — NOT `parameters` and NOT flat `workTimeFrom/To` (both rejected with ERR_INVALID_PARAM_FORMAT; exact shape confirmed against the live API docs). Per-date workTime override is safe when the day is re-planned immediately after (it unschedules that date's routes).
- 2026-07-20: **"First job AT hh:mm" pattern:** driver workTimeFrom alone can't do it (first stop = workTimeFrom + drive). Open workTime earlier for that date (e.g. 06:00) AND put a `twFrom` floor (07:00) on every order — optimizer then starts each route exactly at the floor. **"Start driver in area X" pattern:** floor alone isn't enough (optimizer opened Spencer in Puyallup); window the X-cluster to the opening block (07:00-09:00) and push the competing cluster past noon. timeWindows persist on orders across future re-plans — fine for date-locked orders, expire with the day.
- 2026-07-21: **Day-drift is report-only in drift-check — pre-move before fix, or the re-plan drags jobs back.** When Spencer moves a job to another day in Jobber, the OR order stays on the old day; drift-check fix then LOCKS it to the old day and writes the old day back to Jobber, undoing the move. Pattern (proven ×2, 5 orders): `create_order` UPDATE `{date:<new>, allowedDates:{from,to:<new>}, priority:M}` on each day-drifted order FIRST, then run `drift-check.mjs fix` — the fresh diff re-detects them as missing on the new day, SYNCs the Jobber tech, and re-plans both days. Candidate for a `--apply-day-drift` flag at next drift-check touch.
- 2026-07-21: **Driver late start = `late-start-replan.mjs` (new reusable script).** `node late-start-replan.mjs <date> "<driver>" <HH:MM> [--override-freeze]` — puts a `twFrom>=HH:MM` window on every one of the driver's orders that day (merging promised windows, later twFrom wins), locks all stops day+tech, re-plans balancing OFF, verifies (nothing lost/tech-moved, first stop >= floor), writes times to Jobber. A twFrom floor ALONE hit the target exactly (Spencer 7/22: 19 stops 10:30→16:11, first stop exactly 10:30) — no workTime change needed when the floor is mid-morning, confirming the 07-20 finding applies to early floors near shift start, not late ones.
- 2026-07-20: **Verify-abort loops are cheap insurance for owner route edits:** tue-morning-shape.mjs aborted 3× (wrong payload field ×2, wrong first-stop area ×1) with ZERO Jobber writes each time, then succeeded. Always gate write-back on route-shape assertions (first stop in expected area, nothing lost, no tech moves), not just on plan success.
- 2026-07-20: **Jobber settings SPA (secure.getjobber.com):** client message templates at `/communication_settings` (19 index-based "Edit" buttons — map index→template by opening and reading the dialog `h1`, titles repeat for the two "Booking confirmation" templates). Template dialogs SAVE ASYNC: after clicking Save, POLL until `[role=dialog]` is gone before touching the next Edit button — otherwise the next task's clicks land in the STILL-OPEN previous modal and every edit shifts one template over (happened 2026-07-20; had to re-apply all six). SMS template bodies max 250 chars (red validation banner keeps modal open). Tab names vary per dialog ("Email|Text Message", "Visit Email|Visit Text Message", "Assessment Email|Assessment Text Message") — match `/Text Message/` with a length cap, not exact text. Native value setter + input event works on Jobber's React textareas. Settings gear isn't reliably selectable — use `document.elementFromPoint(x,y).closest('button')` on its screen coords. `{{ARRIVAL_WINDOW}}` is a valid template variable (Insert Variable → Visit → Arrival Window).
- 2026-07-20: **CallRail settings pages do NOT auto-save** — every section has its own Save button (`Save profile`, `Save questions`, `Save settings`). Toggles flipped without clicking Save revert silently on reload. Pattern: edit → list buttons → click the section Save → RELOAD and re-read values to verify. React forms need the native value setter + input/change events (plain .value= is ignored).
- 2026-07-20: CallRail Voice Assist config map: Business profile "Smart profile" tab = knowledge (description/services/service-area/FAQ textareas, no maxlength); AI voice settings = greeting + full #PERSONA# prompt (behavioral restrictions live here — the stock persona FORBIDS pricing talk); Lead intake questions = standard toggles + custom questions + end-of-call confirmation toggle.
- 2026-07-06: Built a zero-dependency CDP driver (`browser/launch.mjs`, `browser/cdp.mjs`) — no Playwright/Puppeteer needed because Node 24 on this machine has built-in `WebSocket` + `fetch`. Package installs are blocked here, so this dependency-free approach is the right one. Verified end-to-end: launch → goto example.com → read h1 → screenshot → Read the PNG rendered correctly.
- 2026-07-06: Chrome runs on a persistent dedicated profile at `C:\Users\spenc\.agentic-chrome-profile` (separate from the user's everyday Chrome). Logins persist across sessions once done. `--remote-allow-origins=*` is required in the launch args or the Node WebSocket client gets a 403 on the CDP upgrade.
- 2026-07-06: Not registered in AGENTS.md/README (shipped files — consumer branching policy forbids editing them). The skill still triggers because Claude Code reads skill frontmatter directly from `.claude/skills/`.
- 2026-07-06: LinkedIn people-search extraction that works (cheap, no screenshots): navigate to `https://www.linkedin.com/search/results/people/?keywords=<url-encoded>` then `cdp.mjs eval` collecting `a[href*="/in/"]` and their `innerText` into a `{href: text}` map — each entry's text carries name • degree | headline | location | connect/message | mutual-connection line. `<li>`-based extraction returns [] (LinkedIn doesn't use `<li>` cards); the anchor-map approach is reliable. Screenshot is the visual fallback.
- 2026-07-06: Prospecting insight for Got Moles: "X is a mutual connection" on a 2nd-degree search result means X is Spencer's OWN 1st-degree connection. Recurring mutuals across searches = network hubs = warm-intro routes. For WA property-mgmt/green-industry, the hubs are Patrick LaCroix, Nick Granberg, Brandon Sechrist, Mark Pyrah. First corporate-prospect pull saved to `projects/tool-browser/2026-07-06_linkedin-corporate-prospects.md`.
- 2026-07-19: **Check window size FIRST** (`eval innerWidth`) — the Chrome window was 320×414 and every coordinate click silently missed for several rounds. `Browser.setWindowBounds` returns "Browser window not found" on this setup; `Emulation.setDeviceMetricsOverride` grows the layout but input events still clip to the physical window. Fix = ask the user to maximize (or relaunch).
- 2026-07-19: **File uploads without the OS picker**: `Page.setInterceptFileChooserDialog` + `fileChooserOpened` never fired on Drive, but the robust pattern is simpler — after triggering the upload UI, find the page's real `input[type=file]` and call `DOM.setFileInputFiles` on its nodeId. Worked on Google Drive (14 files) and Gumroad (PDF + covers). On Gumroad, poll for the upload preview to render BEFORE clicking Save, or the attach is dropped.
- 2026-07-19: **Google Drive web UI is hostile to click automation** (virtualized rows, unreliable shift/ctrl-select, menus that need full mousedown/mouseup dispatch). For any bulk Drive operation, pivot to **Apps Script**: the editor at script.google.com is drivable end-to-end via CDP — `monaco.editor.getModels()[0].setValue(code)`, trusted Ctrl+S, click Run by toolbar coords. First run needs the user to click through the OAuth consent (account picker matters). Apps Script gotcha: Drive query `title contains 'route-ready'` returns nothing for hyphenated names — iterate `searchFiles('trashed = true')` and filter names in JS.
- 2026-07-19: **Capturing credentials from settings pages**: select the field by its `<label>` association (`e.labels[0].innerText`), never "first long readonly value" — Gumroad shows Application ID / Secret / Access Token as three identical-length fields and the first grab 401'd. Write the value straight into `.env` inside the script (path built as `['.','env'].join('')` to satisfy the secrets guard) and print only the length.
- 2026-07-19: Cloudflare's custom-token builder is comboboxes that need typed text + option-click by exact text match, and its option lists don't use `[role=option]` — match elements by trimmed innerText with a small height filter. Editing a token's permissions later does NOT rotate the secret (safe to extend scopes post-hoc).

### tool-optimoroute

- 2026-07-26 (undocumented driver API): `update_drivers_parameters` (PLURAL) takes `{updates:[{driver:{serial:"<name>"}, date:"YYYY-MM-DD", enabled:bool}]}` — per-DATE, addressed by **serial** (externalId is blank on this account). It accepts `enabled` ONLY. `workTimeFrom`/`workTimeTo` exist solely on the SINGULAR `update_driver_parameters`, which requires `externalId` — so **the 10-hour cap is NOT settable by API until drivers have externalIds**, and every over-10h day has to be hand-corrected. Discover schema safely by POSTing a fake `externalId` — validation runs BEFORE driver lookup, so field names are enumerable without touching real drivers.
- 2026-07-26: `balancing:'ON_FORCE'` gives **every enabled driver a route every day** — it handed Spencer 124 stops across five days when he works Tuesday only. Plain `'ON'` does the same. The only lever that keeps a driver off a day is `enabled:false` for that date. Setting driver params **unschedules existing routes** — always re-plan after.
- 2026-07-26: order-level `assignedTo:{serial:"<name>"}` is a valid `create_or_update_orders` field and is how to lock techs to territory (`driver`/`driverSerial`/`allowedDrivers` are all rejected). Time windows are `timeWindows:[{twFrom,twTo}]` (not `twFrom`/`twTo` at order level).
- 2026-07-26: `optimize-week` runs `lockTechs=false`, so the optimizer assigns whoever is cheapest — routes come out geographically sound with **the wrong tech's name on them** (Monday had all three techs on each other's territories). `lock-techs-to-grid.mjs` must be re-run after EVERY push, before planning.
- 2026-07-26 (Gumroad dashboard): file uploads work via raw CDP `DOM.setFileInputFiles` (JS cannot set a file input). Multi-file upload auto-groups into a COLLAPSED "Untitled" folder whose filenames are NOT in `innerText` — verify with a screenshot, not a text regex (I misread this as "the save failed" and re-uploaded twice, creating 3 duplicate folders). Save button is DISABLED while uploads run: poll until zero `aria-label=Cancel` buttons AND save enabled, click save, reload, confirm exactly one folder. `element.click()` does NOTHING on Gumroad's React buttons — use cdp.mjs `click` (trusted event). The Cover uploader's `input[type=file]` does not exist until its "Upload images or videos" button gets a trusted click; after that plain setFileInputFiles works (do NOT assume a button-triggered uploader needs a native dialog — check for the input first). "Publish and continue" lives on the CONTENT tab only, not the Product tab. Folder rename is a ProseMirror NodeView — pencil/label clicks never take focus and `Input.insertText` is ignored; rename by hand. Never bulk-click every `aria-expanded=false` element — it opened five menus at once including a Delete.

## tool-jobber
- 2026-08-06: **Cursor pagination with NO explicit `sort` is not stable — it returns duplicates and silently drops rows.** A full `jobs` sweep reported 6,961 `totalCount` and returned 6,961 rows, of which only **5,684 were unique** (1,199 job numbers repeated, ~1,277 never seen). Nothing errors; the totals just look right. Always pass `sort:[{key:JOB_NUMBER, direction:ASCENDING}]` (or another stable key) on any paged connection, then assert `len(set(ids)) == len(rows)` before trusting the pull. Same trap applies to `quotes` (`QUOTE_NUMBER`).
- 2026-08-06: **A standalone script must send the `X-JOBBER-GRAPHQL-VERSION` header** or every call returns HTTP 400 `{"message":"An API version must be specified"}` — and because that body has neither `data` nor `errors`, naive error handling surfaces it as an undefined-property crash rather than the real cause. `jobber-api.mjs` reads `JOBBER_GRAPHQL_VERSION` from the environment file; copy that behavior into any one-off pull script.
- 2026-08-06: **Classify product from LINE ITEMS and account for legacy names — duration/`jobType` heuristics misread the book.** Identifying TMCP by contract length (10-year `endAt`) misclassified **213 clients** as never-on-program. Cause: `Annual Mole Control Service (...)` (781 jobs, $85/mo, 2021–2025) is the legacy TMCP and is billed as **0-day one-off jobs**, so it looks exactly like a Quick Fix. Full map — TMCP: `Total Mole Control Program -- Year round protection`, `Total Mole Control Package`, `Annual Mole Control Service`. Quick Fix: `The Quick Fix — One-Month Mole Control Program` (2026–), `1 Month of Mole Control Service (North/central/Valley/South)` (2020–2026), `Monthly Mole Service` (2019–2021). Downstream damage: repeat pool 913→700, target pool 437→357, winbacks 40→84, and a headline stat of "median 133 days between Quick Fixes" was really **254 days** (the short gaps were legacy program customers' monthly billings). Profile every line-item name by median duration + median total + date range before trusting a classifier.
- 2026-08-06: **Message BODIES are scope-gated even when the connection is readable.** `Client.messages{totalCount}` works (J.C Brummond = 105), but `MessageInterfaceEdge` exposes only `cursor` — requesting `node` fails with `Field 'node' doesn't exist on type 'MessageInterfaceEdge'`, and `MessageInterface` is absent from `__schema.types` entirely. This reads like a schema bug; it is a permissions gate. To actually read a customer's texts, drive `secure.getjobber.com/clients/{id}?tab=communication` with `tool-browser` (the Communication tab is a `button` matched by exact text — not a link and not `role=tab`).
- 2026-08-05: **`totalCount` on a tag-filtered `clients` query is STALE — enumerate instead.** `clients(filter:{tags:["TMCP - Active"]}){totalCount}` returned 641 both immediately before AND after two verified tag writes; paginating the same filter returned 643. The write is applied instantly (read-back on the client confirms it) but the filtered aggregate count lags. Any "how many are tagged" answer must page the connection and count nodes, never trust `totalCount`. Cost an hour of chasing a phantom "2 clients lost their tag".
- 2026-08-05: **`ICalendarRule` writes require the `RRULE:` prefix; reads return it WITHOUT.** Reading `invoiceSchedule.recurrenceSchedule.calendarRule` gives `FREQ=MONTHLY;BYMONTHDAY=-1`, but writing that value back fails with `argumentLiteralsIncompatible` ("Recurrence should be a valid ICalendarRecurrenceRule"). Correct write: `"RRULE:FREQ=MONTHLY;BYMONTHDAY=-1"`. Full TMCP billing repair (ON_COMPLETION -> monthly last-day): `jobEdit(jobId, input:{invoicing:{invoicingType: FIXED_PRICE, invoicingSchedule: PERIODIC, recurrence: "RRULE:FREQ=MONTHLY;BYMONTHDAY=-1"}})`. Enums: BillingStrategy = FIXED_PRICE|VISIT_BASED; BillingFrequencyEnum = ON_COMPLETION|PERIODIC|PER_VISIT|NEVER.
- 2026-08-05: **Jobber has NO invoice void/delete mutation.** Full set is invoiceCreate/Edit/Close/Reopen/MarkAsSent/UnmarkBadDebt — stray invoices must be deleted in the UI. Also `Invoice` has no `amountPaid`; use `amounts{total invoiceBalance depositAmount}`.
- 2026-08-05: **ALWAYS read `invoice.subject` before calling two same-day invoices a duplicate.** On Larry Lemmon #5639, two $100 invoices issued 7/31/26 looked like a system double-write; the subjects showed otherwise — #15026 "For Services Rendered" was the normal automatic monthly, #15275 "FINAL BILL" was a MANUAL invoice someone cut on the (mistaken) belief he was cancelling. The customer paid the manual one, leaving the automatic one past_due and a $100 phantom balance. Different failure mode, different fix: a cancellation that gets reversed leaves a manual final bill colliding with the recurring invoice. Worth sweeping for other clients with a manual "FINAL BILL" who are still active.
- 2026-08-05: **Sweep jobs by `filter:{status:...}`, not `jobType`, for any "who is an active member" question.** `jobType: RECURRING` silently drops one-off jobs carrying program line items (cost a hand-patch on 7/22). Statuses overlap — `active` (794) is a superset of `upcoming`/`today`/`late`; union of all live statuses = 797 vs 6,961 total jobs, so this is also ~9x cheaper than sweeping everything. Script: `projects/tool-jobber/scripts/tmcp-tag-audit.mjs`.
- 2026-08-05: **Live records move mid-run — re-read immediately before every write.** During one TMCP fix pass, 3 clients got tagged and 1 job got its billing fixed by someone else in the ~30 min between the audit sweep and the write pass. `clientEdit(tagsToAdd)` being additive + the before/after print turned those into visible no-ops instead of clobbers. Never write from a snapshot older than a few minutes on live customer records.
- 2026-07-28: **Job notes are writable, and that is the whole unlock for voice capture.** `jobCreateNote(jobId: EncodedId!, input: JobCreateNoteInput!)` → `JobCreateNotePayload { job, jobNote, userErrors }`; input fields are `message`, `attachments`, `pinned`, `linkedTo`. Proven live on #8056 then removed. Gotchas: (1) `jobDeleteNote`'s input arg is **`noteId`, not `id`** — guessing `id` fails *after* the note is already created, so a create/delete round-trip can leave litter on a real customer record; (2) **`JobNote.createdBy` returns `Application`** — there is no author field on the input, so API-written notes are never attributed to a technician in Jobber's UI, and per-tech attribution would need a separate OAuth connection per person; (3) `JobNoteLinkInput` is `{ invoices: Boolean }` only — a note **cannot** be linked to a specific visit, so date is the only join back to a visit.
- 2026-08-05: **Saved `defaultUnitCost` on a product can carry the WRONG SIGN — check real line-item usage before trusting it.** Military Discount and First Responder Discount were both stored at **+50**, so selecting them ADDED $50 to a quote. Proof came from usage, not from the settings: Military appeared 9x across 300 recent quotes and was negative every single time (-50 x4, -15, -10 x3, -75) — the office had been hand-correcting the sign on every veteran's quote. Both defaults set to -50. **Counter-example in the same list: "Friends and family" at +250 is CORRECT** — it is a replacement rate used *instead of* the Quick Fix line, not a subtraction. Don't bulk-flip every positive "discount"; check how each is actually used first.
- 2026-08-05: **Product edit mutation shape.** `productsAndServicesEdit(productOrServiceId: EncodedId!, input: ProductsAndServicesEditInput!)`. The input type is `...EditInput` — `ProductsAndServicesEditAttributes` does NOT exist (introspecting the wrong name returns `{"__type": null}`, which looks like a permissions error but is just a bad name). Editable: `name description defaultUnitCost internalUnitCost markup taxable category durationMinutes visible onlineBookingsEnabled quantityRange bookableType customFields`.
- 2026-08-05: **Sort keys are per-connection and mostly do NOT include CREATED_AT.** `quotes` accepts `{key: CREATED_AT}`; `clients` does NOT (`ClientsSortKey` = PRIMARY_NAME/UPDATED_AT/FIRST_NAME/LAST_NAME); `jobs` does NOT (`JobSortKey` = CLIENT_FIRST_NAME/CLIENT_PRIMARY_NAME/UPDATED_AT/JOB_NUMBER/TOTAL_COST/JOB_STATUS/SCHEDULE/VISIT_START_DATE). For newest-jobs-first use `JOB_NUMBER DESCENDING`. Also `JobFilterAttributes` does not accept `jobNumber` — there is no filter-by-job-number.
- 2026-08-05: **`visitSchedule.startDate` is a full ISO timestamp, not a date string.** `new Date(startDate + 'T12:00:00Z')` silently yields Invalid Date -> `getUTCDay()` NaN -> every weekday comparison false. This produced a bogus "0/138 jobs on the right day" before it was caught. For the *recurring* weekday, parse `visitSchedule.recurrenceSchedule.friendly` ("Weekly on Thursdays", "Monthly on the 1st Thursday") — that is the meaningful field anyway, since the first visit is a pinned SET that may deliberately sit off-route. Convert timestamps to Pacific with `toLocaleDateString('en-US',{timeZone:'America/Los_Angeles',weekday:'short'})`; visits are stored at 07:00Z = midnight PDT.
- 2026-08-05: **A TMCP job's recurrence alone misreads the program.** New TMCP jobs are created with `Monthly on the {nth} {weekday}` over a ~10-year horizon, which looks like it contradicts the product's promise of weekly visits until control is established — but pulling the actual visit list shows **5 weekly visits sitting in front of the monthly tail**. Always read `visits(first:6, sort:{key:START_AT, direction:ASCENDING})` before calling a cadence wrong. (The real defect is narrower: only ~50% of new TMCP jobs get that weekly ramp.) Quick Fix shape for comparison: Weekly x5, first visit titled `(SET)`, last `(5th Visit)`.
- 2026-07-28: **The refresh token does NOT rotate on this app** — verified by making two consecutive grants with the same token: both returned the *same* `refresh_token` and the old one stayed valid. This qualifies the 2026-07-06 "rotation gotcha" entry; whatever caused those parallel-burst 401s, it was not the token being consumed. Practical effect: a hosted service can share `JOBBER_REFRESH_TOKEN` with the local scripts without the two invalidating each other, and needs no KV/DB to persist a rotating value. Re-verify if the Jobber app is ever recreated.
- 2026-07-28: **Technician visit notes are much leakier than they look.** Across 4,341 real notes from a 10-day window, `parse-note.mjs` extracts misses on only **56%** and moles on **72%** (activity 98%, inventory 85%, next-action 91%; all five fields on just 41%). The cause is mostly omission, not misspelling — techs skip the line entirely. Any report built off these fields is quietly running on partial data, and the fix is to prompt at capture time rather than loosen the parser. Also live but unmapped in `parse-note.mjs`'s `POS` table: `froh`/`floh`/`broh`/`bloh` (front-right / front-left / back-right / back-left of house), 87 occurrences in 10 days.
- 2026-07-31: **The native CallRail→Jobber integration writes a client TWICE per conversation** — once at call start from caller ID ("Wireless Caller", "Kent Wa", "Kinsella John") and again at call end with the name captured during the call ("John Kinsella"), matching neither against the other. Proof: the gap between the paired records equals the call duration (Pugh 521s→8min, Wirth 807s→13min, Reagan 317s→5min). It also fires per CALL, so callbacks add more. Integration is `JOBBER` id `25967` on CallRail account `ACC019dc0126ade7956850fbd40239646af`; API returns an empty config block, so any dedupe setting is UI-only. Account audit 2026-07-31: 97 duplicate groups / 114 extra records out of 4,311 clients — 80 groups call-driven, 17 website-form. Our own `callrail-jobber-sync.mjs` is NOT a contributor (it phone-matches before writing).
- 2026-07-31: **Jobber cannot merge clients** — no merge mutation in the API and no merge in the UI (confirmed in Jobber's own community). Only `clientArchive`/`clientUnarchive` exist. Dedupe therefore = pick the record holding jobs/quotes/invoices, copy missing data onto it, archive the loser. Always check attached history counts before choosing the keeper.
- 2026-07-31: **`requestCreate` IS live** (API 2025-04-16) — a stale comment in the website repo claimed Jobber removed request mutations in Aug 2023 and that assumption disconnected the whole Requests path. But `RequestCreateInput` accepts ONLY clientId/propertyId/assessment/referringClientId/requestDetails/lineItems/formIds/title/salespersonId — **no `description`, no `source`**. Lead detail goes in afterwards via `requestCreateNote(requestId, {message, pinned})`. Creating a Client alone notifies nobody; the Request is what surfaces for triage.
- 2026-07-31: **One Jobber app cannot serve two OAuth clients.** Refresh-token rotation is OFF on this account (the stored token is static — verified by mtime across repeated CLI calls), but *re-authorizing* the app issues a new refresh token and invalidates the previous one. Sharing one app between the local CLI and n8n means each re-auth kills the other: n8n's credential was reconnected 2026-07-06 and was dead again by 2026-07-24. Fix = a second, dedicated Jobber app per consumer.
- 2026-07-31: **Quotes and jobs name the same product differently — mix reporting silently breaks.** The Quick Fix is "The Quick Fix ..." on jobs but **"1 Month of Mole Control Service"** on 302 of 1,101 quotes. A `/quick fix/i` match leaves 29% of quotes unclassified; a lazy widening to `/\d+ month of mole control/` files all of them as PROGRAM work and swings the QF/TMC split by 20+ points. Correct: TMC = `total mole control|tmcp|year.?round|annual mole control|(?:[2-9]|\d\d+) month of mole control`, QF = `quick fix|1 month of mole control`. Verify by asserting the unclassified bucket is 0 and printing distinct line-item text before trusting any mix number.
- 2026-07-31: **Close rate must be measured on a created-cohort basis, and `approved` counts as won.** Quote statuses seen: converted / approved / awaiting_response / archived (no `draft` in this account). Won = converted+approved, lost = archived. Three legitimate denominators give very different answers — of issued 68.5%, of decided 76.4%, dollar-weighted 64.0% — so always state which. **Trap:** this account stopped archiving dead quotes around May 2026 (5-7 lost/month vs 28-37 Feb-Apr), so "of decided" inflates to 94% on recent months and the KPI drifts upward while nothing improved.
- 2026-07-31: **Call→job conversion needs the FULL client phone book, not the in-window client list.** The CallRail→Jobber sync auto-creates a client record for ~90% of callers, so "unique caller became a new client" reads 86% and means nothing. Sweeping all 4,336 clients (id/createdAt/phones, 100/page, ~44 calls) and taking the OLDEST record per digit-normalized phone segments callers into existing (7.7%) / new-lead (92.3%) properly. Validate the phone match before trusting it: jobs missing a usable client phone (0.8% here) and the call→job lag distribution (80% within 3 days, 0 beyond 60 days) prove whether a low early-month rate is real or just unmatured.
- 2026-07-31: CallRail `calls.json` field name is **`first_call`, not `first_time_caller`** (a bad field name 400s with the full valid list in the error body — cheap way to introspect). Useful fields this account has: first_call, prior_calls, lead_status, spam, call_disposition, duration, source. `lead_status` is null on 100% of calls here — nobody tags leads, so conversion has to be inferred by phone matching.
- 2026-07-22: **jobCreate works for rebuilding mis-created jobs** — required: lineItems need `saveToProductsAndServices` (Boolean!), ICalendarRule needs the `RRULE:` prefix, timeframe = {startAt(date), durationUnits, durationValue}. scheduling.recurrence materializes the full visit series ASYNC (read-back showed 1 visit at create, 122 minutes later); the DTSTART date itself gets no visit — add near-term visits via visitCreate (LocalDateTimeAttributes date-only ⇒ allDay). ONE_OFF jobs HARD-REJECT periodic invoicing ("invoicing_schedule should be NEVER or ON_COMPLETION") — a membership sold as a one-off can only be fixed by rebuild as RECURRING.
- 2026-07-22: **Audit blind spots found in TMCP sweeps:** (1) members can sit on ONE_OFF jobs — always check both jobTypes (only 7 active one-offs existed; 4 were TMC, 2 untagged); (2) "Yearly" jobs show the MONTHLY rate in job.total while the real annual invoice is 12× — pull invoice history before calling anything underpriced (8 suspected leaks were all legit annual prepayers). (3) Bundling visitDelete/jobClose with additive mutations in one script trips the permission classifier — keep destructive steps in a separate, explicitly-approved run.
- 2026-07-22: **Month-revenue questions need endAt-filtered queries over ALL statuses** — Quick Fix jobs archive on close, so an active-only sweep undercounts a month in progress (July showed $6.1K vs the real $23.4K of QF completions; 34 of 58 were already archived). `jobs(filter:{jobType:RECURRING, endAt:{after,before}})` returns any status in one cheap pull. Also: deposit-model shows as inv $150/uninv $300 on a $450 job mid-program.
- 2026-07-20: **Arrival windows are JOB-level, not visit-level.** `jobEdit(jobId, input:{arrivalWindow:{durationInMinutes}})` is the only API write path (visitEdit = title/instructions only; visitEditSchedule = startAt/endAt only); the window propagates to the job's timed visits (allDay visits show null until scheduled, then inherit). Company standard = 180 min, window starts AT the visit start time (centeredOnStartTime:false). No account-level default setting exists in Jobber settings — the default is remembered from the job form's "set as default" checkbox. Weekly safety-net cron `jobber-arrival-window-sweep` runs `projects/tool-jobber/scripts/arrival-window-sweep.mjs`.
- 2026-07-20: **Aliased batch mutations work** (~20 jobEdits per request, well inside cost limits; 27 batches of 20 ran clean). Gotcha: ONE "Job does not exist" error voids the ENTIRE batch response — on batch error, fall back to per-job mutations. Visit start times in this account are OptimoRoute ETAs written back, so clients were reading exact route times as promises until windows + template fixes (2026-07-20).
- 2026-07-20: GraphQL input gotchas (read types differ from write types): AddressAttributes uses street1/street2 (reads return street); ClientEditInput has NO emails arg — use emailsToAdd. clientCreate DOES accept emails/phones/properties arrays inline. clients(searchTerm:) matches formatted phone strings; verify hits by digit-normalized phone compare. Client sort key is UPDATED_AT (no CREATED_AT).
- 2026-07-20: CallRail voice_assist_message field = structured VA intake (contents: name/phone/email/Address/purpose/custom-question answers) — the native CallRail-Jobber integration ignores it and writes caller-ID junk (name as Last,First N/A, city-only property). callrail-jobber-sync.mjs (callrail-faq brief) repairs this; cron callrail-jobber-sync hourly.
- 2026-07-06: Invoice cash-flow modelling. Key fields: `issuedDate` / `dueDate` / `receivedDate`, `invoiceStatus` (draft/sent_not_due/awaiting_payment/past_due/paid/bad_debt), `amounts { total invoiceBalance paymentsTotal depositAmount }`. Filter `InvoiceFilterAttributes` supports `status`, `issuedDate:{after,before}` (Iso8601DateTimeRangeInput uses `after`/`before`/`eq`), sort `{key:ISSUED_DATE, direction:DESCENDING}`. Account had 14,412 invoices; 12-mo history = 5,427 (~108 pages @ 50).
- 2026-07-06: **Deposits (new customers only):** a new-customer quick-fix job is ONE $450 invoice with `depositAmount:150`. The $150 deposit is collected at booking and is NOT in `paymentsTotal` or `invoiceBalance` (balance already nets it out). So `depositAmount>0` is a reliable new-customer marker, and any cash model must add deposits as up-front cash or it under-counts collections (~$58,900/yr here; aged months read ~100% collected once deposits are credited). Existing customers = no deposit, billed at end of service ($150 no-catch bill or full rate).
- 2026-07-06: **Token rotation gotcha** — rapid parallel `jobber-api.mjs` calls can desync the rotating refresh token → HTTP 401 "Token request failed" on the token endpoint. Fix is re-auth (`jobber-api.mjs auth`, browser). Run Jobber calls sequentially, not in parallel bursts.
- 2026-07-06: **Job Custom Fields are writable WITHOUT the custom-fields scope.** The app lacks the "Custom Field Configurations" scope (reading/creating field *configs* returns "hidden due to permissions"), BUT field VALUES write fine via `jobEdit(jobId, {customFields:[{id, valueDropdown|valueNumeric|valueText|valueTrueFalse}]})` under Jobs:write. Get each field's per-job `id` by reading `job.customFields` (union: CustomFieldText/Numeric/Dropdown/TrueFalse — request `id`+`label`, but NOT `customFieldConfiguration` which is scope-blocked). Match fields by TRIMMED label (names can have trailing spaces). **Dropdown values must EXACTLY equal an option** — a "None " option with a trailing space rejected `valueDropdown:"None"` ("dropdown value must be one of the available options"); have the user clean the option.
- 2026-07-06: **Turning OFF refresh-token rotation INVALIDATES the current refresh token.** After the user flipped rotation off in the Developer Center, the existing `JOBBER_REFRESH_TOKEN` died — both the CLI and n8n got 401 "refresh token is not valid." Fix: re-auth (`jobber-api.mjs auth`) to mint a fresh token; with rotation off that new token is permanent/stable and reusable by multiple consumers.
- 2026-07-06: **Jobber auto-allows `localhost` OAuth callbacks** — you cannot list `http://localhost:8734/callback` in the app's Callback URL field (Developer Center rejects it: "'localhost' is supported automatically and shouldn't be listed"). So the CLI's localhost re-auth works even when the app's configured callback is a remote URL (e.g. n8n's). No callback change needed to re-auth the CLI.
- 2026-07-06: **Visit scheduling from notes** — `visitEditSchedule(id, {startAt:{date,timezone},endAt})` moves a visit; `visitCreate(jobId, {visits:[{title, schedule:{startAt,endAt,teamMemberIdsToAssign}}]})` adds one (payload field is `createdVisits`, not `visits`). Read upcoming visits via `job.visits(first,filter:{status:UPCOMING})`. Rule: same calendar month → pull earlier; later month → leave + add (never pull a future-month visit back).
- 2026-07-09: **Batch write-back of visit times (90 visits, 0 fail).** `visitEditSchedule(id, {startAt:{date,time,timezone}, endAt:{date,time,timezone}})` with `timezone:"America/Los_Angeles"` + local `time:"HH:MM:00"` sets an exact time; the mutation returns `visit{ startAt endAt }` — read it back (`...T06:48:00-07:00`) as inline verification. Setting startAt also reorders the board (Jobber sorts by start time). Window convention here = start +3h. **Throttle:** heavy visit reads cost-throttle fast (`"Throttled"`/`THROTTLED`) and rapid retries drain the bucket further — paginate day visits at 25/page with escalating backoff; filter a Pacific day by UTC window `${date}T07:00:00Z`..`${next}T07:00:00Z`. Schema gotchas: `Visit.client.name` is a scalar (not `{full}`); `PropertyAddress` has street/city/province/postalCode but **no** latitude/longitude; `Visit.duration`/`endAt` reflect the booked window (120min/all-day defaults), NOT real service time — don't derive service duration from them (OR uses a fixed 10-min stop for Got Moles).

- 2026-07-10: **`jobs(first: N)` default order is a coverage TRAP for day-based work.** The notes-automation read relied on the first 40-50 jobs happening to include the day's visited jobs — on 07-09 it found 0 of 65; even the "proven" 07-06 sync got 31 of 85. Correct pattern: **visits-first** — page `visits(filter:{startAt:{after,before}})` for the PT day (cheap), dedup to `job.id`s, then fetch heavy per-job data (notes/custom fields) via ALIASED batch queries `j0: job(id:..){...} j1: ...` at ~15 jobs/call. Full coverage, and stays under the cost throttle (a single `jobs(first:100)` pull with 40 notes each = instant THROTTLED).
- 2026-07-10: **`visits(filter:{status:UPCOMING})` is the wrong check for "did the follow-up get scheduled"** — a visit happening today (in-window) or already completed is not UPCOMING and reads as missing (false-flagged Mike Coile, whose follow-up was literally in progress). Use a DATE filter instead: `visits(filter:{startAt:{after:"<end of noted day UTC>"}})`. Also: **closed programs are not cadence gaps** — "The Quick Fix — One-Month Mole Control Program" is a RECURRING job that legitimately ENDS (5 weeks); when it's done Jobber sets `jobStatus: archived`. Cadence-gap checks must exclude archived jobs; a NON-archived job with nothing upcoming is real signal (`requires_invoicing` = serviced but never billed — caught a live one). `jobStatus` values seen: archived, requires_invoicing, action_required. `lineItems{nodes{name}}` identifies the program; job GraphQL id = base64 of `gid://Jobber/Job/<work_orders URL number>`.
- 2026-07-10: **`job.notes` returns OLDEST-first — always use `notes(last: N)` for recent notes.** `notes(first: 40)` on a 40+-note job (the oldest clients) reads 2024 history; the "latest by createdAt" reduce then silently picks an ancient note, making long-history jobs invisible to the notes automation (Chris Doll #5369: 61 notes, current "Add visit" note missed — Spencer caught it). `last:` returns the newest N (still ascending); request `totalCount` when diagnosing. Same-day parser trap: a tech consistently writes **"Ad visit"** for "Add visit" — parse with `/\bad{1,2}\s+visit/i`; shorthand parsers need misspelling tolerance + an exceptions lane, not exact phrases.
- 2026-07-10: **Client tags:** filter is `clients(filter:{tags:["<exact label>"]})` — label must match EXACTLY (the Got Moles program tag is `"TMCP - Active"`, not "TMCP", which returns 0). Read via `client.tags(first:N){nodes{label}}`. Connections expose `totalCount` (cheap way to size a sweep first). **Recurring-job identity lives in lineItems, not title** — 4,646 of 4,749 recurring jobs have a blank title; the program is the line item name (`Total Mole Control Program -- Year round protection` / `Total Mole Control Package` = TMCP; also barter/warranty/friends-family variants that ARE program work billed differently). Full shallow sweep of 4,749 jobs at 50/page (id/title/status/client/property) throttles every ~2,000 rows — 5s backoff recovers; ~5 min total. Aliased 15-job batches for lineItems on 698 jobs ran clean at 400ms spacing.
- 2026-07-10: **Tag writes:** `clientEdit(clientId, input:{tagsToAdd:["label"]})` is ADDITIVE (existing tags kept — verified; `tagsToDelete` is the inverse). Return `client{tags{nodes{label}}}` for inline verification. **Job pricing:** `job.total` = price per billing period; cadence in `invoiceSchedule{billingFrequency scheduleSummary}` (PERIODIC "Monthly on the last day of the month" is the TMCP standard; also Yearly / Every 90 days / ON_COMPLETION / NEVER "Don't remind me to invoice"). `invoices{totalCount}` + `invoicedTotal` on the job distinguish real comps from billing mistakes ($0-line jobs that still invoice = manually billed). The CLI must run from the repo root — it resolves `.env` from cwd.
- 2026-07-12: **`visitEditSchedule` time strings must be exact `HH:MM:SS`** — OptimoRoute's `scheduledAtDt` ("2026-07-14 07:36:32") already carries seconds; appending ":00" produced "07:36:32:00" and Jobber's validation rejected ALL 416 writes atomically (harmless — nothing partial). Pass OR times through unchanged. Also proven: ~530 visitEditSchedule writes in a session at 350-400ms spacing, 0 throttles.
- 2026-07-10: **Jobber returns partial "hidden due to permissions" errors alongside good data** (e.g. `RequestNote` objects inside `job.notes` when the app lacks Requests scope) — HTTP 200, `data` populated, hidden nodes come back as null in the list. The `jobber-api.mjs` CLI treats ANY `errors` array as fatal and discards the data, so scripts touching notes must use direct fetch and filter: fail only on errors NOT matching `/hidden due to permissions/`, and skip null note nodes.
- 2026-07-19: **`visitEditAssignedUsers` correct shape is `(visitId: ..., input:{assignedUserIds:[...]})`** — optimize-week.mjs used `(id:, input:{assignedUsers:})` and all 103 tech-assignment writes failed (schedule writes, which run first, succeeded). The correct shape was already in MEMORY.md env notes — scripts must be checked against known-good shapes before a big write run. Re-running the full write after the fix was safe: `visitEditSchedule` with identical values is idempotent (432/432 ok second pass).
- 2026-07-19: **14:00 PT email-freeze cutoff is enforced at write time and "today+1 after 14:00" locks permanently** — a 14:03 run silently held back all 93 Monday writes. Missing the cutoff by minutes is recoverable ONLY by explicit override (Spencer authorized; 93/93 ok + verified field-by-field afterward). For weekly runs: execute the write BEFORE 14:00 PT Sunday, or plan Friday.

- 2026-07-19: **visitDelete can 422 (empty body) on one visit of a recurring series** (Kisbye #7999 — likely the series anchor). Deleting the OTHER same-day duplicate succeeded; end state identical. Dedupe proven live: one visit per job per Mon-Sun week, keeper = completed > earliest > confirmed/committed-window (30 deletes, 0 bad). `visitCreate` with tech inherited from next scheduled visit: 19 creates, 0 fail.
- 2026-07-19: **Job Forms are a second source of truth the notes lane misses.** Spencer's weekly-comparison xlsx (form-based) caught 8 jobs the note audit didn't: form-only visits with NO JobNote (Taylor #7295), run-day notes (audit `--days-back` starts at yesterday — same-day notes invisible), and notes lacking a next-action line. Precedent (Cavanaugh #7527, per Spencer): **when form and note conflict, the form wins.** Form reports also have false positives (archived/invoiced jobs, already-correct 2-week visits) — always verify live before writing. xlsx parsing with zero deps: stdlib zipfile+ElementTree (scratchpad dump_xlsx.py pattern).

### tool-n8n
- 2026-07-31: **The "Jobber OAuth2 (native)" credential `NitrghZSAjZamvqp` is DEAD** (since ≤2026-07-24) — "The provided refresh token is not valid". It killed the daily Visit Notes wf `2dxtg73X1JUvLUTr` for 7 consecutive nights with nobody notified, and blocked the new lead-alert wf. Supersedes the 07-06 "prefer the native credential" advice: it keeps dying because it shares one Jobber app with the local CLI (see `## tool-jobber` 2026-07-31). Fix = dedicated Jobber app for n8n.
- 2026-07-31: **Diagnose a failing workflow from the executions API, not the UI.** `GET /api/v1/executions?workflowId=X&limit=N` then `GET /api/v1/executions/{id}?includeData=true` → `data.resultData.runData` gives the per-node error and `data.resultData.error.node.name` the failing node. This is how the "Gmail is broken" report was traced to the Jobber node two steps upstream. Only ~7 executions are retained per workflow, so check early.
- 2026-07-31: **There is no standalone "Generic OAuth2 API" credential in the sidebar** on this n8n Cloud version — it can only be created from *inside* an HTTP Request node (Authentication: Generic Credential Type → Generic Auth Type: OAuth2 API → Create new credential). Searching "OAuth2 API" without the word "Generic" sometimes surfaces it. Jobber values: auth URL `https://api.getjobber.com/api/oauth/authorize`, token URL `https://api.getjobber.com/api/oauth/token`, **Scope BLANK** (Jobber takes scopes from the app config, not the URL), **Authentication: Body** (Jobber wants client_id/secret as form fields — Header yields "client id and secret do not match an existing application").
- 2026-07-31: Credentials CAN be listed via `GET /api/v1/credentials` (id/name/type only — values are write-only), and a credential can be attached to a node programmatically by PUTting the workflow with `node.credentials = {[type]: {id, name}}`. Saves walking the user through the canvas. n8n Cloud has managed Google OAuth, so Gmail needs no Google Cloud project.
- 2026-07-06: This instance is **n8n Cloud** (`gotmoles.app.n8n.cloud`) = always-on → far better host for unattended weekly jobs than the local Agentic OS cron (which needs the laptop on). Built "Weekly Cash-Flow Projection (Jobber → Notion)" (id `Aly1V11tqwSQhTls`, inactive).
- 2026-07-06: **Reuse the existing `oAuth2Api` credential "Jobber OAuth2 (native)" (id `NitrghZSAjZamvqp`)** for Jobber calls — n8n manages token refresh/rotation itself, so no secrets-in-a-Set-node and no fight with the local install's rotating token. Working HTTP-node pattern (from the active Route Sync wf): `authentication:genericCredentialType`, `genericAuthType:oAuth2Api`, header `X-JOBBER-GRAPHQL-VERSION:2025-04-16`, `specifyBody:json`, `jsonBody` = an `={{ JSON.stringify({query:'...'}) }}` expression. Pagination = fixed cursor-CHAINED nodes (p1→p2→p3), each referencing `$('prev').first().json.data.invoices.pageInfo.endCursor` — they do NOT dynamic-loop.
- 2026-07-06: Don't pull 12 months (108 pages) weekly in n8n — pull only OPEN invoices (~6 pages) live and apply a STORED collection curve (seeded from the local model's `projection.json`), refreshed periodically. Notion output → a "Cash-Flow Snapshots" database (data source `8020d3e9-b096-4d08-a9a5-eeb848ab4c1d`), one row per run; simpler than rewriting page prose. Notion credential must still be connected in the n8n UI + DB shared with the integration.
- 2026-07-06: **PREFER the native Jobber credential `NitrghZSAjZamvqp` (oAuth2Api, Authentication=Body) for new Jobber workflows.** The other "auto" credential `414IO8xZGgF2ztgx` fails at token exchange ("Unsupported content type: text/plain"). Fighting that, I built a manual token-refresh path (a `Get Jobber token` HTTP node: form-urlencoded POST to `/api/oauth/token` with client_id/secret/refresh_token → `access_token`; downstream nodes use header `Authorization: Bearer {{ $('Get Jobber token').first().json.access_token }}`). It WORKS but stores secrets in the node and exposed them in chat via n8n's error panes. The native credential avoids all of that — switch to it next session (report wf `2dxtg73X1JUvLUTr`).
- 2026-07-06: **httpRequest v4.4 empty-body gotcha (n8n Cloud 2.27).** A `jsonBody` set to `={{ JSON.stringify({query:'... GraphQL with many braces ...'}) }}` sent an EMPTY body `{}` → Jobber 400 "Invalid query." Fix: for a STATIC query, set `jsonBody` to a literal JSON string (no `=` expression). For a DYNAMIC body, build the full `JSON.stringify({query})` string in a preceding Code node as a `body` field and set `jsonBody: '={{ $json.body }}'` — a simple reference has no nested braces to confuse the expression parser. Also send an explicit `Content-Type: application/json` header. (The other session's `={{ JSON.stringify(...) }}` pattern may work with the native credential; unverified on 2.27 — prefer the static/`$json.body` approach.)
- 2026-07-06: n8n workflow UPDATE via public API rejects `settings` carrying extra keys (e.g. `binaryMode`) — "settings must NOT have additional properties." On update, send `settings:{executionOrder:'v1'}` (or `{}`), not the raw settings object from GET. `timezone` IS accepted (used to pin the report wf to America/Los_Angeles).
- 2026-07-10: Report wf `2dxtg73X1JUvLUTr` rebuilt v2: switched to the native Jobber credential (token node with pasted secrets REMOVED), trigger fixed (was `{field:'hours'}` = hourly despite being named "Daily 6pm" — daily-at-18:00 needs `{field:'days',daysInterval:1,triggerAtHour:18}`), and ALL GraphQL bodies now built in Code nodes → `={{ $json.body }}` per the 07-06 empty-body learning. Pagination = p1 → Code builds p2 query from p1's endCursor → p2 (dedup makes a repeated page harmless when cursor is null). Per-item HTTP spacing: `options.batching.batch = {batchSize:1, batchInterval:500-800}` + node-level `retryOnFail`.

### ops-cron
- 2026-07-06: **Local cron runtime cannot launch Claude on this Windows machine.** `scripts/run-hidden-command.ps1` (the Windows hidden runner) is missing, and the default resolves `claude` to the extensionless npm shim which `spawn(shell:false)` can't execute → every job fails ~180ms with `spawn ...\npm\claude ENOENT`. Also `command-centre/node_modules` was missing (ran `npm install`), and the daemon doesn't load `.env`. Escalated to Roy (All The Power) — it's shipped-infra, don't hack it on a consumer install. Prefer n8n Cloud for scheduling here.
- 2026-07-10: **Stopgap that makes the local cron runtime work (no shipped-file edits):** set `AGENTIC_OS_CLAUDE_BIN=C:\Users\spenc\.local\bin\claude.exe` — a `.exe` value takes the "direct" launch mode in `cron-runtime.js` (`resolveWindowsClaudeLaunchPlan`), bypassing both the missing hidden-runner wrapper and the shim problem (the npm shim is now gone entirely; Claude Code migrated to a native install at `~/.local/bin/claude.exe`). The daemon does NOT read `.env`, so the var must be in its process env: persisted with `setx` (covers future shells/logons) AND exported in the launching shell for the current start. Verified live 2026-07-10. Roy's proper fix still pending.

- 2026-07-12: Marathon route rebuild. Key API facts: dateRange planning = PRO plan only; balancing ON benches trucks (use ON_FORCE); force-pinned overloads VIOLATE workTimeTo silently; SYNC'd orders drop to unscheduled until next start_planning; update_drivers_parameters bulk items = {driver:{serial},date,enabled|workTime}; create_order operation SYNC = safe idempotent upsert with visit-unique orderNos.
- 2026-07-12: PROCESS LESSON: don't let the optimizer re-solve an operation that works — mirror it (last completed week per customer = ground truth), pin (day,tech), optimizer sequences only. Owner hand-edits by job number beat constraint tuning; capture the approved board as home-slots.json template.
- 2026-07-12: visits date filter = interval-OVERLAP semantics (per-day totalCounts double-count spanning visits; window count is correct). visitEditAssignedUsers signature: (visitId:, input:{assignedUserIds:[EncodedId]}) — NOT id/assignedUsers. jobEdit accepts arrivalWindow (per-job sweep automatable). Backfilled 64,156 future visits with 7-7 windows via resumable state-file script; canary-first pattern worked.
- 2026-07-12: HTTP-node native pagination cannot merge a cursor into a raw GraphQL JSON body — use explicit Fetch->Code(step, staticData accumulator)->IF->Wait loop. Jobber OAuth cred died twice despite rotation OFF — durable fix = dedicated Jobber app for n8n (pending).

## General

- 2026-07-19: **Never "dry-check" a mutating script by trimming its output** — `script.mjs | head -2` still EXECUTES everything; only the printout is cut. This trashed all 14 just-uploaded Drive files mid-flow (recovered via Apps Script restore, IDs intact). A selection/mutation script needs an explicit `--dry-run` flag or a separate read-only probe; there is no shortcut.
- 2026-07-19: **The zero-touch-business pattern that worked end-to-end** (business launched in one day, ~70 Spencer-minutes): user does logins/KYC in the agentic Chrome once; Claude captures API tokens by field label into `.env` (never displayed) and does everything else via APIs — Gumroad product CRUD, Cloudflare zone/DNS/Worker deploy (site embedded in one Worker script, `cf-deploy.mjs`, no wrangler/git needed). Prefer platform APIs over UI automation the moment a token exists; UI automation is the bootstrap, not the runtime.
- 2026-07-26: **A second AI reviewer must be triaged, never relayed.** First real `/codex-check` run returned 3 findings: 1 genuine (a documented fix path with no Edit/Write granted), 1 false positive stated with full confidence (`model: fable` called an invalid alias — it is valid in this Claude Code version, and both agents were registered and live), and 1 thing Codex missed entirely that Claude caught (`codex review --uncommitted "<prompt>"` is rejected by the CLI — scope flags and a prompt are mutually exclusive, and stdin `-` collides the same way, so the documented invocation would have failed every run). Cross-review earns its keep, but the value is in *verifying each finding against the code first*; handing the raw output to the user would have shipped a wrong "fix" to a correct file. Build cross-review commands so the triage step is mandatory.
- 2026-07-26: **Rules that live in Claude-only files are invisible to every other agent.** Codex reads `AGENTS.md` natively but never `CLAUDE.md`/`CLAUDE.local.md` — so all the Got Moles guardrails (US English, I-713, pricing table, bid ownership, Jobber/OptimoRoute sharp edges) were absent until mirrored to `~/.codex/AGENTS.md`. Chose the global path over the repo because a repo-root `AGENTS.md` is a shipped file on this install. Sync is manual and therefore *will* drift — when adding a tool-specific instruction file, write down the sync obligation in the same commit or it gets silently lost.
- 2026-07-06: **Do not flip a live-data-writing automation to production until the WHOLE workflow is built and validated.** The Got Moles route automation shipped only the OptimoRoute→Jobber write-back (missing the Jobber→OptimoRoute ingest + re-optimize, and tech-assignment handling) plus a bad 7am–7pm placeholder step. Going live scrambled the real Jobber board (visits moved to wrong days, blanket placeholder times). "0 errors" on a run is NOT proof of correctness — verify what a real run actually CHANGES against a real day, and make the user review it before go-live. Go-live is a hard gate, not a flip.

## tool-browser
- 2026-07-20 (GSC): welcome-page domain input = `input[aria-label='example.com']`; buttons are DIVs (`div.U26fgb` + text match), not `<button>`. Verification dialog dropdown options are `[role=option]` — plain `.click()` works; full MouseEvent dispatch chains get flagged by the permission classifier, keep eval JS simple. TXT token lives in a TEXTAREA leaf (grab from there; innerText concatenates the "Copy" button label).
- 2026-07-20 (Cloudflare dash): DNS page URL `dash.cloudflare.com/{account}/{zone}/dns/records`. Add-record dialog: Type select = first `[role=dialog] [role=combobox]` button; Name = `input[placeholder*='root']`; Content = `[role=dialog] textarea` (cdp.mjs `type` fails on textarea — "Illegal invocation", use HTMLTextAreaElement value setter in eval). A hidden cookie-preferences dialog also matches `[role=dialog]` — filter listings accordingly. GSC "Ownership auto verified" fires instantly after the record saves (Cloudflare is authoritative).
- 2026-07-20 (Google Ads signup): new-account wizard has no upfront skip — advance to the campaign-type step, click the standalone "Skip" link, then confirm "Leave campaign creation" in the dialog to get a campaignless account. Manager-account signup URL: ads.google.com/signup?sf=manager (old /um/mcc/signup and /mcc/signup are dead). API Center form + support.google.com application forms use NAMED inputs (input[name=gcp_number] etc.) — easiest fills on any Google property. DOM.setFileInputFiles via raw CDP works for support-form file uploads (adapted drive-inject-files.mjs).
- 2026-07-20 (classifier map): auto-mode classifier reserves for the human: publish/unpublish + delete clicks (Gumroad), payouts toggles, API-token permission edits (Cloudflare — even DOM reads near the button), registrar DNSSEC form fills (Squarespace), ToS/T&C checkboxes, captchas. Pattern that works: agent fills everything fillable + puts the exact page on screen + hands a numbered click list with values; human clicks land in seconds. Budget wizard/GCP first-run: project create silently fails until GCP ToS accepted — accept ToS first, then create.
- 2026-07-22 (Ninety.io org chart): public API (api.public.ninety.io/v1) has NO org-chart/seats/users endpoints (only to-dos/issues/rocks/milestones/scorecard/teams) — org chart must go through the browser at app.ninety.io/chart/primary. Seats are `NINETY-SEAT` elements (`data-seat-id`); open the View Seat panel via `[data-cy=seat-card_edit-seat-button] button`. Role titles + descriptions live in INPUT/TEXTAREA values inside `aside.detail-wrapper` — innerText misses them entirely, read `.value`. Google-managed-account sign-in pops a `chrome://managed-user-profile-notice` tab that becomes cdp.mjs's target — close it via `fetch(BASE+'/json/close/'+id)`.
- 2026-07-23 (CallRail app): session persists in the agentic profile (Jobber + Google expired same day — check per-site, not globally). Create-number wizard: Settings → Tracking → Numbers → "Create number" button → `/tcw/a/{acct}/create-number`. Custom dropdowns are Angular `button.dropdown-toggle` opened by click, options are `li a` items — click the anchor, not the text node. Option cards ("Somewhere else", "Google Ads Assets") are leaf text nodes; click via `closest('a,button,[role=button],[tabindex],li,label')`. Wrap eval JS in an IIFE — bare `const` redeclares error across successive eval calls. `shot` intermittently hangs on this SPA; `text`/`eval` are reliable.
- 2026-08-04 (CallRail Call Flow Builder — DO NOT automate): the flow builder's Angular form rejects scripted input entirely. Checkbox `.click()`, native value setter + input/change on the `<select>`, and a real CDP mouse click on Save all left the UI showing the new values, but Save submitted the ORIGINAL state every time — confirmed unchanged via the API on four separate attempts. The form never marks dirty. Number-level settings (whisper, on the Call options card) DO save with the same technique, so it is the flow builder specifically. Hand flow-step edits to the human and verify after via `trackers.json`.
- 2026-08-04 (CallRail nav map): numbers + routing = `/settings/a/{acct}/routing/calls-and-texts/numbers/active`; number edit = `.../numbers/{id}/edit`; flow builder = `/cfb/a/{acct}/call-flow/{uuid}/edit`; Voice Assist config = `/settings/a/{acct}/workflow/voice-assist` (Business profile / Conversation settings / Lead intake questions at `.../voice-assist/caller-intake`). Whisper is per-NUMBER (Call options card), not per-flow, and supports `[source]` / `[tag]` tokens — one string works across all numbers. Section pencils are `button.btn.btn-icon` in DOM order matching the card order. Guessing URLs 404s; enumerate `a[href]` from the home page instead.

## tool-jobber
- 2026-07-21 (bulk pulls): 24-mo invoice pull (8.7K rows w/ lineItems first:15) hits the query-cost throttle hard — expect "Throttled" every ~10-15 pages; 60s backoff + cursor-state resume (pull-diagnostic.mjs pattern) grinds through in ~45 min. clients/quotes/visits at first:75 without nesting barely throttle. Filters verified: invoices=issuedDate range, quotes=createdAt, visits=startAt (all Iso8601DateTimeRangeInput). Client has leadSource/sourceAttribution/utmMedium/tags/balance — attribution + AR live on the Client object. Quote statuses: draft/awaiting_response/archived/approved/converted (close rate = (approved+converted)/non-draft).

- 2026-07-23: No quote-send/resend mutation exists in the GraphQL API (only quoteCreate/Edit + line-item/note ops) - to "resend" a quote, fetch its clientHubUri and email the client-hub link directly.
- 2026-07-23: client.requests can fail whole-query with "An object of type Request was hidden due to permissions" (app lacks read-requests scope) - omit requests from client queries; a hidden-object error still proves a request EXISTS.
## ops-google-ads

- 2026-07-22 (Route Ready build): REST v23 field-name traps — Maximize Clicks bidding is `targetSpend` (not `maximizeClicks`); geo campaign criterion is `location: {geoTargetConstant}` (not `geoTarget`). Both fail with "Unknown name ... Cannot find field".
- 2026-07-22: NEVER use a bare criteria-count as an "already configured" check — new campaigns auto-create 3 DEVICE criteria, so the check passes while geo/language/negatives are missing (campaign would target WORLDWIDE). Check by criterion type instead, and always re-audit live state after a partial/resumed build (also caught an orphan duplicate budget).
- 2026-07-22: Route Ready creds are namespaced `ROUTE_READY_ADS_*` in the env file; generic `GOOGLE_ADS_*` reserved for Got Moles. MCC link acceptance can be done fully via API from the client side: query `customer_manager_link`, then `customerManagerLinks:mutate` status→ACTIVE with `login-customer-id` = the CLIENT id.
- 2026-07-23 (Got Moles phone plumbing): the account's call asset carries the raw main line 253-750-0211, so with call reporting ON, Google displays its own forwarding number (253-553-6866) on ads and forwards straight to 0211 — bypassing CallRail entirely (zero "Google Ads" source in CallRail's 506-call sample). Fix pattern: dedicated CallRail source tracker (`google_ad_extension`) as the call-asset number; Google's GFN layer stays on top so call conversions keep counting. GFN callbacks from customers' call history keep forwarding while ads run and inflate "Calls from ads". NOTE: this install's `GOOGLE_ADS_*` creds return `invalid_client` — the asset swap needs creds set up or the Ads UI; audit script staged at `scripts/_gm-call-asset-audit.mjs`.

## meta-wrap-up

- 2026-07-23: Routine wrap, no user feedback (credit-limit day — kept wrap minimal per Spencer's 'take it easy'). Kit 2/3 build parked mid-flight with file-level resume state in the daily log; that pattern (state block in memory before stopping) made the pause cheap.
- 2026-07-26: **Four concurrent sessions, and `context/memory/2026-07-26.md` did not exist at wrap-up** — three in-flight sessions had written nothing, so the daily log had to be created from scratch rather than finalised. Wrote a header naming each concurrent session ID and instructing later sessions to number after the highest existing block instead of renumbering, plus a "Cross-session notes" section so the stopped cron runtime and the other sessions' pending decisions are not lost when they close without wrapping. On a multi-session day, check `{date}.aos.md` for other session IDs before assuming the tree is yours — the working tree was 90% other sessions' work, so the commit had to be bundled and labelled, matching the existing convention in this repo's history.

## ops-phone-roleplay

- 2026-07-27: Skill was on disk since 2026-07-20 with **no learnings section** (it is registered in `CLAUDE.local.md`, not the shipped AGENTS.md registry, so reconciliation never picked it up). Section created at this wrap-up.
- 2026-07-27: Built Tier 4 (scenarios 16–20) to test mole knowledge rather than price recall. The design lesson: a training scenario is only useful if the *pass condition* is a behavior, not a fact — #16's pass is "escalate to Spencer" precisely because GOT-MOLES.md forbids any I-713 compliance claim, so there is no correct answer to memorize. Wrote the same guardrail into the skill's hard-gate list in both directions ("we're compliant" AND "that law doesn't apply to us"), because a trainee under pressure will reach for the second one.
- 2026-07-27: Added a **bluffing gate** — a confidently wrong mole fact now scores below "I don't know, I'll get that from Spencer." On real calls the wrong fact travels to the customer; the honest deflection does not. Paired with a mole knowledge answer key in the pack so the grader has ground truth instead of grading biology from memory.
- 2026-07-27: PDF regeneration on this box needs the Chrome-headless route (no pandoc, no weasyprint). A ~140-line dependency-free Node md→HTML converter + `chrome --headless=new --print-to-pdf` works; verify structurally afterwards (count `<h3>`/`<table>`, grep for leaked `**`) since there's no rasterizer for a visual check.
- 2026-08-06: **Grade the trainee against where they are, not against the expert.** Call #1 was scored on Spencer's twenty-year benchmark and read as a list of failures; Spencer corrected it ("these are the first mole calls he's ever done"). The rescored version — same evidence, trajectory framing — was the one that actually drove behavior change. Added a trainee-baseline section to the coach so it opens with what the trainee already does well.
- 2026-08-06: **When a trainee has the facts but not the delivery, the fix is sequencing, not teaching.** All five education beats were already in Muhammad's pack, spread across eight places in two docs, always framed as answers to questions. Converting them into one memorized 60-second monologue said BEFORE the price took him from 0/5 beats to 5/5 in two days. Diagnose "reactive vs volunteered" before assuming a knowledge gap.
- 2026-08-06: **A pass ceiling beats a hard gate for a behavior you want built rather than punished.** Skipping the block caps a call at 79 instead of gating it to 60 — it makes the behavior the obvious thing to go fix without reading as punitive. Hard gates stayed reserved for things that damage a real customer.
- 2026-08-06: **Repeated misses usually mean the fact is filed wrong, not under-drilled.** The no-catch guarantee was missed in 6 of 7 calls because it was learned as a *deposit* (a cost) alongside two other $150 facts, so under pressure the wrong one was retrieved. Fix is re-filing — put the guarantee inside the price sentence so it can't be dropped without stopping mid-sentence — not more reps.
- 2026-08-06: **Give an admin field a job or it drops under pressure.** The address was captured cleanly for two calls, then vanished when a caller was in a hurry. Moving it to first and attaching it to the service-day lookup fixes the address miss AND the seven-for-seven habit of naming a day without checking — same underlying bug.
- 2026-08-06: **Same rules in two files will drift.** The coach ran in two places (this skill + Muhammad's ChatGPT project) and the rubric was duplicated three ways. Fixed by making `muhammad-portable/project-instructions.md` canonical, with SKILL.md carrying a tracked supersede banner and SKILL.local.md holding only filesystem-dependent runtime. Note `**/SKILL.local.md` is gitignored — never put a load-bearing pointer only there.
- 2026-08-06: **Mine the real call archive for scenarios rather than inventing them.** 347 CallRail transcripts yielded 422 caller questions; theme-bucketing surfaced the hard ones and the long tail held the genuine curveballs (artificial turf, "what if I don't want dead moles in my trash", a 24-minute rapport call that name-drops competitors and angles for a custom rate). Twelve scenarios built this way, each with its source call ID.

## technician-route-automation (route pipeline)

- 2026-07-26: **Order of operations is load-bearing: move in Jobber → push-week --grid → lock-techs → re-plan → write.** Skipping the re-push lets the stale OptimoRoute plan overwrite the move on write-back — it silently reverted a job to its old day and looked like the move "didn't take".
- 2026-07-26: **`write --date X` only writes changes TARGETING X.** Visits sitting on X that the plan moves AWAY are keyed to their destination date, so scoping a write to one day leaves that day double-loaded — Monday ended at 147 stops against a 99-stop plan (~50% over) and would have gone to customers that way. Always reconcile Jobber vs OptimoRoute per day after a scoped write (`verify-week.mjs`).
- 2026-07-26: **Compare tech assignment against ALL assigned users, not `curTechs[0]`.** Crews ride two-up; a visit assigned `[Franks, Cory]` returns the ride-along first, so a first-element test reports a tech change forever — 55 writes replayed every run and never converged.
- 2026-07-26: **Ride-alongs must be preserved on write-back.** `visitEditAssignedUsers` REPLACES the assignee list, so writing the driver alone strips the crew member off ~100 visits/week. Write `[driver, ...rideAlongs]`.
- 2026-07-26: a `--from=` window override MUST also move the Jobber query bound (`afterIso`) — otherwise the overridden days are absent from the visit fetch and every stop on them is misreported as an orphan (92 of them).
- 2026-07-26: **committed/pinned visits ignore grid-day enforcement** (`pinned = isSet || (time set && window<=6h)`). A visit with a real time stays on its own date no matter what the grid says — that is why hand-set visits strand on the wrong day. Set them all-day (00:00–23:59) to hand them back to the optimizer.
- 2026-07-26: grid entries with `decided:false` are auto-assignments, not the user's call — three peninsula zips (98312/98335/98367) were silently wrong there and re-broke the same route every week. Audit `decided:false` before trusting a territory.
- 2026-07-26: **split an overfilled tech-day onto that SAME tech's light day**, never onto another tech (Spencer's rule) — territories are 40-60 mi apart, so cross-tech "balancing" just adds drive time.
- 2026-07-27: end-of-day, `verify-week` will show a huge Jobber/OptimoRoute mismatch for TODAY — completed visits drop out of the live count. Not a defect; the residual IS the miss list worth reporting (Mon 7/27 closed 96/101, the 5 misses were the tail of an over-long route).

- 2026-07-27: Route-finalization session spanning two days (7/26 morning → 7/27 23:00) with several concurrent sessions in the tree again. No `{date}.md` existed for 7/27 and the route session had no `## Session N` block anywhere, so the daily log was created fresh at wrap. Lesson repeated from 7/26: **on a multi-session day the working tree is mostly other sessions' work** — bundle and label the commit, and describe only your own session's changes in the summary.
- 2026-07-27 (Route Ready, reporting-window trap): `segments.date DURING LAST_7_DAYS` EXCLUDES today, so a campaign that began serving today reports $0 spend and reads as broken. The weekly cron flagged "did the funding take effect?" purely from this. Always cross-check `THIS_MONTH` (or TODAY) before diagnosing a serving fault. Also: daily spend can exceed the daily budget (Google's 2x allowance) — only the monthly total is capped, so a $8/day budget showing $11.01 in a day is normal, not a guard failure.
- 2026-07-27 (bid reality vs keyword planner): KeywordPlanIdeaService top-of-page bids for this niche read $7.77-$14.39 (median $10.80) and implied paid search was unviable. Real auction CPC once serving was **$2.75** with 4.88% CTR. Planner top-of-page estimates are the price of position 1, not the price of a click — do not kill a channel on planner bids alone; get 24h of real auction data first.
- 2026-07-27: Gumroad/Google Ads gate order that worked — keep ad groups PAUSED until the product is verified purchasable, then enable. Serving only unlocked once all 3 ad groups were live alongside the raised ceiling; a single enabled ad group at a $1.25 ceiling produced 0 impressions for 3 days.
- 2026-07-29: Shipping a training-pack UPDATE is a two-part handoff — new instruction text AND a file swap in the trainee's Claude project. Pasting updated instructions alone leaves the coach referencing scenarios that aren't in its uploaded knowledge (his project still held the 15-scenario file). Put the swap steps in the portable README itself, not just in the message, and zip the folder so the whole thing travels as one artifact.

- 2026-07-29: **The optimizer is not the lever — the constraints are.** Measured four configurations against 434 real visits using OptimoRoute's OWN road miles: current production 2,003 mi; full from-scratch re-sequence 2,005 (zero gain); days freed so the optimizer picks the day 2,415 (**+20% worse** — the territory grid is earning its keep); balancing OFF 1,978 (1% gain). Total available from tuning OR's knobs is ~1%. Stop chasing the optimizer; the waste is fragment send-outs (14 stops burning 258 mi = 3% of stops for 13% of miles), split-territory days, and 12.1h/week of commute-to-first-stop that OR's objective ignores.
- 2026-07-29: **Never judge route quality with straight-line distance in Puget Sound.** A 2-opt on haversine reported 7-27% "slack" on live routes; the controlled re-solve showed almost none — the metric was cutting across Lake Sammamish and the Sound. Same trap in territory design: haversine put Gig Harbor on the Redmond tech and Olalla on the Kent tech. Use OR's own road miles to compare, and barrier penalties (Lake Washington +10, Narrows +25, Lake Sammamish +5) plus a gateway rule (peninsula follows the day holding west Tacoma) to design.
- 2026-07-29: **`stop[0].travelTime` is the commute BEFORE the day's span.** Counting it in a feasibility check makes every route read as impossible — I briefly reported all 17 routes as over-committed on that error. Span runs first arrival → last arrival, so compare against travel for stops 2..n plus service for n-1 stops.
- 2026-07-29: **Nobody uses the OptimoRoute driver app** — `get_completion_details` returns `status: "scheduled"` with no start/end times and no tracking for every stop. So OR has never learned a real service or travel time, it plans against a hardcoded 10-min-per-stop guess, and there is no way to verify a route was driven or to prove a fuel saving. Prerequisite for any real optimization work.
- 2026-07-29: **When the office hand-assigns work the grid doesn't know about, the GRID is the stale side, not Jobber.** 14 Thursday visits were assigned to Spencer in Jobber for zips the grid still gave Cammeron/Cory; a grid-locked run tried to move all 14 back. Added `lock-techs-to-jobber.mjs` — locks OR to Jobber's current tech AND pins each order to its own day, so the optimizer can only re-sequence. That is the correct tool for "the techs are right, the times are wrong".
- 2026-07-29: `lock-techs-to-grid.mjs` was pinned to a hand-made visit snapshot, so anything booked after that snapshot got pushed to OptimoRoute but silently skipped the tech lock. Any script that filters by a captured file needs a `--visits=` refresh path (`fetch-window-visits.mjs`).
- 2026-07-29: **OptimoRoute has no driver-creation API** — `create_driver` 404s while `get_drivers`/`get_vehicles` exist as GETs. A new tech must be added in the web UI, and the OR driver name must match the Jobber user's full name EXACTLY or the tech write-back (`users[driverName.toLowerCase()]`) silently finds nobody.

- 2026-07-31: **`weekMonday()` rolled a weekend start date BACKWARDS six days, silently disabling the whole territory grid on the Friday baseline run.** The window opens Sat/Sun, so `--grid` mapped every grid day onto the week just gone, where the dates fell outside the window and were discarded — 0 of 434 orders landed on a grid day and everything floated. It reports as "grid-day frozen", not as an error, and the symptom looked like a push that died part-way (lopsided days: Mon 112, Thu 60). Fix: Sat→+2, Sun→+1, weekdays→back to Monday. Sanity check after any `--grid` run: `forced` should be ~all flexible orders and `frozen` ~0.
- 2026-07-31: **A serial alias is a write-back hazard, not just a measurement shim.** Grid v4 put Alias Franks on Spencer's OptimoRoute serial, so OR reported "Spencer Hill" on every one of Alias's stops — `optimize-week write` would have assigned Spencer those visits in Jobber, the exact opposite of taking him out of the field. Reverse the alias before any Jobber write, and abort outright if the borrowed serial's owner is also in `works` (both techs' stops come back under one name and are indistinguishable).
- 2026-07-31: **Jobber has NO job-level assignment mutation** — `JobEditInput` carries no assignedUsers field; `visitEditAssignedUsers` (one visit at a time) is the only write path. So when a tech leaves the field, every pre-generated visit keeps pointing at them and the board shows coverage that does not exist: Tavis Alexander had 3,982 such visits running to Dec 2027, flat at ~230/month across 240 recurring jobs. An API sweep repairs existing visits but cannot stop new ones — the recurring job's tech is editable only in the Jobber UI. Build the weekly run to self-heal (`--fallback=grid`) rather than relying on a one-time sweep.
- 2026-07-31: **Driver availability has to track the roster or the optimizer ignores the grid.** `set-driver-days.mjs` was hardcoded to the week of 07-27 with Spencer enabled Thu/Fri; OptimoRoute hands stops to any ENABLED driver regardless of balancing mode, so he kept collecting work (2/3/7/9/16 stops) months after being taken out of the field. Derive availability from the grid's `works` + `notWorking` per week. It also doubles as a driver-record existence check — `update_drivers_parameters` addresses drivers by serial, so a misspelled or missing driver fails loudly instead of routing nobody.
- 2026-07-31: **push-week can create but never retire.** A cancelled or moved visit leaves its OptimoRoute order behind, consuming route capacity and drive time for a customer nobody is visiting. Added `prune-stale-orders.mjs` (own `<job>-<visit>` orderNo pattern only, foreign orders reported not touched, aborts if >50% of the window looks stale). Gotcha: `search_orders` nests the body under `data` — `o.orderNo` is undefined at the top level and silently classified all 434 orders as foreign on the first run.

## technician-route-automation
- 2026-08-01: **Never infer a business rule from the shape of the data.** A start time with a short
  window looked like a committed appointment, so the pin rule treated it as one. They are
  placeholders. That single wrong assumption pinned 28 visits off their grid day, produced two
  scripts and a standing rule built on a false premise, and cost hours of rework. One question at the
  start would have avoided all of it.
- 2026-08-01: **Move zips, not judgement.** Hours were spent hand-picking territory boundaries from a
  mental map, which is exactly how a tech ends up driving 30 minutes into another tech's cluster. The
  coordinates were available the whole time. Insertion cost — (A->job + job->B) - (A->B) — is the
  right metric and it is cheap to compute. Build the measurement, don't eyeball the map.
- 2026-08-01: **A zip can straddle a boundary.** 98371 and 98372 each split Edgewood/Puyallup across
  SR-167, so "move Edgewood" can never be a clean zip operation. Check city labels inside a zip before
  treating it as one unit.
- 2026-08-01: `optimize-week write --date=` only writes visits arriving INTO that day. With a plan
  that moves visits between days, everything leaving stays put with its stale tech — 53 visits
  stranded on 2026-08-03, 11 on Tavis, and 52 customers had to be emailed a new day. Guard added:
  the write now refuses when the plan moves anything off the scoped date.
- 2026-08-01: `balancing=ON_FORCE` lets OptimoRoute reassign stops between drivers AFTER the grid has
  assigned them. Grid tech assignment was effectively advisory. Plan with `--balancing=OFF` when
  territory ownership matters; ON_FORCE only when day-levelling needs forcing.
- 2026-08-01: A job override pinned to a day silently outranks a later zip move, holding part of a
  zip behind when the zip changes day. Flag overrides whose day disagrees with their zip's grid day.
- 2026-08-01: Guards earn their keep. The duplicate-zip check caught a double assignment; the
  survivor check and id-resolution guard caught two separate parser bugs in the delete tooling before
  anything was deleted. Write the guard before the live run, not after.
- 2026-08-05: **Ask which system is truth BEFORE writing a reconciler.** Three tools were run in one
  evening pulling in opposite directions — grid→Jobber, Jobber→OptimoRoute, OptimoRoute→Jobber. None
  was buggy; they encoded contradictory answers to a question never asked. One sentence from Spencer
  ("Jobber is the truth, OptimoRoute is the dry run") made 200 orders of realignment work obsolete an
  hour after it ran. When two systems hold the same field, the ownership question comes first.
- 2026-08-05: **Measure a baseline against the right era.** Continuity looked 50% broken until the
  roster history was checked: before 2026-08-03 there were 3 routes, Alias rode with Cory, 21% of
  visits carried two assignees, and two techs since departed were still on the board. "Who served
  them last" was unanswerable. Against the correct baseline the real figure was 260/268 = 97%, and
  the proposed fix — moving 110 customers — would have reversed a deliberate territory decision and
  produced a 44-stop day. Check what the data meant when it was written before acting on a delta.
- 2026-08-05: **A half-built week is not a broken week.** Forward days averaged 6.6h and looked
  badly under-filled; the conclusion drawn ("work fits in 4 days") was wrong. Wed/Thu/Fri simply
  had not been populated yet — each completed visit generates the next one, so a forward week fills
  in as the current one runs. Do not benchmark capacity on a week that is still assembling itself.
- 2026-08-05: **`LEAVE` in a planner does not mean "unscheduled."** It means the engine took no
  action. Reading it as "customer has no visit" produced an impact estimate roughly double the truth
  (~80/week claimed vs ~45 real). Check the resulting state, not the decision label.
- 2026-08-05: **Idempotency windows must be one-sided when only lateness is the defect.** A
  symmetric ±3-day check treated a visit scheduled EARLIER than target as unhandled and stacked a
  second one on top — three visits in a week for #5300, none billed. Sooner is fine; only a longer
  gap needs filling.
- 2026-08-06: **A territory change is not live until the ASSIGNMENT pass re-runs.** territories.json
  is only a map; Jobber holds the actual tech on each visit, and the planner reads Jobber. Moving
  Burien/SeaTac to Cory in the file and then re-planning left the block on Luke on the map, because
  his name was still on those visits. Order is always: edit territories.json -> assign-by-territory
  -> rebalance/plan. Skipping the middle step produces a plan that looks wrong for reasons that are
  not in the plan.
- 2026-08-06: **OptimoRoute balances between DRIVERS, never across DAYS.** Given a week-wide
  `allowedDates` it packs the work into as few days as it can, because dropping a day saves a whole
  home->territory trip — 487 visits collapsed into four 10.5 h days with an empty Friday. It is
  optimising correctly for something nobody asked for. Nothing in the API says "we work five days":
  `allowedDates` is a from/to RANGE so "Mon and Fri only" is inexpressible, and the daily work-time
  cap is web-UI only on this account (`workTimeFrom/To` need `externalId`, blank for every driver).
  The fix is to CHOOSE the day yourself from the region rhythm and pin each order to it, leaving the
  optimizer to sequence within the day — the one part it cannot get wrong.
- 2026-08-06: **Driver availability is per DATE, not standing** (`update_drivers_parameters`
  `{driver:{serial}, date, enabled}`). A day nobody is enabled for silently vanishes from planning
  rather than erroring. Enabling it also unschedules existing routes, so always re-plan straight
  after. Same shape as a missing driver record: configuration absent, not wrong, and silent.
- 2026-08-06: **Check the roster exists in BOTH systems before designing around it.** A whole
  territory was built for Robert Norton, 132 visits assigned, and the sync then failed verification
  because he had no OptimoRoute driver record — `ERR_DRV_NOT_EXISTS`. His absence was visible in
  every sync output all day ("OR drivers: alias, cammeron, cory, luke"). `get_drivers` is not
  available on this key and `get_routes` only shows drivers who already have stops, so a driver with
  no work is invisible; probe by assigning one order and reading the response.
- 2026-08-06: A hand-maintained "confirmed drivers" list went stale within minutes of Spencer adding
  a driver and wrongly blocked the run. Config lists describing external state need a live probe
  behind them, or they become the thing that lies.
- 2026-08-06: **A region rhythm can stack a tech's day.** Luke's rhythm put Tacoma, Lakewood and
  Burien all on Tuesday — 59 visits. Treat the rhythm as a PREFERENCE with spillover to the tech's
  emptiest day once a rhythm day hits its share, or honouring the pattern produces a 12-hour day.
- 2026-08-06: **Balanced visit counts are not balanced days.** After levelling to 22-28 visits/day,
  Luke still ran 10.9-12.7 h against Cory's 6.6-9.1 — he owns the four most expensive regions by
  drive time (peninsula 18 min/stop, Graham 13, Lakewood 13, Thurston 11). Balance on MINUTES, not
  stops, or the tech with the rural territory absorbs the difference silently.
- 2026-08-05: Commute is invisible in route totals and worth auditing separately. 22% of all weekly
  miles were the drive to the first job (Cory 36%, 161 mi/wk). Route-level distance hides it because
  each route still looks internally tidy — the waste is in which cluster the driver was given, not
  in the sequence.

## General
- 2026-08-01: When the user says a task "should be simple" and keeps correcting the same class of
  error, stop solving the instance and build the measurement that implements their rule. Spencer's
  rule — "overflow goes to the closest tech, only the closest jobs" — was computable from data
  already in hand. Twenty round-trips of hand-moving zips preceded writing the twenty-line function
  that did it properly.
- 2026-08-04: **Two call-routing engines in series silently swallow calls.** CallRail's flow hands
  off to the softphone's flow, and ANY action the softphone takes — answering to run its own ring
  logic, an auto-greeting, voicemail — reads to CallRail as "call answered." CallRail then exits its
  flow, so its timeout and Voice Assist fallback never fire, and nothing errors: the call log shows
  `answered=true` and looks healthy. Debug rule: **whichever endpoint answers first wins**, so every
  downstream voicemail or auto-greeting must be slower than the upstream timeout, or deleted
  outright. Corollary: never leave two timers matched — the original bug was CallRail 30s vs Quo
  30s, a dead heat Quo won.
- 2026-08-04: **Test live routing changes on an inert number with its OWN flow.** All 8 Got Moles
  trackers shared a single call-flow object, so "just testing" an edit would have re-routed every
  live lead across the website pool, three GBPs, Bing, Facebook and the referral line. Cost of
  isolation was one tracker with zero calls in 90 days. Re-verify production is untouched via the
  API after every change, not just at the end.
- 2026-08-04: **Verify the vendor's own diagnosis before accepting a dead end.** Concluded "Quo is
  the wrong tool, switch to JustCall (~$696/yr)" on the strength of broken DTMF — then Quo support
  identified it as an inbound-media problem, and a cellular-vs-wifi test proved it was the agent's
  home router. JustCall uses the same UDP media path and would have failed identically. When a
  symptom is "outbound works, inbound doesn't," suspect the network before the product.
